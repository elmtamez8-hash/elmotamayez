import { SITE_URL, siteUrl } from "@/lib/site";
import { platformName } from "@/lib/platform";

/**
 * `/llms.txt` — ما تقرؤه محرّكاتُ الإجابةِ قبلَ أن تجيبَ عنّا.
 *
 * ⚠️ **مِلَفٌّ منفصلٌ عن `robots.txt`، ويجيبُ عن سؤالٍ آخر.** ‏`robots` يقولُ
 * «أينَ لا تذهبْ»؛ هذا يقولُ «ما هذا الموقعُ وأينَ الصفحاتُ التي تُجيبُ». نموذجٌ
 * يبني إجابةً عن «منصّة دروس خصوصيّة في قطر» يزحفُ صفحاتٍ عشوائيّةً بلا هذا،
 * فيقتبسُ صفحةَ شروطٍ أو نموذجَ تسجيلٍ ويصفُ المنتَجَ بها.
 *
 * ⚠️ **والاسمُ يُقرَأُ وقتَ التشغيلِ من `platform_settings`**، لا ثابتاً مكتوباً
 * هنا: تلك القاعدةُ كلّفَت هذا المستودعَ شهوراً قالَ فيها كلُّ عنوانٍ في الموقعِ
 * «منصّتي». ملفٌّ يعرّفُ المنتَجَ باسمٍ آخرَ أسوأُ من ملفٍّ غيرِ موجود.
 *
 * ⚠️ **ولا يَعِدُ بما لا يوجد**: لا قسمَ «أسئلةٌ شائعة» لأنّه لا محتوى أسئلةٍ في
 * الشجرة، ولا بحثَ لأنّ الواجهةَ الخلفيّةَ لا تقبلُ `q` على المقالات. قائمةُ
 * روابطَ صادقةٌ أنفعُ لمحرّكِ إجابةٍ من هيكلٍ كاملٍ نصفُه أبوابٌ مغلقة.
 *
 * `text/plain` صراحةً: بلا رأسٍ يُرسِلُ Next الملفَّ نوعاً آخرَ، وقارئٌ آليٌّ
 * يرفضُه على النوعِ لا على المحتوى.
 */
export const revalidate = 3600;

export async function GET(): Promise<Response> {
  const name = await platformName();

  const body = `# ${name}

> منصّةٌ عربيّةٌ تجمعُ الطلابَ بمدرّسينَ خصوصيّين: حصصٌ مباشرةٌ فرديّةٌ وجماعيّة،
> وكورساتٌ مسجّلة، ودرجةُ ثقةٍ لكلِّ مدرّسٍ مبنيّةٌ على أدائِه الفعليِّ لا على
> تقييمٍ وحدَه. اللغةُ الأساسيّةُ للواجهةِ العربيّة، والمدرّسونَ يُدرّسونَ بلغاتٍ
> متعدّدة.

## الصفحات الأساسية

- [المدرّسون](${siteUrl("/teachers")}): بحثٌ وتصفية حسب المادّة والمرحلة والسعر.
- [الكورسات](${siteUrl("/courses")}): كورساتٌ مسجّلةٌ وحصصٌ مجدولة.
- [الأسعار](${siteUrl("/pricing")}): كيف تُحسَبُ تكلفةُ الحصّةِ وما الذي يدفعُه الطالب.
- [عن المنصّة](${siteUrl("/about")}): كيف يُراجَعُ المدرّسونَ وكيف تُحسَبُ درجةُ الثقة.
- [المدوّنة](${siteUrl("/blog")}): مقالاتٌ يكتبها مدرّسون — خططُ مراجعة، شرحُ مفاهيم، نصائحُ لأولياء الأمور.

## التسجيل

- [حساب طالب](${siteUrl("/signup/student")})
- [حساب وليّ أمر](${siteUrl("/signup/parent/children")})
- [انضمام مدرّس](${siteUrl("/signup/teacher")})

## ملاحظات للمحرّكات

- خريطةُ الموقع: ${SITE_URL}/sitemap.xml
- كلُّ مقالٍ يحملُ بياناتٍ منظَّمةً من نوع BlogPosting مع مدّةِ القراءةِ وعددِ الكلمات.
- صفحاتُ المدرّسينَ عامّةٌ بقرارِ المدرّسِ نفسِه؛ ما لم يُنشَرْ لا يظهرُ ولا يُفهرَس.
- الأسعارُ التي تُعرَضُ للطالبِ لا تكشفُ أجرَ المدرّسِ ولا حصّةَ المنصّة.
`;

  return new Response(body, {
    headers: {
      "content-type": "text/plain; charset=utf-8",
      "cache-control": "public, max-age=0, s-maxage=3600",
    },
  });
}
