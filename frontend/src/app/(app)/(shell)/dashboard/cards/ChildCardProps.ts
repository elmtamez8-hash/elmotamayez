/**
 * ما تحتاجُه كلُّ بطاقةِ ابنٍ في لوحةِ وليِّ الأمر.
 *
 * ⚠️ **الاسمُ خاصّيّةٌ ولا يُجلَبُ من الخادمِ في البطاقة.** كلُّ العناوينِ كانت
 * تقولُ «ابنك» — وذلك خطأٌ من وجهَين: وليُّ أمرٍ له ثلاثةُ أبناءٍ يقرأُ خمسَ
 * بطاقاتٍ لا تقولُ أيَّهم يرى (بلاغُ ٢٠٢٦-٠٩-٠٧)، **ووصيٌّ ليس أباً** — و
 * `relation_type_label` في هذا المستودعِ يعرفُ الفرقَ منذُ ٠١٣. والاسمُ في يدِ
 * `GuardianDashboard` أصلاً من `‎/family/relations`، فجلبُه هنا طلبٌ ثانٍ عن
 * شيءٍ وصل.
 */
export type ChildCardProps = {
  studentUuid: string;
  studentName: string;
};
