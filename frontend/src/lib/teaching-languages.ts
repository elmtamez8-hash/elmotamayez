/**
 * اللغاتُ التي يُدرَّسُ بها — نسخةُ الواجهةِ من `TeachingLanguages` في الخادم.
 *
 * ⚠️ نسختانِ في زمنَي تشغيلٍ مختلفَينِ قرارٌ مقصودٌ ومكتوبٌ في الصنفِ الآخر: لغةٌ
 * رابعةٌ ليستْ صفّاً في قاعدةِ بيانات، بل ترجمةُ المنتَجِ كلِّه، وشاشةُ إدارةٍ
 * لثلاثةِ صفوفٍ جدولٌ لا يُحرِّرُه أحدٌ وباذرٌ لا يتذكّرُه أحد.
 *
 * ⚠️ أمّا نسختانِ في زمنِ التشغيلِ **الواحد** فعطبٌ لا قرار: القائمةُ كانتْ داخلَ
 * `TeacherSignupWizard` وحدَه، فشاشةُ تحريرِ الملفِّ كانتْ ستنسخُها ثالثةً — ثمّ
 * تفترقُ الاثنتانِ عندَ إضافةِ لغةٍ، فيرى المتقدّمُ خياراً لا يراه المحرِّرُ.
 */
export const TEACHING_LANGUAGES = [
  { value: "ar", label: "العربية" },
  { value: "en", label: "الإنجليزية" },
  { value: "fr", label: "الفرنسية" },
] as const;
