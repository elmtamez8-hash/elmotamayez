node_modules/
.next/
out/
build/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Playwright storageState — holds a real auth token
/e2e/.auth/

# `next dev --experimental-https` writes a self-signed cert AND ITS PRIVATE KEY
# here. Added by Next itself the first time it runs; kept, because the rule
# against a secret in the repository does not care that this one is only ever
# trusted by one laptop.
/certificates/
