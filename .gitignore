# Per-developer Claude Code permissions — the shared skills under .claude/skills
# are committed, this file is not.
.claude/settings.local.json

# OS noise
.DS_Store
Thumbs.db

# Impeccable design-skill working state (question pages, hook cache).
.impeccable/

# Playwright MCP scratch output — snapshots, console logs and ad-hoc screenshots.
# Written into the repo root by the browser tooling; none of it is a deliverable.
.playwright-mcp/

# سكربتُ تهيئةِ حصةٍ مؤقّتٌ للتجربة اليدوية — لا يُلتزم به.
backend/make_session_tmp.php

# ⚠️ إعداداتُ النشرةِ الإنتاجيّة — أسرارٌ تعيشُ على الخادمِ وحدَه. المثالُ
# `docker/.env.prod.example` هو ما يُتابَع؛ مفتاحٌ يدخلُ التاريخَ لا يخرجُ منه
# بحذفِه، وتدويرُه يعني نشرةً جديدةً لا commit.
docker/.env
