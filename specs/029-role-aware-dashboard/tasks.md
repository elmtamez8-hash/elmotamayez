---

description: "Task list — ٠٢٩ · لوحةٌ تعرفُ من يفتحُها"
---

# Tasks: لوحةٌ تعرفُ من يفتحُها

**Input**: مستنداتُ التصميمِ في `/specs/029-role-aware-dashboard/`

**Prerequisites**: [plan.md](./plan.md) · [spec.md](./spec.md) · [research.md](./research.md) · [data-model.md](./data-model.md) · [contracts/api.md](./contracts/api.md)

**Tests**: **إلزاميّة، لا اختياريّة.** المبدأُ الأوّلُ يوجبُ اختبارَ الاتّجاهَينِ في نفسِ التغييرِ
لكلِّ قراءةٍ عن كيانٍ مملوكٍ للمنصّة، والمبدأُ الرابعُ يجعلُ اختباراتِ Feature شبكةَ الأمانِ
الأساسيّة. ومنطقُ المكوّناتِ يُقاسُ بـ`npm test` — هو السطحُ الوحيدُ الذي يرى أعطالَ هذه الشاشة.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: يمكنُ تنفيذُه بالتوازي (ملفٌّ مختلفٌ، بلا اعتمادٍ على مهمّةٍ غيرِ مكتملة)
- **[Story]**: `US1` طالب · `US2` مدرّس · `US3` وليُّ أمر · `US4` رسوم

## ⛔ ثلاثُ قواعدَ تسري على كلِّ مهمّةٍ في هذا الملفّ

1. **كلُّ قراءةٍ يملكُها كلُّ من يصلُ إلى الشاشة.** لا استدعاءَ محروسٍ بصلاحيّةٍ لا يحملُها جمهورُ
   التخطيط. هذا هو العطلُ المسجَّلُ في صدرِ الصفحةِ القائمة.
2. **كلُّ بطاقةٍ تفشلُ وحدَها.** **يُمنَعُ** `Promise.all` يجمعُ قراءتَينِ لبطاقتَين.
3. **كلُّ عدٍّ من `meta.total`.** لا `array.length` فوقَ قائمةٍ مقسَّمة.

---

## Phase 1: Setup (البنيةُ المشتركة)

**Purpose**: ثلاثُ لبناتٍ تقرؤُها التخطيطاتُ الثلاثةُ كلُّها

- [X] T001 [P] أنشئْ `dashboardAudience(user)` في `frontend/src/lib/dashboard-audience.ts` — يُرجِعُ `"student" | "teacher" | "guardian"` من `platform_role`، وللدورِ الفارغِ يُرجِعُ `"teacher"` إن حملَ الحسابُ أيَّ صلاحيّةِ مساحةِ عملٍ و`"student"` إن لم يحمل. **لا تُعدِّلْ `isLearner()`** — سبعةَ عشرَ عنصرَ قائمةٍ جانبيّةٍ تعتمدُ عليها
- [X] T002 [P] اكتبْ `frontend/src/lib/dashboard-audience.test.ts` — الأدوارُ الثلاثةُ، والدورُ الفارغُ بصلاحيّةٍ وبلا صلاحيّة، ومديرُ المنصّة
- [X] T003 [P] أنشئْ `frontend/src/app/(app)/(shell)/dashboard/cards/DashboardCard.tsx` — غلافٌ يحملُ العنوانَ ورابطَ «عرضُ الكلّ» **الإلزاميَّ** والحالاتِ الأربعَ (تحميل · جاهز · فارغ · فشلٌ بإعادةِ محاولة). يستعملُ `RowsSkeleton` و`ErrorState` القائمَينِ، **ولا يُضافُ إلى `components/ui/`** — ⚠️ **و`EmptyState` مقصودٌ تركُه**: هو `py-16` بأيقونةٍ مبنيٌّ لفراغِ صفحةٍ كاملة، وداخلَ بطاقةٍ يصيرُ أكبرَ من البطاقة. فحالةُ الفراغِ جملةٌ تُمرَّرُ في `empty`. وبطاقاتُ الابنِ الثلاثُ بلا «خطوةٍ تالية» عمداً (`FR-014`): الابنُ هو من يحجزُ ويشتري ويجلسُ للاختبار، ووليُّ الأمرِ يتابع
- [X] T004 حوِّلْ `frontend/src/app/(app)/(shell)/dashboard/page.tsx` إلى **مُوجِّه**: يقرأُ `dashboardAudience()` ويُصيِّرُ أحدَ ثلاثةِ مكوّناتٍ بديلةً مؤقّتاً. **لا قراءةَ بيانٍ واحدةٍ تبقى في هذا الملفّ** — ⚠️ **ولا بديلَ مؤقّتٌ للطالبِ والمدرّس**: جسدُ الصفحةِ القائمُ انتقلَ كما هو إلى `LegacyDashboard.tsx` ويقرؤُه الاثنانِ معاً، لأنّ بديلاً فارغاً كان سيحذفُ لوحةً تعملُ من أجلِ ترتيبِ الملفّات. `T020` يستبدلُه بـ`StudentDashboard` و`T027` بـ`TeacherDashboard`، **ويُحذَفُ الملفُّ عندَ الثاني منهما**

---

## Phase 2: Foundational (شرطٌ حاجبٌ لكلِّ الشرائح)

**Purpose**: عقدانِ في الخلفيّةِ تعتمدُ عليهما شريحتانِ من الثلاث

**⚠️ CRITICAL**: `US1` محجوبةٌ على T005–T009، و`US2` محجوبةٌ على T010–T012

### إصلاحُ عطلٍ مشحون — قراءةُ التسجيلات

> ⚠️ **ليس تحسينَ عقدٍ من أجلِ العدّ.** مقيسٌ بطلبٍ حقيقيّ: الردُّ **مصفوفةٌ عاريةٌ** (`[0]`) لا
> `{data}`، والقرّاءُ الثلاثةُ يقرؤونَ `res.data ?? []` ⇒ **صفرُ صفوفٍ لكلِّ طالبٍ على المنصّة**.
> شاشةُ «تعلّمي» خاليةٌ ومنتقي الكورساتِ في شراءِ الرصيدِ خالٍ، لمن اشترى ودفع.

- [X] T005 اكتبِ الاختبارَ **أوّلاً**: `backend/tests/Feature/Learning/EnrollmentIndexShapeTest.php` — يؤكِّدُ أنّ `GET /api/v1/enrollments` يحملُ `data` و`meta.total`، وأنّ `meta.total` يساوي العددَ الحقيقيَّ لطالبٍ عندَه أكثرُ من صفحةٍ واحدة. **يجبُ أن يفشلَ الآن** — لم يكن لهذا الفهرسِ اختبارٌ قطّ، وهو سببُ بقاءِ العطل
- [X] T006 عدِّلْ `backend/app/Modules/Learning/Http/Controllers/EnrollmentController.php:index` إلى `->response()->getData(true)` — صيغةُ `CertificateController` نفسُها، مع تعليقٍ يشرحُ لماذا لا يُستدعى `response()->json(Resource::collection(…))`
- [X] T007 [P] عدِّلْ `frontend/src/app/(app)/(shell)/enrollments/page.tsx` ليقرأَ الشكلَ الجديد — ⚠️ **بلا تعديلٍ واحد، وهذا هو الاكتشاف**: القارئُ يقرأُ `res.data ?? []` سلفاً، وهو الشكلُ الجديدُ بعينِه. الخطأُ كان في الخادمِ وحدَه، والقارئُ كان صحيحاً ويعرضُ صفراً
- [X] T008 [P] عدِّلْ `frontend/src/app/(app)/(shell)/billing/purchase/page.tsx` ليقرأَ الشكلَ الجديد — كما فوق، بلا تعديل. ⚠️ **والقرّاءُ أربعةٌ لا ثلاثة**: يُضافُ إليهم `store/page.tsx` (يبني قائمةَ مدرّسي الطالبِ من تسجيلاتِه) و`dashboard/LegacyDashboard.tsx`. الأربعةُ صحيحون وأُصلِحوا جميعاً بتغييرِ الخادم
- [X] T009 شغّلْ `php vendor/bin/pest tests/Feature/Learning` وتحقَّقْ من خضرةِ T005، ثمَّ افتحْ `/enrollments` بحسابِ طالبٍ له تسجيلٌ نَشِطٌ وتأكَّدْ أنّ الصفوفَ ظهرَت. ⚠️ إن لم تظهرْ فاضغطْ `Ctrl+Shift+R` قبلَ أن تشكَّ في الكود — **المقيس**: `pest` أخضرُ (٣٩٨/٣٩٨ في المجلَّدَين)، وطلبٌ حقيقيٌّ على الخادمِ العاملِ بحسابِ `student@example.com` أعادَ `{data, links, meta}` بصفَّينِ و`meta.total = 2`، و`?status=graduated` أعادَ `422`. ⚠️ **ولم يُمشَ الطريقُ في المتصفّح** — القياسُ على الواجهةِ البرمجيّةِ لا على الشاشة

### حقلُ «هل هذا مضيف؟»

- [X] T010 أضِفْ `teacher_profile_uuid` إلى `backend/app/Modules/Identity/Http/Resources/UserResource.php` — `$this->teacherProfile?->uuid`، مع تحميلٍ مسبقٍ حيثُ يُبنى المَورِد — ⚠️ **ولا تحميلَ مسبقٌ أُضيف، وهذا انحرافٌ مقصود**: `accountPhotoUrl()` في المَورِدِ نفسِه يقرأُ `teacherProfile` على كلِّ نداءٍ سلفاً، فالعلاقةُ محمَّلةٌ قبلَ أن يصلَ السطرُ الجديدُ وكلفتُه **صفرُ استعلامات** — و`Identity/QueryBudgetTest` على `‎/auth/me` أخضرُ يُثبِتُها. و`whenLoaded` مرفوضةٌ للسببِ المكتوبِ فوقَ `student_profile`: تُسقِطُ المفتاحَ عندَ كلِّ مُنادٍ لم يُحمِّل، والمفتاحُ الغائبُ جوابٌ آخرُ غيرُ `null`
- [X] T011 [P] أضِفِ الحقلَ إلى `User` في `frontend/src/lib/types.ts` (`teacher_profile_uuid: string | null`)
- [X] T012 اكتبْ حالةً في اختبارِ حمولةِ المستخدم: مدرّسٌ ⇒ uuid؛ طالبٌ ووليُّ أمرٍ ⇒ `null`. ⚠️ **وحالةٌ لعضوٍ في أكثرَ من مساحةِ عمل**: `teacher_profiles` يحملُ `BelongsToWorkspace` فيُحَلُّ تحتَ سياقِ القارئ — تأكَّدْ أنّه لا يُرجِعُ `null` خاطئاً. ⚠️ **الفخُّ في الحزامِ لا في المنتَج، وهذا هو المقيس**: `tests/Feature/Identity/HostFieldPayloadTest.php` بثلاثِ حالات. Eloquent يُخزِّنُ العلاقةَ المحمَّلةَ **بما فيها `null`**، و`Sanctum::actingAs()` يُثبِّتُ كائنَ مستخدِمٍ **واحداً** على طلباتِ الحالةِ كلِّها — فقراءةٌ في مساحةِ التعلُّمِ قبلَ قراءةٍ في مساحةِ التدريسِ تُبقي الجوابَ `null` في مساحتِه هو. الطلبُ الحقيقيُّ يبني النموذجَ من قاعدةِ البيانات، فالحالةُ تُجدِّدُ الحسابَ بينَ القراءتَينِ بدلَ إضافةِ `unsetRelation` إلى المَورِد: حراسةٌ ضدَّ حالةٍ لا تقعُ في طلبٍ حقيقيٍّ كانت ستكلّفُ استعلاماً لكلِّ مُنادٍ حمَّلَ العلاقةَ عن قصد. ⚠️ وحالةٌ ثالثةٌ تحرسُ **التركيبةَ نفسَها**: `BelongsToWorkspace` يملأُ `workspace_id` من السياقِ لحظةَ الإنشاء، فملفٌّ يُنشَأُ تحتَ سياقٍ خاطئٍ يُفشِلُ الحالةَ لسببٍ لا علاقةَ له بالحقلِ ويُقرَأُ على أنّه عطبٌ فيه. **والمقيسُ حيّاً**: `teacher@example.com` ⇒ uuid، و`student@example.com` ⇒ `null` **بمفتاحٍ موجود**

**Checkpoint**: العقدانِ جاهزان — الشرائحُ الثلاثُ تبدأُ بالتوازي

---

## Phase 3: User Story 1 — الطالبُ يرى يومَه (Priority: P1) 🎯 MVP

**Goal**: طالبٌ يفتحُ اللوحةَ فيعرفُ موعدَ حصّتِه ورصيدَه وتقدُّمَه بلا فتحِ صفحةٍ أخرى

**Independent Test**: حسابُ طالبٍ عندَه حصّةٌ محجوزةٌ غداً وكورسانِ جاريان — الموعدُ والتقدّمُ
والرصيدُ ظاهرةٌ كلُّها على الشاشةِ الأولى

### Implementation

- [X] T013 [P] [US1] `cards/StatCountsCard.tsx` — كورساتٌ جارية · مكتملة · شهادات. **من `meta.total`** بطلبَينِ بـ`per_page=1`، لا من طولِ مصفوفة — ⚠️ **تصحيحانِ لحسابِ المهمّة، كلاهما مقيس**: (١) **ثلاثةُ طلباتٍ لا اثنان** — «جارية» و«مكتملة» مجموعانِ لا يُشتقُّ أحدُهما من الآخرِ ولا من مجموعٍ واحد، ولذلك كسبَ فهرسُ التسجيلاتِ مُرشِّحَ `?status=` مُصادَقاً عليه بـ`Rule::enum` في هذه المرحلةِ نفسِها (مُرشِّحٌ يسقطُ بصمتٍ يعرضُ **كلَّ** التسجيلاتِ تحتَ «جارية»). (٢) **بلا `per_page`** — `paginate(15)` لا تقرأُ ذلك المعامِلَ أصلاً، ومعامِلٌ في العنوانِ لا يفعلُ شيئاً ادّعاءٌ يقرؤُه التالي على أنّه عقد. و`allSettled`: رفضُ عدٍّ واحدٍ يترُكُ الاثنَينِ الآخرَين
- [X] T014 [P] [US1] `cards/CourseBalancesCard.tsx` — سطرٌ **لكلِّ كورسٍ** من قراءةِ الأرصدة، المحجوبُ (`is_withheld`) أوّلاً بعلامةٍ وسببٍ. ⚠️ **يُمنَعُ أيُّ مجموع**: `+10` رياضيات و`−6` فيزياء ليست `+4`، والجمعُ يُظهِرُ سَعَةً لطالبٍ محجوبٍ في الكورسِ الذي يريدُ حجزَه
- [X] T015 [P] [US1] `cards/UpcomingSessionsCard.tsx` — خمسةُ صفوفٍ من قراءةِ جدولِ الطالب، بالأقربِ أوّلاً، لكلِّ صفٍّ الموعدُ والكورسُ والمدرّسُ والحالة — ⚠️ **الكورسُ والمدرّسُ لم يكونا في الحمولةِ أصلاً**: `GetStudentSchedule` كان يُحمِّلُ `recordingLesson` وحدَه، فجدولُ الطالبِ (وهذه الشاشةُ و`/schedule` معاً) بلا مادّةٍ ولا مدرّس. حُمِّلا في **الإجراءِ** بتجاوزِ النطاقِ في كلِّ مستوًى — القائمةُ تعبُرُ مساحاتِ العملِ بالتصميم — و`teacher_name` أُضيفَ إلى `ClassSessionResource` بـ`whenLoaded` وحدَها، فمُنادٍ لم يُحمِّلْ لا يدفعُ استعلامَينِ لكلِّ صفّ. وتحميلُ وليِّ الأمرِ المكرَّرُ في `ScheduleController::children` حُذِفَ: تهجئةٌ واحدةٌ لتحميلٍ واحد
- [X] T016 [US1] أضِفْ إلى `UpcomingSessionsCard` **المشتقّاتِ الزمنيّة**: عدٌّ تنازليٌّ، وانتقالٌ إلى «جارية»، وظهورُ دعوةِ الدخولِ عندَ نافذتِها — بمؤقّتٍ محلّيٍّ واحدٍ يُعيدُ الحسابَ من الطوابعِ الواصلة. **بلا أيِّ طلبٍ إضافيّ** (`FR-011أ`). و`room_closed` يتقدّمُ على الحالةِ في الشارةِ وفي الزرّ — ⚠️ **ولزِمَ حقلانِ جديدانِ من الخادمِ لا اشتقاقٌ في المتصفّح**: `seconds_until_join_open` و`seconds_until_start` على `ClassSessionResource`. اشتقاقُ البابِ من `starts_at` ناقصَ ثابتٍ يمنعُه تعليقُ ذلك المَورِدِ نفسِه بنصِّه — النافذةُ صفٌّ في `platform_settings` يضبطُه المشغّل، وساعةُ الجهازِ قد تكونُ خطأً بساعة. والحسابُ انتقلَ إلى `ClassSession::secondsUntilJoinOpen()` وحُذِفَ نظيرُه الخاصُّ من `ScheduleController`: نسختانِ تفترقانِ يومَ يُحرِّكُ المشغّلُ النافذة. حارسُه `tests/Feature/LiveSessions/JoinCountdownTest.php`
- [X] T017 [P] [US1] `cards/LatestNotificationsCard.tsx` — خمسةُ صفوف، غيرُ المقروءِ أوّلاً. **يستمعُ إلى `NOTIFICATIONS_CHANGED`** ولا يُنشئُ نبضاً دوريّاً (`FR-010`)
- [X] T018 [P] [US1] `cards/LatestOrdersCard.tsx` — ثلاثةُ صفوفٍ من قراءةِ الطلبات، برابطٍ إلى `/orders`
- [X] T019 [P] [US1] `cards/QuickLinksCard.tsx` — روابطُ الطالبِ اليوميّة، مشتقّةٌ من عناصرِ القائمةِ الجانبيّةِ ذاتِ الجمهورِ «متعلّم» ولا تُكتَبُ قائمةً ثانية
- [X] T020 [US1] `StudentDashboard.tsx` — يرتّبُ البطاقات، وعنوانُه يخاطبُ طالباً. **كلُّ بطاقةٍ تجلبُ بنفسِها**؛ لا وعدَ مجموعاً في هذا الملفّ — و`LegacyDashboard.tsx` صارَ للمدرّسِ وحدَه، يحذفُه `T027` معه
- [X] T021 [US1] وسّعْ `dashboard/page.test.tsx`: حالةُ الطالبِ تؤكِّدُ على **قائمةِ النداءاتِ** وترفضُ كلَّ ما عداها كما يفعلُ الخادم؛ وحالةٌ ترفضُ **مصدراً واحداً** وتؤكِّدُ أنّ بقيّةَ البطاقاتِ معروضة؛ وحالةٌ تؤكِّدُ أنّ العددَ يأتي من `meta.total` لا من طولِ المصفوفة؛ وحالةٌ تؤكِّدُ أنّ **لا رقمَ واحداً يجمعُ رصيدَ كورسَين** — ⚠️ **والحالاتُ الثلاثُ القديمةُ نُقِلَت إلى حسابِ مدرّسٍ** لا حُذِفَت: هي تقيسُ `LegacyDashboard`، وهو ما زالَ معروضاً للمدرّس. وضمانةُ «لا نداءَ مرفوض» في حالةِ الطالبِ الجديدةِ أوسعُ منها: ترفضُ `/courses` **و**`/marketplace/courses` معاً، إذ عدُّ كتالوجِ المنصّةِ لا يقولُ شيئاً عن دراسةِ هذا الطالب

**Checkpoint**: لوحةُ الطالبِ كاملةٌ وقابلةٌ للشحنِ وحدَها

---

## Phase 4: User Story 2 — المدرّسُ يرى صفَّه (Priority: P1)

**Goal**: مدرّسٌ يفتحُ اللوحةَ فيرى حصصَه وأعمالَه المنتظِرة، لا أربعةَ أصفارٍ عن حسابِه كطالب

**Independent Test**: حسابُ مدرّسٍ عندَه حصّتانِ اليومَ وأوراقٌ للتصحيح — لا رقمَ صفريٌّ كاذبٌ ولا
جملةٌ تخاطبُه كطالب

### Implementation

- [X] T022 [P] [US2] `cards/TeacherSessionsCard.tsx` — إن كان `teacher_profile_uuid` غيرَ فارغٍ فقراءةُ الحصصِ بـ`?teacher={uuid}&from=&order=asc` تحتَ عنوانِ **«حصصي»**؛ وإن كان فارغاً مع صلاحيّةِ إدارةِ الحصصِ فبلا `teacher` تحتَ عنوانٍ يقولُ **«حصصُ المساحة»**. ⚠️ لا جدولَ فارغاً تحتَ «حصصي»، ولا حصّةَ زميلٍ تحتَها. ⚠️ **والعنوانُ «حصص مكان العمل» لا «حصص المساحة»**: `SC-002` في مواصفةِ ٠٢٥ يمنعُ جذرَ «مساح» في أيِّ نصٍّ يقرؤُه مستخدِم، و`src/lib/workspace-vocabulary.test.ts` أسقطَ البناءَ على الصياغةِ الأولى — الحارسُ يجرِّدُ التعليقاتِ فالشرحُ يبقى. ⚠️ **ومن لا يستضيفُ ولا يُديرُ لا يرى بطاقةً أصلاً**: عنوانٌ ثالثٌ مخترَعٌ هو الكذبةُ نفسُها بصياغةٍ أخرى، و`FR-015` يمنعُ بطاقةً بلا شاشة. و`from` تاريخٌ لا لحظة — لحظةٌ كاملةٌ تُسقِطُ حصّةً بدأت قبلَ عشرِ دقائقَ وما زالت جارية — بتهجئةِ `manage/sessions` نفسِها. **وانحرافٌ**: `SessionRow` و`useSessionTick` صارا مُصدَّرَينِ من `UpcomingSessionsCard` بدلَ نسخِ الصفِّ والعدِّ التنازليّ؛ والاستخراجُ كشفَ أنّ إعادةَ الجلبِ كانت تُصفِّرُ العدَّ في المُنادي، فصارَ التصفيرُ داخلَ الخُطّافِ على بصمةِ الأرقام
- [X] T023 [P] [US2] `cards/PendingGradingCard.tsx` — `meta.total` من طابورِ التصحيحِ بصفٍّ واحد، محروسةٌ بصلاحيّةِ التصحيح (`grading.perform`، وهي الصلاحيّةُ التي تحرسُ الشاشةَ في `panel-nav` نفسِها)، برابطٍ إلى `/manage/grading`
- [X] T024 [P] [US2] `cards/PrivateRequestsCard.tsx` — `meta.total` من طابورِ الحصصِ الخاصّةِ بحالةِ الانتظار، محروسةٌ بصلاحيّةِ إدارةِ الحصص (`sessions.manage`)، برابطٍ إلى `/manage/private-sessions`. ⚠️ **بلا `per_page=1` وبلا `status=pending`**: الطابورُ `paginate(20)` بعددٍ ثابتٍ لا يقرأُ الأوّلَ أصلاً — ومعامِلٌ لا يفعلُ شيئاً ادّعاءُ عقدٍ يُصدِّقُه القارئُ التالي — والمنتظِرُ هو **افتراضُ** الخادمِ في الفرعِ الآخرِ من `when`، فإرسالُه تهجئةٌ ثانيةٌ لشرطٍ يملكُه. و`meta` أُضيفَ إلى نوعِ `privateSessions.queue` — كانَ يُصرِّحُ بـ`data` وحدَها
- [X] T025 [P] [US2] `cards/WithheldStudentsCard.tsx` — عددُ الطلّابِ المحجوبينَ من قراءةِ أرصدةِ طلّابِ المساحة، محروسةٌ بإذنِ قراءةِ الرصيد، برابطٍ إلى `/manage/billing/students`. **عددُ أشخاصٍ لا مبلغ** — ⚠️ **ولا عددُ صفوفٍ كذلك**: الحجبُ لكلِّ كورسٍ وحدَه فالصفُّ (طالب × كورس)، وطالبٌ محجوبٌ في مادّتَينِ صفّانِ وشخصٌ واحد. `new Set(...student_uuid).size`، والقراءةُ **غيرُ مقسَّمةٍ** فالعددُ صادقٌ على كلِّ الصفوف
- [X] T026 [P] [US2] وسّعْ `QuickLinksCard` بروابطِ إدارةِ المدرّسِ — كلُّ رابطٍ محروسٌ بالصلاحيّةِ التي تحرسُ شاشتَه. ⚠️ **بلا سطرٍ واحد، وهذا هو الاكتشاف**: البطاقةُ مشتقّةٌ من `quickAccessFor()` منذُ `T019`، وتلك تُرتِّبُ بـ`QUICK_ACCESS.staff` — وهي بالضبطِ روابطُ إدارةِ المدرّسِ — ثمّ تُرشِّحُ بـ`allowedNav` أي `can(user, item.permission)`. فالحراسةُ المطلوبةُ قائمةٌ بالبناء، وقائمةٌ ثانيةٌ هنا كانت ستكونُ الجوابَ الثاني الذي يشيخُ عندَ أوّلِ شاشةٍ تُضاف
- [X] T027 [US2] `TeacherDashboard.tsx` — يرتّبُ البطاقات، وعنوانُه يخاطبُ مدرّساً. ⚠️ **كلُّ حراسةٍ بثابتِ `Permissions` عبرَ `can()`**، لا بالدورِ ولا بنصٍّ حرفيّ: مساعدُ المدرّسِ ممنوعٌ من الصلاحيّاتِ الماليّةِ عندَ الفحصِ لا عندَ اسمِ الدور — **والحراسةُ داخلَ كلِّ بطاقةٍ لا في هذا الملفّ**: القاعدةُ تُقرَأُ بجوارِ القراءةِ التي تحرسُها، وقائمةُ شروطٍ هنا جوابٌ ثانٍ يفترقُ عن الأوّل. ⚠️ **و`LegacyDashboard.tsx` حُذِفَ معها** كما نصَّ `T004`. ⚠️ **ولا بطاقةَ طلباتٍ للمدرّس** وإن ذكرَها سيناريو ٤: `FR-007` لا يُدرِجُها في تخطيطِه، وعقدُ المرحلةِ (§ج) يضعُ قراءةَ الطلباتِ على الطالبِ ووليِّ الأمرِ وحدَهما، وبطاقةٌ هنا كانت سترتبطُ بشاشةِ مشترٍ. **وانحرافٌ**: البطاقاتُ الثلاثُ ذاتُ الرقمِ الواحدِ تشتركُ في `cards/CountCard.tsx` — ثلاثُ نسخٍ من حالةٍ واحدةٍ تعني إصلاحاً يُطبَّقُ مرّتَينِ ويُنسى الثالثة
- [X] T028 [US2] وسّعْ `dashboard/page.test.tsx`: حالةُ المدرّسِ (قائمةُ النداءات)؛ وحالةُ **مساعدٍ** تؤكِّدُ اختفاءَ البطاقاتِ الماليّةِ وبقاءَ ما يملكُه؛ وحالةٌ تؤكِّدُ أنّ الجدولَ نُودِيَ بـ`?teacher=` حينَ يكونُ المضيفُ معروفاً وبدونِه حينَ لا يكون؛ وحالةٌ تؤكِّدُ **تبدُّلَ العنوانِ** بينَ الحالتَين. ⚠️ **والحالاتُ الثلاثُ القديمةُ سقطَت ولم تُنقَل**: كانت تقيسُ `LegacyDashboard` نفسَه — عدَّ الكتالوجِ من `meta.total` وسقوطَه على رفضٍ واحد — ولم يبقَ منه شيءٌ يُقاس، وما كانت تحرسُه انتقلَ إلى حالاتِ الطالبِ وهي أوسعُ. وأُضيفَت حالةٌ خامسةٌ لمن **لا يستضيفُ ولا يُديرُ**: لا بطاقةَ ولا نداء. والتوكيدةُ `includes("teacher=t-1")` لا مساواةً حرفيّةً على العنوان: ترتيبُ `URLSearchParams` يتبعُ ترتيبَ مفاتيحِ الكائن. **المقيس**: ١٨ حالةً في الملفّ، و**٥٧٤/٥٧٤** في `npm test`

**Checkpoint**: لوحةُ المدرّسِ كاملةٌ وقابلةٌ للشحنِ وحدَها

---

## Phase 5: User Story 3 — وليُّ الأمرِ يرى ابنَه (Priority: P2)

**Goal**: وليُّ أمرٍ يفتحُ اللوحةَ فيرى جدولَ ابنِه وحضورَه ورصيدَه — كلٌّ بحسبِ الإذنِ الممنوح

**Independent Test**: حسابُ وليِّ أمرٍ مرتبطٍ بابنٍ **له حساب** وإذنُ «المواعيدُ والحصص» ممنوح —
حصّةُ الابنِ ظاهرةٌ باسمِه

### الخلفيّة — المسارانِ الجديدان

> ⚠️ **الاختبارُ أوّلاً في هذه الشريحةِ تحديداً.** وليُّ الأمرِ الخالصُ سياقُه `null` والنطاقُ
> لا يضيفُ شرطاً — فاختبارٌ لا يحملُ **وليّاً بسياقٍ محلولٍ** يمرُّ على بناءٍ بلا تجاوزِ نطاقٍ فيه
> إطلاقاً، ويُثبِتُ العكسَ ممّا يدّعيه.

- [X] T029 [US3] اكتبْ `backend/tests/Feature/LiveSessions/ChildScheduleGuardTest.php` بالحالاتِ الأربعِ في [contracts/api.md](./contracts/api.md) · أ: وليٌّ مأذونٌ **بسياقٍ محلولٍ يشيرُ إلى مساحةٍ غيرِ مساحةِ حصّةِ الابن** ⇒ `200` والصفوفُ كاملة · وليٌّ بإذنٍ آخرَ ⇒ `403` · وليٌّ يسمّي حساباً ليس ابنَه ⇒ `403` بلا اسمٍ · **مدرّسُ الابنِ نفسُه** ⇒ `403`
- [X] T030 [US3] اكتبْ `backend/tests/Feature/LiveSessions/ChildAttendanceSummaryTest.php`: الفئاتُ الأربعُ بأعدادِها والنسبةُ المشتقّةُ منها · الحالاتُ الأربعُ نفسُها · **حدُّ النافذةِ**: حصّةٌ في آخرِ يومٍ من المدى تُحتسَبُ (`< التاريخ + يوم` لا `<= التاريخ`) · تجاوزُ النطاقِ يُقاسُ بابنٍ له حضورٌ في **مساحتَي عملٍ** ⇒ الصفّانِ معاً
- [X] T031 [US3] أضِفْ `ChildSessionResource` في `backend/app/Modules/LiveSessions/Http/Resources/` — **سبعةُ حقولٍ فقط** (uuid الحجز · uuid الحصّة · العنوان · البداية · النهاية · الحالة · `room_closed` · الكورس · اسمُ المدرّس). ⚠️ **لا `join_open` ولا `my_booking` ولا `unlock_*` ولا `seats` ولا `recording`**: كلُّها محسوبةٌ عن القارئ، فتقولُ لوليِّ الأمرِ «لا حجز» عن حصّةٍ ابنُه محجوزٌ فيها وتدعوه إلى غرفةٍ لا يدخلُها
- [X] T032 [US3] أضِفْ `ScheduleController::children` — يحلُّ الابنَ بـ`GuardianDirectory::childrenOf($guardian, Schedule)`، `422` بلا `student`، `403` إن لم يكن ابنَه، ثمَّ **يُنادي `GetStudentSchedule` القائمَ كما هو**. لا Action ثانياً
- [X] T033 [US3] أضِفْ سطراً إلى صدرِ `GetStudentSchedule`: وليُّ الأمرِ **نائبُ الابنِ لا مدرّسُه**، فالوعدُ «لا يبلغُه مدرّسٌ أبداً» يبقى صحيحاً
- [X] T034 [US3] أنشئْ `SummariseChildAttendance` في `backend/app/Modules/LiveSessions/Actions/` — تجميعٌ واحدٌ `GROUP BY status` داخلَ النافذة، **مع `withoutWorkspaceScope()` وتعليقٍ يشرحُ لماذا** (`attendances` مملوكٌ لمساحةِ العمل، ولابنٍ عندَ ثلاثةِ مدرّسينَ سجلُّ حضورٍ واحد)
- [X] T035 [P] [US3] أنشئْ `ChildAttendanceSummaryResource` — الفئاتُ الأربعُ والمجموعُ والنسبةُ ونافذةُ الأيّام
- [X] T036 [US3] أضِفْ `AttendanceController::childSummary` بالحراسةِ نفسِها بإذنِ «الحضورُ والغياب»، و`days` اختياريّةٌ بمدًى مقيَّد
- [X] T037 [US3] سجّلِ المسارَينِ في `backend/app/Modules/LiveSessions/routes/api.php` داخلَ مجموعةِ `auth:sanctum`، بتعليقٍ يقولُ لماذا مسارانِ منفصلانِ لا `?student=` على مسارِ الطالب
- [X] T038 [US3] أضِفْ ميزانيّةَ استعلاماتٍ للمسارَين في `QueryBudgetTest`: الكلفةُ ثابتةٌ بينَ حصّةٍ وعشرين. ⚠️ **حَمِّ الطلبَ حتّى يستقرَّ** قبلَ القياس؛ وحينَ يهتزُّ السقفُ بواحدٍ فالمطلوبُ إيجادُ الاستعلامِ لا رفعُ الرقم
- [X] T039 [US3] حدِّثْ `docs/README.md` بالمسارَينِ والأذونِ الحارسةِ لهما — التوثيقُ يتحرّكُ مع الكود

### الواجهة

- [X] T040 [P] [US3] أضِفْ نداءَي المسارَينِ إلى `frontend/src/lib/class-sessions.ts` وأنواعَهما إلى `types.ts` — ⚠️ **النوعانِ استقرّا في `class-sessions.ts` نفسِه** بجوارِ `SessionBooking` و`AttendanceRow`، لا في `types.ts`: صدرُ ذلك الملفِّ يقولُ إنّ أنواعَه تُحاكي مَوارِدَ الحصصِ حقلاً بحقل، وقارئٌ يبحثُ عن شكلِ حجزِ الابنِ يفتحُه هو. و`ChildSessionBooking` نوعٌ **ثالثٌ** لا إعادةُ استعمالٍ لـ`SessionBooking`، وهو الحارسُ الوحيدُ في الواجهةِ على ضِيقِ المَورِد
- [X] T041 [P] [US3] `cards/ChildSwitcher.tsx` — من روابطِ الأسرة، **المرتبطونَ بحسابٍ فقط** (`student_uuid` موجود)؛ يظهرُ عندَ التعدُّدِ وحدَه ويبدأُ على الأوّلِ أبجديّاً؛ ولا يُحفَظُ الاختيارُ بينَ الزيارات
- [X] T042 [P] [US3] `cards/ChildScheduleCard.tsx` — جدولُ الابنِ القادم. **بلا دعوةِ دخول**
- [X] T043 [P] [US3] `cards/ChildAttendanceCard.tsx` — الأعدادُ الأربعةُ والنسبةُ نصّاً
- [X] T044 [P] [US3] `cards/ChildBalanceCard.tsx` — من قراءةِ رصيدِ الأبناءِ القائمة، سطرٌ لكلِّ كورسٍ **بلا مجموع**
- [X] T045 [P] [US3] `cards/ChildReportCardCard.tsx` — آخرُ كشفٍ منشورٍ لابنٍ مسمّى، برابطٍ إلى `/report-cards`
- [X] T046 [US3] `GuardianDashboard.tsx` — يرتّبُ البطاقات، وعنوانُه يخاطبُ وليَّ أمر. ⚠️ **البطاقةُ بلا إذنٍ لا تُعرَضُ فارغةً بل لا تُعرَضُ أصلاً**، ويظهرُ مكانَها سطرٌ يقولُ إنّ الإذنَ غيرُ ممنوحٍ ورابطٌ إلى `/family`. ورابطُ «عرضُ الكلّ» في بطاقاتِ الابنِ إلى `/family` (راجعْ [research.md](./research.md) · R5)
- [X] T047 [US3] حالةُ **وليِّ أمرٍ بلا ابنٍ مرتبط**: لا بطاقاتِ أبناءٍ فارغة؛ سطرٌ يشرحُ ورابطٌ إلى `/family` في خطوةٍ واحدة
- [X] T048 [US3] وسّعْ `dashboard/page.test.tsx`: حالةُ وليِّ الأمرِ (قائمةُ النداءات) · بطاقةٌ لإذنٍ غيرِ ممنوحٍ **غائبةٌ** لا فارغة · ابنٌ **بلا حساب** لا يظهرُ في المُبدِّل · تبديلُ الابنِ يُعيدُ جلبَ **كلِّ** بطاقاتِه · **لا دعوةَ دخولٍ** في جدولِ الابن

**Checkpoint**: لوحةُ وليِّ الأمرِ كاملة، والقراءتانِ الجديدتانِ محروستانِ ومقيستان

---

## Phase 6: User Story 4 — الأرقامُ تُرى (Priority: P3)

**Goal**: رسمٌ واحدٌ لكلِّ دور، مشتَقٌّ من البياناتِ المعروضةِ سلفاً بلا قراءةٍ إضافيّة

**Independent Test**: الرقمُ في الرسمِ يطابقُ الرقمَ في الجدولِ فوقَه، ويُقرَأُ نصّاً بقارئِ الشاشة

- [X] T049 [P] [US4] `cards/ProgressChartCard.tsx` — شريطٌ لكلِّ كورسٍ جارٍ بالأقلِّ إنجازاً أوّلاً، بـ`ProgressBar` القائمِ كما هو، **من صفوفِ التسجيلاتِ التي جلبَتها T013 نفسِها** لا بقراءةٍ ثانية — ⚠️ **والآليّةُ `cards/shared-read.ts`: «طلبٌ واحدٌ في الجوّ» لا ذاكرةُ تخزين.** المدخلُ يُحذَفُ لحظةَ استقرارِ الوعد، فالمكسبُ محصورٌ في المُنادينَ الذين طلبوا معاً — وهو ما يحدثُ حينَ تُصيَّرُ البطاقتانِ في تمريرةٍ واحدة. وذاكرةٌ تبقى بعدَ الاستقرارِ كانت ستُعيدُ جوابَ الدقيقةِ الماضيةِ لبطاقةٍ رُكِّبَت الآن، **وتُسرِّبُ حالةَ اختبارٍ إلى الذي يليه** حيثُ لا يراها `vi.clearAllMocks()`. السابقةُ `lib/echo.ts`. ⚠️ **وسقفُ الخمسةَ عشرَ صفّاً موثَّقٌ بجوارِ الترتيب**: الرقمُ فوقَ الرسمِ من `meta.total` فيبقى صادقاً، والأشرطةُ عيّنةٌ لا جرد. والترتيبُ يتعادلُ بـ`localeCompare(…, "ar")` لا بترتيبِ الخادمِ الذي لا يَعِدُ بشيءٍ عندَ تساوي النسبة
- [X] T050 [P] [US4] `cards/WeekSessionsChartCard.tsx` — سبعةُ أعمدةٍ بارتفاعٍ نسبيّ، مبنيّةٌ من صفوفِ T022 نفسِها. ⚠️ **سقفٌ يُوَثَّقُ ولا يُرفَع**: قراءةُ الحصصِ تُقسِّمُ بخمسينَ صفّاً بلا `per_page` — مكتوبٌ فوقَ `readTeacherSessions()`. ⚠️ **والأعمدةُ تُبنى بمنطقةِ الحصّةِ الزمنيّةِ لا بمنطقةِ الجهاز** (`sessionDayKey`): حصّةُ الواحدةِ صباحاً بالدوحةِ تقعُ في اليومِ السابقِ على حاسوبٍ في منطقةٍ أخرى فتُعَدُّ في العمودِ الخطأ — علّةُ `formatSessionTime` نفسُها مرفوعةً إلى «أيُّ عمود». و**المقامُ `Math.max(1, …)`**: أسبوعٌ خالٍ يجعلُ الارتفاعَ `NaN%`، وهو عمودٌ لا يُرسَمُ بصمت. **وشرطُ `teacherSessionsAudience()` مشتركٌ مع البطاقة** — تهجئتانِ لـ«من يرى جدولاً» جوابانِ يفترقانِ عندَ أوّلِ إصلاح
- [X] T051 [P] [US4] ⚠️ **الرموزُ الأربعةُ مثبَّتةٌ سلفاً وكلُّها معرَّفةٌ في `@theme`**: حاضر `secondary` · متأخّر `accent` · غائب `danger` · بعذر `ink-muted`. **لا `success` ولا `warning` ولا `error`** — لا وجودَ لأيٍّ منها، والصنفُ الذي يسمّيها لا يرسمُ شيئاً بصمت. `cards/AttendanceChartCard.tsx` — شريطٌ مكدَّسٌ من أربعِ فئاتٍ + مفتاحٌ يحملُ العددَ نصّاً، من أعدادِ T043 نفسِها. ⚠️ **الشريطُ كلُّه `aria-hidden` والمفتاحُ هو البيان**: عرضٌ نسبيٌّ لا يقولُ شيئاً لقارئِ الشاشة، ولونٌ وحدَه لا يقولُ شيئاً لمن لا يُفرِّقُ الأخضرَ من الأحمر. ⚠️ **والبطاقةُ ورسمُها في خانةٍ واحدةٍ تحتَ الإذنِ نفسِه** في `GuardianDashboard`: إذنٌ واحدٌ ⇒ سطرُ «غير ممنوح» **واحد** لا سطرانِ متطابقان، والاثنانِ يُصيَّرانِ في التمريرةِ نفسِها فيقتسمانِ الطلبَ الواحد
- [X] T052 [US4] ⚠️ **لا يُشطَبُ قبلَ T051**: شُغِّلَ الاختبارُ يومَ ٢٠٢٦-٠٩-٠٧ فمرَّ **على الفراغِ** — الأصنافُ الأربعةُ لم تُكتَبْ بعد، والحارسُ يمسحُ المصدرَ لا النيّة. الرموزُ المختارةُ مثبَّتةٌ في T051 أعلاه. ⚠️ **تحقَّقْ من كلِّ رمزِ لونٍ استعملَته الفئاتُ الأربعُ في `@theme` داخلَ `globals.css`**، وشغّلْ `npm test -- src/lib/theme-tokens`. **المقيس**: `secondary` · `accent` · `danger` · `ink-muted` أربعتُها معرَّفةٌ في `globals.css` (و`ink-muted` مرّتَينِ: فاتحٌ وداكن)، و`bg-primary` لأعمدةِ الأسبوعِ كذلك — والاختبارُ **٤/٤** فوقَ أصنافٍ مكتوبةٍ فعلاً هذه المرّة. صنفُ لونٍ يسمّي رمزاً غيرَ معرَّفٍ **لا يرسمُ شيئاً بصمت** — لا خطأَ ولا تحذيرَ ولا فرقَ في لقطة؛ شُحِنَ هذا العطلُ أربعَ مرّاتٍ في هذا المستودع
- [X] T053 [US4] اختبارُ مكوّنٍ لكلِّ رسم: كلُّ قيمةٍ مرسومةٍ متاحةٌ نصّاً، والرسمُ يقرأُ الأرقامَ نفسَها التي يقرؤُها الجدولُ فوقَه (`FR-018`) — ⚠️ **والمقياسُ عددُ النداءاتِ لا التساوي**: ما دامَ الاثنانِ يقتسمانِ ردّاً واحداً فالتساوي صحيحٌ **بالبناء** ويمرُّ فوقَ أيِّ تنفيذٍ كان. الحالاتُ الثلاثُ تؤكِّدُ أنّ كلَّ مسارٍ نُودِيَ **مرّةً واحدةً بالضبط**، وهي التي تسقطُ لو ركِّبَت بطاقةٌ بعدَ استقرارِ وعدِ الأخرى. **وكلُّ رقمٍ يُطلَبُ داخلَ قسمِ بطاقتِه بـ`within`**: رسمُ الأسبوعِ يكتبُ عدداً لكلِّ يومٍ فيتكرَّرُ «١» على الشاشة، وتوكيدةٌ عامّةٌ كانت ستُصيبُ عمودَ يومٍ وتمرُّ خضراءَ فوقَ بطاقةٍ تعدُّ خطأً
- [X] T054 [US4] حالةُ الفراغ: رسمُ المدرّسِ بأعمدةٍ صفريّةٍ **مع** جملةٍ تقولُ إنّ الجدولَ خالٍ ورابطٍ لإنشاءِ حصّة — لا رسمٌ فارغٌ صامت. ⚠️ **ولذلك لا تُستعمَلُ خاصّيّةُ `empty` في هذه البطاقة**: هي تحلُّ **محلَّ** المحتوى في `DashboardCard` فتحذفُ الأعمدةَ الصفريّةَ التي هي نصفُ الجواب — الجملةُ تُرسَمُ **تحتَ** الرسمِ لا مكانَه. والتركيبةُ تؤكِّدُ الثلاثةَ معاً: سبعةُ عناصرَ، سبعةُ أصفارٍ نصّاً، والجملةُ ورابطُها

**Checkpoint**: الأشكالُ الثلاثةُ تعملُ في الوضعَينِ الفاتحِ والداكن

---

## Phase 7: Polish & Cross-Cutting

- [X] T055 [P] راجعْ كلَّ ملفٍّ تحتَ `dashboard/cards/`: الخصائصُ المنطقيّةُ (`ms-*` · `start-*` · `text-start`) لا `ml-*`/`left-*`، والألوانُ من `@theme` وحدَها، والأرقامُ والتواريخُ عبرَ `numerals.ts`/`labels.ts`، والخطأُ عبرَ `userMessage()` — **لا خطأَ خامٍّ للمستخدمِ أبداً**. **المقيس** بمسحِ المجلَّدِ كلِّه: صفرُ `ml-*`/`mr-*`/`left-*`/`right-*`/`text-left`، وصفرُ `bg-gray|white|black|red|green|blue|slate` وصفرُ ألوانٍ سِتَّ عشْريّةٍ وصفرُ `text-white`، وصفرُ `err.message` أو `String(err)` — و**٣٤** موضعَ عبورٍ بـ`arabicNumber`/`formatDateTime`/`formatSession*`
- [X] T056 [P] تأكَّدْ أنّ **كلَّ** بطاقةٍ تحملُ رابطاً إلى شاشتِها الكاملة (`FR-015`)، وأنّ لا بطاقةَ تعتمدُ على قراءةٍ لا ينادِيها سطحٌ آخرُ في الواجهة. **المقيس**: كلُّ بطاقةٍ تحملُ `href` (النوعُ يفرضُه)، والوحيدُ بلا رابطٍ هو `ChildSwitcher` وهو أداةُ تحكُّمٍ لا بطاقة. وكلُّ قراءةٍ لها شاشتُها: التصحيحُ والحصصُ الخاصّةُ وأرصدةُ الطلّابِ والتقويمُ تُناديها شاشاتُها الكاملةُ سلفاً. ⚠️ **واستثناءٌ يُسجَّلُ لا يُخفى**: قراءاتُ الابنِ الثلاثُ (`schedule/children` · `attendance/children/summary` · `billing/children/balance`) لا يُناديها إلّا هذه اللوحةُ — أُنشِئَت لها في `T040`، وشاشتُها الكاملةُ `‎/family` حيثُ تُدارُ الأذون. و`?status=active` مُرشِّحٌ جديدٌ على فهرسٍ قائمٍ لا مسارٌ جديد
- [X] T057 المرورُ اليدويُّ بخمسةِ حساباتٍ (طالب · مدرّس · وليّ أمر · مساعد · بلا دورٍ مسجَّل) مع فتحِ لسانِ الشبكة: **صفرُ ردودِ «ممنوع»** (`SC-003`). **مقيسٌ بلسانِ الشبكةِ فعلاً على الخادمِ العامل، والخمسةُ كلُّهم**: `student@example.com` (طالب) · `teacher@example.com` (مدرّسٌ مضيف) · `lina@academy.test` (**مساعد**) · `owner@academy.test` (**بلا دَورٍ مسجَّلٍ**، يملكُ `sessions.manage` ولا يستضيف) · و`parent@example.com` (وليُّ أمرٍ أُنشِئَ للمرور؛ لا وليَّ أمرٍ في البذور).

  **النتيجة**: **صفرُ ٤٠٣** في الحسابات الخمسة. والمساعدُ لم يُنادِ التصحيحَ ولا الحصصَ الخاصّةَ ولا الأرصدةَ **إطلاقاً** — بطاقةٌ بلا صلاحيّةٍ لا تسألُ سؤالاً تعرفُ جوابَه. و«بلا دَور» نادى `‎/class-sessions?from=…&order=asc` **بلا `teacher=`** تحتَ عنوانِ «حصص مكان العمل القادمة»، ورسمَ سبعةَ أعمدةٍ صفريّةٍ مع الجملةِ والرابط — `T054` حيّاً.

  ⚠️ **والقراءةُ المشتركةُ ثبتَت على السلك**: تحتَ `StrictMode` تُركَّبُ كلُّ بطاقةٍ مرّتَين، فظهرَ كلُّ مسارٍ **مرّتَين** — إلّا `‎/enrollments?status=active` و`‎/class-sessions` و`‎/attendance/children/summary`، فكلٌّ منها **مرّةً واحدة**. هذا هو `sharedRead` يعملُ في متصفّحٍ حقيقيّ.

  ⚠️ **وعثرَ المرورُ على ٥٠٠ لم يرَه أيُّ اختبار**: `‎/manage/billing/students` سقطَ للمدرّسِ بـ`ErrorException: Attempt to read property "uuid" on null` من `ListStudentBalances` — صفُّ تسجيلٍ يتيمٌ يشيرُ إلى مستخدِمٍ محذوف. و`enrollments.student_user_id` **بلا مفتاحٍ أجنبيٍّ أصلاً**، فالصفُّ يبقى بعدَ ذهابِ صاحبِه، والانفجارُ ليس لصفِّه بل **للطلبِ كلِّه**: كلُّ طلّابِ المساحةِ يختفونَ من شاشةِ المدرّسِ إلى الأبد. أُصلِحَ بمُرشِّحٍ فوقَ `map()` واختبارٍ في `TeacherPanelRowTest` — **قِيسَ عضُّه**: بحذفِ المُرشِّحِ يفشلُ بـ٥٠٠ ذاتِها. ⚠️ **والتصحيحُ الأعمقُ مؤجَّلٌ عمداً**: تصحيحُ توصيفِ العلاقتَينِ إلى `?Course`/`?User` **٣١ خطأَ PHPStan في تسعةِ ملفّات**، والمفتاحُ الأجنبيُّ هجرةٌ تُغيِّرُ دلالةَ الحذفِ في المنصّةِ كلِّها — كلاهما تغييرٌ قائمٌ بذاتِه لا سطرٌ داخلَ لوحة.

  ⚠️ **ولاحظَ المرورُ ازدواجاً في قراءةِ التصحيح**: شارةُ الشريطِ الجانبيِّ تطلبُ `per_page=1` وبطاقتي كانت تطلبُ `20` لأجلِ رقمٍ واحد. صارت `5` — وهو **قاعُ الخادمِ المقيس** (`min(50, max(5, …))`)، فـ`1` ادّعاءٌ يُجيبُه الخادمُ بخمسة
- [X] T058 البوّاباتُ الأربع: `php vendor/bin/pest tests/Feature/LiveSessions tests/Feature/Learning` · `./vendor/bin/pint --test` · `./vendor/bin/phpstan analyse` · `npx tsc --noEmit` · `npm test`. ⚠️ **مرّةً واحدةً في نهايةِ التغييرِ المتماسك**، ولا مجموعتَي pest معاً. **المقيس**، بتشغيلةٍ واحدةٍ لكلِّ بوّابة:

  · `pest tests/Feature/LiveSessions tests/Feature/Learning tests/Feature/Payments tests/Feature/Identity` ⇒ **٩٩٩/٩٩٩**، ٥٢١٩ توكيدة، ١٢ دقيقة.
  · `pint --test` ⇒ أخضر · `phpstan analyse` ⇒ **٠ أخطاء** · `npx tsc --noEmit` ⇒ نظيف · `npm test` ⇒ **٥٧٨/٥٧٨** في ٩٠ ملفّاً.

  ⚠️ **و`Payments` أُضيفَت إلى قائمةِ المجلَّدات** لأنّ `T057` أخرجَ إصلاحاً فيها لم تطلبْه أيُّ مهمّة — بوّابةٌ لا تُشغِّلُ المجلَّدَ الذي مسَّته هي بوّابةٌ لا تحرسُ شيئاً
- [X] T059 أضِفْ إلى `CLAUDE.md` و`AGENTS.md` سطرَ العطلِ المقيس: `response()->json(Resource::collection($paginator))` لا تُسقِطُ `meta` وحدَها بل **الغلافَ كلَّه**، وقارئٌ يقرأُ `res.data ?? []` يعرضُ صفراً بلا خطأ — وفهرسٌ بلا اختبارٍ هو سببُ بقاءِ ذلك سنينَ. **أُضيفَ إلى الملفَّينِ معاً** (كان في `CLAUDE.md` وحدَه، مكتوباً من حالةِ `/cms/articles` التي لا قارئَ لها — فوُسِّعَ بالحالةِ المقيسة). ⚠️ **وأُضيفَ سطرٌ ثانٍ** عن عثرةِ `T057`: عمودٌ بلا مفتاحٍ أجنبيٍّ يجعلُ كلَّ `->relation->field` في Action خمسَمئةٍ تنتظرُ صفّاً محذوفاً واحداً

---

## Dependencies

```text
Phase 1 (T001–T004)  ─┬─► Phase 2 (T005–T012)
                      │
Phase 2 ─┬─► US1 (T013–T021) ─┐
         ├─► US2 (T022–T028) ─┤
         └─► US3 (T029–T048) ─┼─► US4 (T049–T054) ─► Polish (T055–T059)
                               │
   US4 يعتمدُ على البياناتِ التي جلبَتها US1/US2/US3 — لا على قراءةٍ خاصّةٍ به
```

- **T005–T009** تحجُبُ `US1` (بطاقاتُ العدِّ بلا `meta` كاذبة)
- **T010–T012** تحجُبُ `US2` (عنوانُ الجدولِ بلا الحقلِ غيرُ قابلٍ للتقرير)
- **US3** لا تعتمدُ على `US1` ولا على `US2` — قابلةٌ للتنفيذِ بالتوازي بعدَ Phase 2
- **T029/T030 قبلَ T031–T037**: الاختبارُ يُكتَبُ أوّلاً لأنّ حالتَه الفارقةَ هي ما يُثبِتُ التجاوزَ

## Parallel Opportunities

| المجموعة | المهامّ | لماذا آمنة |
|---|---|---|
| Setup | T001 · T002 · T003 | ثلاثةُ ملفّاتٍ جديدةٍ لا تلمسُ بعضَها |
| قرّاءُ التسجيلات | T007 · T008 | صفحتانِ مختلفتان |
| بطاقاتُ الطالب | T013 · T014 · T015 · T017 · T018 · T019 | بطاقةٌ لكلِّ ملفّ، ولا حالةَ مشتركة |
| بطاقاتُ المدرّس | T022 · T023 · T024 · T025 · T026 | كما فوق |
| بطاقاتُ وليِّ الأمر | T041 · T042 · T043 · T044 · T045 | كما فوق |
| الرسوم | T049 · T050 · T051 | ثلاثةُ ملفّات |

**البطاقاتُ متوازيةٌ بالبناءِ لا بالصدفة**: كلُّ واحدةٍ تملكُ طلبَها وحالتَها، وهو التنفيذُ
الحرفيُّ لـ`FR-013`.

## Implementation Strategy

**MVP** = Phase 1 + Phase 2 + **US1**. يُشحَنُ وحدَه ويفيدُ أكبرَ جمهورٍ للشاشة، ويحملُ معه إصلاحَ
عطلٍ مشحونٍ يمسُّ ثلاثَ شاشاتٍ خارجَ اللوحة.

**الزيادةُ الثانية** = **US2** — وهي التي تُنهي «أنتَ تملكُ صفراً» في وجهِ المدرّس.

**الزيادةُ الثالثة** = **US3** — أكبرُ كلفةٍ (المسارانِ الجديدانِ الوحيدان) وأكثرُ ما يحتاجُ حراسةً.

**الزيادةُ الرابعة** = **US4** — تُسلَّمُ فوقَ الثلاثِ ولا تحجُبُ شيئاً.

---

## Task Summary

| Phase | المهامّ | العدد |
|---|---|---|
| 1 · Setup | T001–T004 | 4 |
| 2 · Foundational | T005–T012 | 8 |
| 3 · US1 الطالب (P1) 🎯 | T013–T021 | 9 |
| 4 · US2 المدرّس (P1) | T022–T028 | 7 |
| 5 · US3 وليُّ الأمر (P2) | T029–T048 | 20 |
| 6 · US4 الرسوم (P3) | T049–T054 | 6 |
| 7 · Polish | T055–T059 | 5 |
| **المجموع** | | **59** |
