# Tasks: ٠٣٨ — سجلُّ الجلساتِ والأجهزةِ يُجهَّلُ بالعمر، ويُسقَفُ بالعدد

**Input**: `specs/038-session-log-retention/` — spec.md · plan.md (**النسخةُ الثانية**) · research.md (ر-١…ر-١٢) · data-model.md · contracts/legal-hold-directory.md · quickstart.md (٠…١٤) · review-findings.md

**Tests**: **مطلوبةٌ صراحةً**. و`quickstart.md` § ٠ يُقرَأُ قبلَ كتابةِ حالةٍ واحدة — فيه ثلاثةُ ألغامٍ في المُهيَّأِ تُبطِلُ كلَّ تأكيدٍ فوقَها.

**Organization**: بالقصص، وقبلَها طوران. **US1 وUS2 معاً هما المنتَجُ الأدنى** بنصِّ المواصفة: التجهيلُ وحدَه **لا يُنقِصُ صفّاً واحداً**، والسقفُ وحدَه يحذِفُ صفوفاً ما زالت تحملُ عناوين.

---

## ⛔ هذه النسخةُ الثانيةُ من قائمةِ المهامّ — وما أسقطَته مراجعةُ الوكلاء

ستّةُ وكلاءَ قرؤوا النسخةَ الأولى على الشجرةِ في ٢٠٢٦-٠٩-٢٢، بُعدٌ لكلِّ واحد. **وكلُّ CRITICAL هنا فُتِحَ وقُرِئَ باليدِ قبلَ أن يُكتَب.** ولم يُحذَفْ شيءٌ بل صُحِّح.

| ما سقط | البُعد | أينَ ذهب |
|---|---|---|
| ⛔ `->groupBy()->get()` هو `select *` ⇒ **MySQL ERROR 1055** تحتَ `ONLY_FULL_GROUP_BY`، وSQLite تُرجِعُ صفّاً فتمرُّ خضراءَ للأبد | MySQL | T026 |
| ⛔ T008 وحدَها تُحمِّرُ الحزمة (**حارسُ اليتامى**)، وT010 وحدَها تُحمِّرُها (**حارسُ الفئةِ المُعلَنةِ بلا صفّ**) — التزامةٌ واحدةٌ لا ثلاث | الترتيب | بندُ T008–T010 |
| ⛔ `audience` عمودٌ `NOT NULL` بلا افتراضٍ وغائبٌ عن جردِ المهمّة ⇒ **كلُّ اختبارِ Feature يقع** داخلَ `migrate:fresh` | الترتيب | T008 |
| ⛔ `ip_hash` **ليس تجزئةً**: `sha256` بلا ملحٍ على فضاءِ ٢³² — قائمةُ عناوينَ مقروءةٌ يفتحُها **الطالبُ** لا صاحبُ الحساب | الخصوصيّة | T010 · T011 |
| ⛔ `playback_grants.issued_ip_hash` **نفسُ القيمة**، مفهرسٌ على `auth_session_id`، بلا كتالوجٍ ولا كنسة | الخصوصيّة | T003 · T033 |
| ⛔ `PanelHandoffController:84` · `:152` يكتبانِ العنوانَ **صريحاً** في `activity_log` بلا كتالوجٍ ولا كنسة | الخصوصيّة | T003 |
| ⛔ ذراعُ المحوِ كانت تُفرِّغُ `session_id` على الصفوفِ **النشطة** ⇒ جلسةُ لوحةٍ **لا يُمكِنُ إنهاؤُها** (`TerminateAuthSession:57` مِقبضُها الوحيد) | الخصوصيّة | T013 |
| ⛔ وإعادةُ توجيهِ النشطةِ إلى المقبرةِ **تُطفِئُ حدَّ الأجهزة** — `enforceLimit()` يَعُدُّ `device_id` المتمايزةَ بينَ النشطات | الخصوصيّة | T013 |
| ⛔ `surface` **مشتقٌّ من `session_id`** (`AuthSessionResource:48`)، فمحوُه يقلِبُ شارةَ كلِّ جلسةِ لوحةٍ مُجهَّلةٍ إلى «app» — على الشاشةِ **وفي الأرشيف** | الشجرة · الخصوصيّة | T019 |
| ⛔ تأكيدُ `rows_anonymised` جوفاء: مُهيَّأُ `RetentionSweepIdempotencyTest` **بلا صفِّ جلسةٍ واحد** | الاختبارات | T022 |
| ⛔ «الثانيةُ صفر» خضراءُ على ذراعٍ **تنفجرُ كلَّ مرور**: `RunRetentionSweepJob:135` يلتقطُ الرميةَ ويُكمِل | الاختبارات | T021 |
| ⛔ SC-002 وUS2·١ **تناقضانِ أرضيّةَ FR-004** — ١٤٠ صفّاً مُجهَّلاً والسقفُ ٥٠ | التغطية | T003 |
| ⛔ SC-004 (**ثلاثةُ أشهرٍ على الأقلّ**) بلا مهمّةٍ تقيسُها، وصحيحةٌ اليومَ بالصدفةِ الحسابيّةِ وحدَها | التغطية | T031 · T034 |
| `whereIn` بقائمةٍ غيرِ محدودةٍ ⇒ **ERROR 1390** (٦٥٥٣٥) و`SQLITE_MAX_VARIABLE_NUMBER` (٣٢٧٦٦) | MySQL | T026 |
| `started_at` **ليس عموداً**، و`hash('sha256')` **٦٤** محرفاً لا ٦٦، والهجرتانِ تحتَ الوحدةِ لا الجذر | الشجرة | T001 · T009 · T010 · T014 |
| مهمّةُ «اكتبْ حالةَ اتّفاقِ وضعِ المحو» كانت تطلبُ حالةً **قائمةً** تشتقُّ من السِّجِلّ | الشجرة · الاختبارات | T015 |

### قرارا المالكِ بعدَ المراجعة (٢٠٢٦-٠٩-٢٢)

1. **`ip_hash` يُصدَّرُ لصاحبِ الحسابِ وحدَه** — ويُحذَفُ من أرشيفٍ يفتحُه وليُّ أمرٍ أو منتدَب (T010).
2. **`playback_grants` خارجَ النطاق، ومعه أرضيّةٌ دنيا للمدّة** — ثمانيةُ أيّامٍ في حارسِ `SaveDataCategory`، لأنّ المنحَ تُكنَسُ بعدَ انتهائِها بأسبوع (T033).

### ما قِيسَ ولم يتغيّر

| الموضع | ما قِيس |
|---|---|
| `IdentityPersonalData.php:260` · `:280` | `expire()` جسدُها `return 0;` · `alreadyAnonymised()` على بادئةِ البريد |
| `Device.php:33` | `$fillable` يحملُ `label` — فلا صمتَ في `firstOrCreate` |
| `PlatformSettings.php:88` · `:319` · `:328` | بادئةُ `auth.` قائمة · `all()`/`flush()` تلفّانِ على `KEYS` وحدَها |
| `Shared/Contracts/` | ٢٥ ملفّاً، **١٦ منها `*Directory`**، وصفرُ واجهةِ قدرةٍ بصيغةِ فعل |
| `app/Modules/*/Filament/` | ٢٥ استدعاءَ Action، **كلُّها من وحدتِها** — فT033 أوّلُ عبور |
| فئاتُ `identity` الخمس | كلُّها `ErasureMode::Anonymise` (فقيمةٌ مخالفةٌ تُرجِعُ `Retain` للوحدةِ كلِّها) |
| `config/database.php:60` | `'strict' => true` بلا `modes` ⇒ `MySqlConnector:148` يفرضُ `ONLY_FULL_GROUP_BY` |
| `frontend/src/lib/sessions.ts:24` · `security/page.tsx:106` | `device: { uuid, label }` · الشاشةُ تقرأُ `label` **ولا تبني على `uuid`** زرّاً |

---

## Phase 1: Setup — المُهيَّأُ ونصُّ المواصفةِ قبلَ كلِّ قياس

- [X] T001 اكتبْ `ip_hash` في `backend/database/factories/Modules/Identity/AuthSessionFactory.php` — ⛔ **المصنعُ لا يكتبُه إطلاقاً، و`ip_hash IS NULL` هي علامةُ «جُهِّلَ سلفاً»**: فكلُّ صفٍّ مبنيٍّ بالمصنعِ **مولودٌ مُجهَّلاً**، وكلُّ حالةِ تجهيلٍ فوقَه خضراءُ على بناءٍ لا ذراعَ فيه. ⚠️ **والقيمةُ ٦٤ محرفاً** (`hash('sha256', …)`) — وهو عرضُ العمودِ بالضبط (`string('ip_hash', 64)`)؛ وأيُّ زيادةٍ **ERROR 1406** على MySQL الصارمةِ وقصٌّ صامتٌ على SQLite
- [X] T002 اشتقَّ `device_id` من `user_id` في `backend/database/factories/Modules/Identity/AuthSessionFactory.php` — ⛔ **العمودانِ مصنعانِ مستقلّانِ اليوم**، وهو شكلُ `ChapterFactory` الذي دفعَ المستودَعُ ثمنَه: جلسةُ شخصٍ وجهازُ آخر، فاختبارُ الحظرِ القضائيِّ يُعفي صاحبَ الجلسةِ ويُجهِّلُ جهازَ غيرِه **ويمرُّ أخضر**. ⚠️ **والمفتاحُ المُشتَقُّ يُسمّى بعدَ والدِه**: `expandAttributes()` يمشي بالترتيبِ ولا يُعطي المُغلَقةَ إلّا ما حَلَّ قبلَها
- [X] T003 رقِّعْ `specs/038-session-log-retention/spec.md` — **أربعةُ مواضعَ، ويُكتَبُ التصحيحُ ولا يُحذَفُ الأصل**: (أ) US1 السيناريو ٢ وتذييلُ FR-008 ⇐ «يُقرَأُ كما كانَ **إلّا اسمَ الجهاز**، فهو يصيرُ جملةَ FR-008 الصريحة» — ⚠️ **وبابُ الدخولِ يبقى بنصِّه لأنّ T019 يُصلِحُ اشتقاقَه، لا لأنّه لم يكنْ مهدَّداً**. (ب) SC-002 وUS2 السيناريو ١ ⇐ «المنتهيةُ المُجهَّلةُ **التي تجاوزَتِ الأرضيّة**» — ⛔ **بدونِها المعيارُ كاذبٌ على كلِّ حسابٍ يدخلُ يوميّاً**: التجهيلُ عندَ ٩٠ والأرضيّةُ ١٨٠، فما بينَهما مُجهَّلٌ وخارجَ متناولِ السقفِ ⇒ ١٤٠ صفّاً والسقفُ ٥٠. (ج) SC-001 ⇐ «لا طريقَ **من الصفِّ ولا من صفِّ جهازِه**» بدلَ إطلاقِ النفي. (د) «خارجَ النطاق» يكسبُ ثلاثةَ مخازنَ مقيسة: `activity_log` (`PanelHandoffController:84` · `:152` يكتبانِ `properties.ip` **صريحاً بلا تجزئة**) · `terms_consents.ip_address` · `playback_grants.issued_ip_hash` (مفهرسٌ على `auth_session_id`، **نفسُ قيمةِ** عنوانِ الجلسة)
- [X] T004 [P] أضِفْ إلى `specs/038-session-log-retention/plan.md` القرارَ الحاديَ عشرَ: حدُّ التصديرِ ونافذةُ المنح — قرارا المالكِ أعلاه بنصِّهما، فمن يقرأُ الخطّةَ وحدَها يرى لماذا التصديرُ مُقيَّدٌ ولماذا للمدّةِ أرضيّةٌ دنيا

---

## Phase 2: Foundational — العقدُ والكتالوجُ والتصديرُ والمحو

**⚠️ CRITICAL**: لا قصّةَ تبدأُ قبلَ اكتمالِ هذا الطور.

- [X] T005 [P] اكتبْ `backend/app/Shared/Contracts/LegalHoldDirectory.php` بالتوقيعِ الحرفيِّ في [contracts/legal-hold-directory.md](./contracts/legal-hold-directory.md): `heldUserIds(): array` وحدَها، **أعدادٌ خروجاً ولا نوعَ من `Compliance` في التوقيع** — قاعدةُ `OutstandingCreditsDirectory` و`SettlementClearance` نفسُها. ⚠️ **ودليلٌ لا قدرة**: ١٦ من ٢٥ ملفّاً في `Shared/Contracts/` هي `*Directory` و**صفرٌ منها واجهةُ قدرةٍ بصيغةِ فعل**
- [X] T006 اكتبْ `backend/app/Modules/Compliance/Support/EloquentLegalHoldDirectory.php` — سطرٌ واحدٌ فوقَ `LegalHold::query()->inForce()->pluck('subject_user_id')`، بجوارِ `EloquentTeacherOffboardingDirectory.php`. ⚠️ **ولا `[P]` مع T005**: هذا الملفُّ `implements` واجهتَها فلا يُترجَمُ قبلَها
- [X] T007 اربِطِ العقدَ في `backend/app/Modules/Compliance/ComplianceServiceProvider.php` — والوحدةُ التي تُعلِنُ الحظرَ هي التي تُنفِّذُ الدليل، فالهُويّةُ لا تستوردُ `LegalHold` (المبدأُ الثالث)

> ⛔ **T008 · T009 · T010 التزامةٌ واحدةٌ لا ثلاث — والاتّجاهانِ كلاهما أحمر.** `DataCategorySeeder` مُنادى من `TestCatalogueSeeder:60` فيعملُ في **كلِّ** اختبارِ Feature: فصفّانِ في الكتالوجِ بلا `describe()` يُسقِطانِ فحصَ اليتامى في `PersonalDataContractCoverageTest` («كلُّ مفتاحٍ في الجدولِ له مالك»)، و`describe()` بلا صفَّينِ يُسقِطُ `ErasureModeAgreementTest::finds a catalogue row for every category a module declares`. **والقاعدةُ في هذا الملفِّ أنّ الحزمةَ خضراءُ بعدَ كلِّ مهمّة** — فتُكتَبُ الثلاثُ وتُلتزَمُ معاً.

- [X] T008 أضِفِ الصفَّينِ `auth_session` و`device` إلى `backend/database/seeders/DataCategorySeeder.php::categories()` — ⛔ **والجردُ الكاملُ هو المصفوفةُ الحرفيّةُ في `data-model.md` §٢، لا قائمةٌ تُعادُ كتابتُها هنا**: `audience` عمودٌ **`NOT NULL` بلا افتراض** (`…_create_data_categories_table.php:31`)، وصفٌّ بدونه يرمي داخلَ `TestCatalogueSeeder` داخلَ `migrate:fresh` ⇒ **كلُّ اختبارٍ في الحزمةِ يقعُ**، شكلُ الـ٦٩٠/٦٩٠ المسجَّلُ في `CLAUDE.md`. و`is_required` افتراضُه **`false`** (`:36`) فيُكتَبُ `true` صراحةً وإلّا شُحِنَ الصفّانِ قابلَينِ للسحبِ عكسَ §٢. ⛔ **و`erasure_mode = Anonymise` ليس خياراً**: `modeFor()` يشترطُ وضعاً **واحداً** للوحدة، والخمسُ القائمةُ `Anonymise`. ⛔ **و`retain_days` عددٌ لا `null`**: `expires()` تشترطُ العمودَينِ معاً، وصفٌّ بمدّةٍ فارغةٍ يُطبَعُ عنه «يُحفظ ما دام الحساب قائماً» — إشعارٌ كاذب. ⚠️ و`label`/`purpose` قابلانِ للترجمة (٠٥٥)
- [X] T009 اكتبْ ترحيلَ الإملاءِ الخلفيِّ للصفَّينِ تحتَ `backend/app/Modules/Identity/Database/Migrations/` — ⛔ **تحتَ الوحدةِ لا تحتَ `database/migrations/`**: الجذرُ يحملُ سبعةَ ملفّاتِ إطارٍ وحدَها، والسابقةُ المُستشهَدُ بها `2026_09_16_000200_backfill_guardian_link_data_category.php` **في هذا المجلّدِ بعينِه** وتكتبُ صفَّ كتالوجٍ من وحدةِ الهُويّةِ تماماً كهذا. ⛔ **ويُنادي `run()` لا `seedMissing()`** — الأخيرةُ غيرُ موجودةٍ على هذا الباذرِ أصلاً و`run()` هو `firstOrCreate` على المفتاحِ وحدَه. ⚠️ **وترحيلٌ يُخفِقُ يأخذُ معه كلَّ ما خلفَه في الطابور** — كما حدثَ لصفِّ الكتالوجِ وقالبِ التنبيهِ في نشرِ `course_waitlist_entries`
- [X] T010 أضِفِ المفتاحَينِ إلى `describe()` **وذراعَي التصديرِ إلى `export()`** في `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — الحقول: `auth_session` ⇐ `surface · status · ended_reason · device_label · started_at` (⟵ **`created_at`**، فلا عمودَ بهذا الاسم) `· last_active_at · ended_at` · و`device` ⇐ `label · created_at · last_seen_at`. ⛔ **و`ip_hash` يُخرَجُ لصاحبِ الحسابِ وحدَه** (قرارُ المالك): يُضافُ حينَ `$subject->grantedScope === null` ويُحذَفُ من أرشيفٍ يفتحُه وليُّ أمرٍ أو منتدَب، على سابقةِ بوّابةِ `guardian_link` في الملفِّ نفسِه. **والسببُ مقيسٌ**: `sha256` بلا ملحٍ على فضاءِ ٢³² **ليس تجزئةً** بل عنوانٌ مقروء، والأرشيفَ يُنزِّلُه **الطالبُ** لا الموضوع، والتصديرُ يُدفَعُ تلقائيّاً بلا موظّفٍ في المسار. ⛔ **والمفتاحُ يُخرَجُ فارغاً ولا يُحذَف** (SC-008) — تهجئةُ `date_of_birth` عينُها. ⚠️ **و`devices.first_seen_at` غيرُ موجود**: العمودانِ `last_seen_at` و`timestamps()`
- [X] T011 [P] أضِفْ `fingerprint_hash` إلى `forbiddenKeys()` في `backend/app/Modules/Compliance/Support/ExportFieldAllowlist.php` بسببِه مكتوباً — بصمةُ آلةٍ لا يقرؤُها صاحبُها ولا تُثبِتُ له شيئاً. ⚠️ **واكتبْ بجوارَها لماذا `ip_hash` ليس معها**: تعليقُ الملفِّ (`:52`) يقولُ إنّ غيابَه **مقصود** — لكنّ ذاك القرارَ كُتِبَ عن `terms_consents.ip_address`، **عنوانٍ واحدٍ يُثبِتُ موافقةً واحدة**، لا عن سجلِّ أماكنَ لكلِّ دخول. فالبوّابةُ في T010 هي التمييز، لا قائمةُ المنع
- [X] T012 اكتبْ `backend/app/Modules/Identity/Support/AuthSessionRetention.php` — بدائيّاتُ التجهيلِ الثلاث: صفُّ المقبرةِ `firstOrCreate(['user_id' => $u, 'fingerprint_hash' => 'anonymised:u'.$u], ['label' => 'جهازٌ لم تعدْ تفاصيلُه محفوظة'])` · تجهيلُ صفوفِ الجلساتِ · تجهيلُ صفوفِ الأجهزة. ⛔ **والقيمةُ تُبنى في PHP** على سابقةِ `Anonymiser::email()`: **لا `||`** — عاملُ «أو» منطقيٍّ على MySQL بلا `PIPES_AS_CONCAT` (و`MySqlConnector:148` لا يضعُها)، فتُكتَبُ `'1'` في كلِّ صفّ **فتصطدمُ بـ`unique(user_id, fingerprint_hash)`** عندَ ثاني جهازٍ وتُرجِعُ معاملةَ المحوِ كلَّها، **ولا تتقارَب** لأنّ `'1' NOT LIKE 'anonymised:%'` — **وعلى SQLite تعملُ صحيحةً تماماً** (مقيسٌ على ٣.٤٩). ⚠️ **ولا `CONCAT()`**: وصلَت SQLite في ٣.٤٤ وقد يحملُ عاملُ CI أقدم
- [X] T013 اكتبْ ذراعَي المحوِ في `erase()` داخلَ `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — ⛔ **و`status = 'ended'` شرطٌ على ذراعَي `session_id` و`device_id`، لا على `ip_hash`**، وهذا أخطرُ سطرٍ في الطور: `session_id` هو **المِقبضُ الوحيدُ** لجلسةِ اللوحة (`RecordPanelSession:48` يكتبُ `token_id => null`)، و`TerminateAuthSession:57` يُنهيها بـ`if ($session->session_id !== null)` — فتفريغُه على صفٍّ نشطٍ يجعلُ الجلسةَ **غيرَ قابلةٍ للإنهاءِ من أيِّ باب** بينما المتصفّحُ يعملُ، وهو عينُ الثقبِ الذي أغلقَته ٠٣٧. **وإعادةُ توجيهِ النشطةِ إلى المقبرةِ تُطفِئُ حدَّ الأجهزة**: `enforceLimit()` يَعُدُّ `device_id` المتمايزةَ بينَ النشطات، فأربعُ آلاتٍ تصيرُ واحدة. والمحوُ لا يُنهي جلسةً ولا يحذِفُ رمزاً، فحسابٌ مُحِيَ ولوحتُه مفتوحةٌ هو الحالةُ العاديّة. ⛔ **والذراعانِ فوقَ حارسِ `alreadyAnonymised()` لا تحتَه** (FR-014). ⚠️ **وذراعُ الأجهزةِ تحملُ `WHERE fingerprint_hash NOT LIKE 'anonymised:%'`** وإلّا أعادَت كتابةَ المقبرةِ المكتوبةِ قبلَها بسطرٍ فتُولَدُ مقبرةٌ ثانيةٌ في المرّةِ التالية. ⚠️ **ولا يُضعَّفُ الحارسُ**، والذراعانِ تُرجِعانِ `0` فلا تُطيلانِ حلقةَ `$done < $limit`
- [X] T014 اكتبْ ترحيلَ المرّةِ الواحدةِ لمن مُحِيَ قبلَ اليومِ تحتَ `backend/app/Modules/Identity/Database/Migrations/` — ⛔ **بـ`DB::table()` ولا نموذجَ واحد**: ترحيلٌ يتكلّمُ مخطَّطَ تاريخِه ونموذجٌ يتكلّمُ اليوم، و`2026_08_29_003100_backfill_referral_catalogue_action` أسقطَ **٦٩٠ من ٦٩٠** اختباراً بهذا عينِه. ⛔ **و`chunkById` لا `chunk`**: مُرشِّحُ `ip_hash IS NOT NULL` ينكمِشُ تحتَ الترقيمِ بالإزاحةِ فتتخطّى كلُّ صفحةٍ ما أصلحَته سابقتُها **وتُبلِّغُ بالنجاح**. ⛔ **وصفُّ المقبرةِ يُكتَبُ بـ`uuid` وطوابعِ وقتٍ صريحة**: الإدراجُ الخامُّ لا يُقلِعُ النموذجَ فلا يعملُ `HasUuid`، وMySQL يُنزِلُ انتهاكَ NOT NULL إلى تحذيرٍ ويخزِّنُ `''` فيصطدمُ كلُّ صفٍّ لاحقٍ على `unique(uuid)`. ⛔ **وقيدُ `status = 'ended'` نفسُه في T013 يسري هنا حرفاً بحرف**. ⚠️ **ويُستخرَجُ الجسدُ إلى دالّةٍ يُناديها الترحيل** (`AuthSessionRetention::backfillErasedAccounts()`) — وإلّا فالترحيلُ يعملُ داخلَ `migrate:fresh` قبلَ وجودِ أيِّ صفّ، **ولا اختبارَ في الشجرةِ يقدِرُ أن يراه**
- [X] T015 وسِّعْ `backend/tests/Feature/Compliance/ErasureModeAgreementTest.php` **بسطرٍ واحد** — ⚠️ **والحالتانِ العامّتانِ قائمتانِ في الملفِّ وتشتقّانِ من `PersonalDataRegistry` فتُغطّيانِ `identity` تلقائيّاً، فلا تُكرَّران**: المطلوبُ `expect($identity->describe())->toContain('auth_session')->toContain('device')` ومعه أنّ أوضاعَ السبعِ مجموعةٌ واحدة — **بدونِ النصفِ الأوّلِ تُجمَعُ الأوضاعُ فوقَ خمسِ فئاتٍ وتمرُّ خضراءَ والصفّانِ غائبان**

**Checkpoint**: الفئتانِ مُعلَنتانِ، والتصديرُ والمحوُ يصلانِهما، والحزمةُ خضراء — و`expire()` ما زالت `return 0`.

---

## Phase 3: User Story 1 — الطالبةُ تقرأُ سجلَّها الأمنيَّ بعدَ التنظيف (P1) 🎯 نصفُ الـMVP

**Independent Test**: تُشيَّخُ جلسةٌ منتهيةٌ **باستعلام**، تُشغَّلُ المكنسة، ثمّ تُقرأُ الحمولة: الصفُّ قائمٌ بتاريخَيه وسببِ انتهائِه **وبابِ دخولِه**، و`ip_hash` و`session_id` فارغان، و`device_id` يُشيرُ إلى المقبرة — ومعه **ضابطٌ موجَبٌ في التشغيلةِ نفسِها**: جلسةٌ حديثةٌ لم تُمَسّ.

- [X] T016 [US1] أعِدْ كتابةَ جسدِ `expire()` وتعليقِها في `backend/app/Modules/Identity/Support/IdentityPersonalData.php:260` — ⛔ **التعليقُ يُعادُ كتابتُه لا يُضافُ تحتَه**: نصُّه اليومَ «**لا شيءَ في هذه الوحدةِ ينتهي بساعة**» ويصيرُ كذباً بهذه المهمّة. ⚠️ **والذراعانِ تُرجِعانِ `0` في هذه المهمّةِ وحدَها** حتّى T017/T018، وإلّا لم يُترجَمِ الملفُّ ولا مرَّ المحلّلُ الساكنُ لمهمّتَين
- [X] T017 [US1] اكتبْ ذراعَ الجلسةِ في `expire()` داخلَ `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — المُرشَّحون: `status = 'ended' AND COALESCE(ended_at, created_at) < :before AND ip_hash IS NOT NULL AND user_id NOT IN (:exempt) LIMIT :batch`، ولكلِّ صفٍّ في PHP: `device_id ← المقبرة · ip_hash ← NULL · session_id ← NULL`. ⛔ **`ended_at` لا `created_at`** وهو انحرافٌ عن تعليقِ العقدِ يُكتَبُ بدلَ أن يُسكَتَ عنه: جلسةٌ بُدِئَت قبلَ سنةٍ وانتهَت أمسِ دليلٌ عمرُه يوم. ⚠️ **و`COALESCE` تهجئةٌ واحدةٌ مع ذراعِ الجهاز**: العمودُ `nullable` في الهجرة، و`NULL < :before` فارغةٌ فيُحتفَظُ بالعنوانِ **إلى الأبد** — والكاتبانِ الوحيدانِ لـ`ended` يكتبانِه اليوم (`TerminateAuthSession:62` · `RevokeTeacherSessions:48`)، فالارتدادُ غيرُ مُدرَكٍ ويبقى لئلّا تكونَ تهجئتان. ⛔ **و`session_id` يُمحى مع `ip_hash`** (FR-015): أُضيفَ ٢٠٢٦-٠٩-١٧ وهو غائبٌ عن كلِّ جردٍ كُتِبَ لهذا الجدول. ⚠️ **و`user_id NOT IN` تُلَفُّ بـ`->when($exempt !== [], …)`** — `NOT IN ()` خطأُ تركيبٍ على المحرّكَين؛ السابقةُ ذراعُ `attendance_record` في `LiveSessionsPersonalData`
- [X] T018 [US1] اكتبْ ذراعَ الجهازِ في `expire()` داخلَ `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — `WHERE fingerprint_hash NOT LIKE 'anonymised:%' AND user_id NOT IN (:exempt) AND created_at < :before AND NOT EXISTS (جلسةٌ active) AND NOT EXISTS (جلسةٌ COALESCE(ended_at, created_at) >= :before) LIMIT :batch` ⇒ `fingerprint_hash ← 'anonymised:'.$id` **مبنيّةً في PHP** (T012). ⛔ **`created_at < :before` يُغلِقُ سباقاً حقيقيّاً**: `StartAuthSession` يكتبُ الجهازَ ثمّ الرمزَ ثمّ الجلسة، وبينَهما الجهازُ **بصفرِ جلسات** فيصدُقُ عليه الشرطانِ **فراغاً** — وتجهيلُه يكسِرُ مطابقةَ `firstOrNew` فتصيرُ الآلةُ الواحدةُ آلتَينِ في حدِّ الأجهزة، أي **طردُ الطالبِ من الحاسوبِ الذي يجلسُ أمامَه**. ⛔ **و`COALESCE`**: `NULL >= :before` فارغةٌ فلا يُرى الصفُّ فتُجهَّلُ بصمةُ جهازٍ عليه جلسةٌ حديثة. ⚠️ **ولا يُقرَأُ `last_seen_at`** — قابلٌ للفراغ. ⚠️ **و`created_at` نفسُه قابلٌ للفراغِ** (`timestamps()` تُخرِجُه كذلك على المحرّكَين)، فصفٌّ به فارغٌ لا يُجهَّلُ أبداً — يُقالُ ولا يُصلَحُ، فلا كاتبَ في الشجرةِ يترُكُه
- [X] T019 [P] [US1] اشتقَّ `surface` من `token_id` لا من `session_id` في `backend/app/Modules/Identity/Http/Resources/AuthSessionResource.php:48` — ⛔ **بدونِها يقلِبُ T017 شارةَ كلِّ جلسةِ لوحةٍ مُجهَّلةٍ إلى «app»**، على الشاشةِ **وفي أرشيفِ التصديرِ معاً**، بينما `spec.md:78` تُسمّي «بابَ الدخولِ» ضمنَ ما يبقى. و`token_id` هو الجوابُ الصحيحُ للسؤالِ نفسِه: `null` لكلِّ صفِّ لوحةٍ (`RecordPanelSession:48`) ومكتوبٌ لكلِّ صفِّ تطبيق (`StartAuthSession:60`)، **ولا ذراعَ تُفرِّغُه**، ولا يُسمّي آلةً. ⚠️ **والتعليقُ فوقَ السطرِ يُعادُ كتابتُه**: نصُّه اليومَ يُبرِّرُ الاشتقاقَ من `session_id` بأنّ «عمودٌ ثانٍ يُجيبُ السؤالَ نفسَه هو الجوابُ الذي يتخلّفُ عن الأوّل» — والحجّةُ تبقى صحيحةً وتنتقلُ إلى العمودِ الذي لا يُمحى. ⚠️ **وخلفيّةٌ بحتة**، فقياسُ T023 لا يتأثّر
- [X] T020 [US1] اكتبْ `backend/tests/Feature/Identity/AuthSessionRetentionTest.php` — ⛔ **وذراعُ الجهازِ تحتاجُ خمسَ حالاتٍ، لكلِّ واحدةٍ شرطٌ حامٍ واحدٌ فقط**، وإلّا نجا كلُّ حذفٍ مفردٍ لشرط (شكلُ ٠٠٦·US6 الذي مرَّ بحذفِ الحارسِ كلِّه): (أ) جهازٌ مُشيَّخٌ **وعليه جلسةٌ نشطة** ⇒ بصمتُه كما هي · (ب) مُشيَّخٌ · بلا نشطةٍ · جلسةٌ منتهيةٌ **حديثةُ `ended_at`** ⇒ كما هي · (ج) مُشيَّخٌ · صفٌّ `ended` بـ`ended_at = NULL` و`created_at` حديث ⇒ كما هي (حالةُ `COALESCE`) · (د) `created_at = now()` **وبصفرِ جلسات** ⇒ كما هي (سباقُ الميلاد) · (هـ) مُشيَّخٌ وكلُّ جلساتِه منتهيةٌ قديمة ⇒ **`LIKE 'anonymised:%'`**، وهو الضابطُ الموجَبُ في التشغيلةِ نفسِها. **وتُقاسُ على `devices.fingerprint_hash` مباشرةً لا بتسجيلِ دخولٍ عبرَ HTTP.** وحالاتُ الجلسةِ من `quickstart.md` § ٣–٦: المُجهَّلةُ `ip_hash` **و`session_id`** كلاهما `NULL` **في تأكيدٍ واحد** (وإلّا نجا حذفُ FR-015 من الملفِّ كلِّه) · الحديثةُ لم تُمَسّ · **النشطةُ المُشيَّخةُ** (`created_at` قديمٌ و`ended_at` فارغ) لم تُمَسّ — وهي وحدَها ما يعضُّ تنفيذاً يُرشِّحُ بـ`created_at` وينسى `status` · المقبرةُ واحدةٌ لكلِّ مستخدِم · `Device::count()` قبلَ وبعدَ **متساوٍ** ولا جلسةَ تُشيرُ إلى صفٍّ غيرِ موجود (إشارةٌ مكسورةٌ = خطأُ ٥٠٠ **للطلبِ كلِّه**) · `surface` على صفِّ لوحةٍ مُجهَّلٍ ما زالَ `panel` · الحظرُ القضائيُّ يُوقِفُ الذراعَينِ معاً **بقيمةِ العمودَينِ لا بعددِ الصفوف** · المحوُ يصلُ الجدولَينِ **عبرَ `ExecuteDataErasure` لا بنداءِ `erase()` مباشرة** (النداءُ المباشرُ يتخطّى `modeFor()` فيمرُّ على وحدةٍ صارَ محوُها `Retain`) · **وجلسةُ لوحةٍ نشطةٌ لحسابٍ مُحِيَ ما زالَ `session_id` لها مكتوباً** (T013). ⛔ **والتشييخُ باستعلام**: `created_at` ليس في `$fillable` فصفٌّ «قديمٌ» في `create()` يُولَدُ اليوم
- [X] T021 [US1] شغِّلِ المكنسةَ **مرّتَينِ** في كلِّ حالةِ تجهيلٍ في `backend/tests/Feature/Identity/AuthSessionRetentionTest.php` — ⛔ **ثلاثةُ تأكيداتٍ لا واحد**: المرورُ الأوّلُ `rows_anonymised > 0`، والثاني `=== 0`، **و`findings_count === 0` في الاثنَين**. والسببُ مقيسٌ: `RunRetentionSweepJob:135` يلتقطُ رميةَ الفئةِ ويُكمِلُ ويكتبُ `rows_anonymised = 0` — فذراعٌ تنفجرُ في **كلِّ** مرورٍ (اصطدامُ الفهرسِ الذي تحذِّرُ منه T012) تُبلِّغُ صفراً مرّتَينِ **وتُرضي «الثانيةُ صفر» تماماً**
- [X] T022 [US1] وسِّعْ **مُهيَّأَ** `backend/tests/Feature/Compliance/RetentionSweepIdempotencyTest.php` بجلسةٍ منتهيةٍ بعمرِ ٤٠٠ يومٍ تحملُ `ip_hash` وصفِّ جهازٍ بعمرِها (كلاهما مُشيَّخٌ **باستعلام**) — ⛔ **توسيعُ التأكيدِ وحدَه جوفاء**: مُهيَّأُ الملفِّ اليومَ تقدُّمُ دروسٍ وحضورٌ وأصلُ وسائطَ وتنبيهان و**صفرُ صفوفٍ في `auth_sessions`**، و`rows_anonymised` يُغذّيه `attendance_record` سلفاً — فالتأكيدُ أخضرُ والميزةُ غائبةٌ تماماً. وبعدَ المرورِ الأوّل: `ip_hash IS NULL` والبصمةُ `LIKE 'anonymised:%'`؛ وبعدَ الثاني `rows_anonymised === 0` **و`findings_count === 0`** — والملفُّ لا يُؤكِّدُ الثانيَ إطلاقاً اليوم
- [X] T023 [P] [US1] اقِسْ «صفرُ تغييرٍ في ملفّاتِ الواجهة» (FR-008) وسجِّلْه في `specs/038-session-log-retention/quickstart.md` § ١٣ — ⛔ **والأمرُ `git diff --name-only main... -- frontend/`**: `git diff --stat frontend/` يقرأُ غيرَ المُدرَجِ وحدَه فهو فارغٌ على أيِّ فرعٍ التُزِمَت تغييراتُه. **و**الحمولةُ `frontend/src/lib/sessions.ts:24` تحملُ `device: { uuid, label }` والشاشةُ `settings/security/page.tsx:106` تقرأُ `session.device.label` **ولا تبني على `device.uuid`** زرّاً ولا تجميعاً

**Checkpoint**: US1 تعملُ وحدَها — الجدولُ نظيفُ المحتوى وحجمُه لم ينقُصْ صفّاً واحداً.

---

## Phase 4: User Story 2 — الجدولُ يتوقّفُ عن النموِّ بلا سقف (P1) 🎯 نصفُ الـMVP

**Independent Test**: حسابٌ بجلساتٍ منتهيةٍ **مُجهَّلةٍ سلفاً وعبرَتِ الأرضيّة** تفوقُ السقف، ومعها حديثاتٌ ونشطاتٌ تفوقُ السقفَ وحدَها، تُشغَّلُ المهمّة، ثمّ ثلاثُ عدّات.

- [X] T024 [US2] اكتبْ `backend/config/auth_sessions.php` بالافتراضَين — السقفُ **٥٠** والأرضيّةُ **١٨٠ يوماً**. ⚠️ **وكلُّ مسارٍ يُرجِعُ قيمةً غيرَ فارغة**: `PlatformSettingsTest` يلفُّ على الخريطةِ كلِّها ويشترطُ `config($path) !== null`، و`PlatformSettingsSeeder` يكتبُ القيمةَ في عمودٍ `NOT NULL`. ⚠️ **ولا `[P]`**: T025 تُحمِّرُ هذَينِ الحارسَينِ بدونَه
- [X] T025 [US2] أضِفِ المفتاحَينِ إلى `KEYS` في `backend/app/Modules/Tenancy/Support/PlatformSettings.php` — `'auth.auth_session_cap_per_user' => 'auth_sessions.cap_per_user'` و`'auth.auth_session_cap_min_age_days' => 'auth_sessions.cap_min_age_days'`، بجوارِ `auth.device_limits`. ⛔ **بدونِهما هما غائبانِ عن `all()` وعن `flush()`** — كلتاهما تلفُّ على `array_keys(self::KEYS)` — فلا تراهما اللوحةُ ولا يُبطِلُهما مسحُ الذاكرة، **والقراءةُ `rememberForever`**. ⚠️ **والبادئةُ `auth.` لا `identity.`**
- [X] T026 [US2] اكتبْ `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php` على قالبِ `TransferDataOwnershipJob.php` — ⛔ **والاستعلامُ الأوّلُ يختارُ `user_id` و`count(*)` وحدَهما**: `->select('user_id')->selectRaw('count(*) as total')->groupBy('user_id')->having('total','>',$cap)`. و`->get()` بعدَ `groupBy` بلا `select` صريحٍ هو `select *` أي **MySQL ERROR 1055** تحتَ `ONLY_FULL_GROUP_BY` الذي يضعُه `MySqlConnector:148` بلا شرطٍ لمّا `'strict' => true` — **وSQLite تُرجِعُ صفّاً فتمرُّ خضراءَ إلى الأبد**، والمهمّةُ ترمي في أوّلِ جملةٍ كلَّ ليلةٍ والجدولُ ينمو. السابقةُ الصحيحةُ `DeviceRegistry.php:76`. ⛔ **ثمّ لكلِّ مستخدِمٍ `pluck('id')` ثمّ `whereIn`، ولا استعلامٌ فرعيٌّ على الجدولِ نفسِه**: `DELETE … WHERE id NOT IN (SELECT … FROM auth_sessions …)` هو **ERROR 1093**، وSQLite تُعيدُ كتابتَه فيمرُّ أخضر. ⛔ **وتُقطَّعُ القائمةُ قبلَ `whereIn`** (`array_chunk($ids, 1000)`): الحسابُ الواحدُ قد يحملُ عشراتِ الآلافِ فوقَ الأرضيّةِ — وهو **سببُ الميزةِ لا حالتُها الشاذّة** — و`whereIn` بقائمةٍ كاملةٍ هي **ERROR 1390** على MySQL (٦٥٥٣٥ متغيّراً، والتحضيرُ خادميٌّ) و**٣٢٧٦٦** على SQLite. ⛔ **والمُرشَّحُ `status='ended' AND ip_hash IS NULL AND ended_at < :floor`** — ما جُهِّلَ سلفاً وعبرَ الأرضيّة: سقفٌ بالعددِ وحدَه يُبلَغُ من لوحةِ مفاتيحٍ حيّةٍ في عشرِ دقائقَ تحتَ `throttle:auth` (خمسٌ بالدقيقةِ للعنوانِ وللبريدِ معاً). ⛔ **`ORDER BY ended_at DESC, id DESC`** لا `created_at`: الثانيةُ دقّةُ الأخيرِ فالتعادُلُ يُغيِّرُ الباقيَ بينَ تشغيلَينِ تُقارِنُهما SC-006، و`id` مفتاحٌ أساسيٌّ فالترتيبُ تامّ. ⚠️ **و`auth_sessions_eviction_index` لا يخدمُ الترتيبَ لأنّ `ended_at` ليس عموداً فيه أصلاً** — فلا ترتيبٌ لأعمدتِه يُصلِحُه؛ ويخدمُ البادئةَ `(user_id, status)` فالاستعلامُ بحثٌ مجالِيٌّ ثمّ `filesort` لكلِّ مستخدِم. ⚠️ **وسقفٌ ≤ صفرٍ يُقرَأُ «لا سقف»** ويرجِعُ بلا حذف. ⚠️ **ولا حذفَ لصفِّ جهازٍ البتّة** (FR-006). ⚠️ **و`playback_grants.auth_session_id` يُشيرُ إلى الصفِّ المحذوفِ بلا مفتاحٍ أجنبيّ** و`PlaybackGuard:39` يقرأُ `->session->status`: آمنٌ عندَ أرضيّةِ ١٨٠ وحدَها لأنّ المنحَ تُكنَسُ بعدَ انتهائِها بأسبوع، **وخفضُ الأرضيّةِ تحتَ ذلك يفتحُ خطأَ ٥٠٠**
- [X] T027 [US2] اسألِ `LegalHoldDirectory` في `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php` **على رأسِ كلِّ صفحةِ اكتشافٍ وقبلَ حذفِ كلِّ مستخدِم** — ⛔ **والفرقُ بينَ الموضعَينِ يُكتَبُ لأنّه معكوس**: الإعفاءُ في `WHERE` استعلامِ الاكتشافِ **لازم** (عددُه المُرجَعُ يُنهي الحلقة، فتخطٍّ بعدَ السحبِ هناك يُنهي الليلةَ لكلِّ من خلفَه — لا الليلةَ ولا أيَّ ليلة)؛ أمّا خطوةُ المستخدِمِ الواحدِ فتُرجِعُ صفوفَه هو لا عدَّ صفحة، **فـ`continue` عليها صحيحٌ ولازم** لحظرٍ وُضِعَ بينَ الاكتشافِ والحذف — والحذفُ لا يُعكَس. ⛔ **وقائمةٌ فارغةٌ تعني «لا حظر» لا «لا تحذِفْ شيئاً»، وإخفاقُ النداءِ يُوقِفُ الصفحةَ بلا حذف** — اتّجاهُ الخطأِ في الثانيةِ آمنٌ وفي الأولى لا يُعكَس
- [X] T028 [US2] أضِفْ إلى `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php`: `WithoutOverlapping` **بمفتاحٍ خاصٍّ بها** مع `expireAfter()` على قيمةِ `RunRetentionSweepJob` نفسِها (`$timeout + 60`) · **سقفَ دفعاتٍ خاصّاً بها** لا ثابتَ المكنسة (ذاك **لكلِّ فئة** وهذا يمشي على المستخدِمين) · `try/catch` **لكلِّ مستخدِم** — ⚠️ **ونداءُ الدليلِ خارجَه** فالإخفاقُ يتصاعدُ (T027) · وسطراً بما فعلَته. ⛔ **`expireAfter()` هو النصفُ الحامل**: قفلٌ لا ينتهي يعني أنّ السقفَ لا يعملُ بعدَ أوّلِ عاملٍ يُقتَل — ⚠️ **و`withoutOverlapping()` على `Schedule::job()` يحرسُ الدفعَ لا التشغيل**. ⛔ **والسجلُّ يحملُ صنفَ الاستثناءِ لا `getMessage()`**: `QueryException` يدسُّ الروابطَ في نصِّه، وروابطُ هذه المهمّةِ معرِّفاتُ مستخدِمين — والسطرُ يخرجُ إلى مزوِّدِ المراقبة (FR-041 · `LogHygieneTest`)
- [X] T029 [US2] جدوِلِ المهمّةَ في `backend/routes/console.php` — `Schedule::job(new EnforceAuthSessionCapJob, 'compliance')->dailyAt('04:15')` بجوارِ `TransferDataOwnershipJob` (`:407`). ⛔ **بعدَ مكنسةِ ٣:٣٠ لا قبلَها**: التجهيلُ يُنتِجُ مُرشَّحَ السقف. ⚠️ **وتُقاسُ بحالةٍ تقرأُ أحداثَ `Schedule` المسجَّلةَ لا بالعينِ في `schedule:list`** — هذا المستودَعُ شحنَ مكنسةً غيرَ مجدولةٍ مرّتَين (`ReconcileAssetStatus` · طابورُ `maintenance`)
- [X] T030 [US2] اكتبْ `backend/tests/Feature/Identity/AuthSessionCapTest.php` — ⚠️ **ملفٌّ مستقلٌّ عن `AuthSessionRetentionTest` عن قصد**: المكنسةُ والمهمّةُ بابانِ يُشغَّلانِ مستقلَّين. الحالاتُ: الثلاثُ عدّاتٍ (`quickstart.md` § ٧) · **حسابٌ بستّينَ منتهيةً بعمرِ ٤٠٠ يومٍ وكلُّها ما زالت تحملُ `ip_hash` ⇒ صفرُ حذف** — ⛔ **بدونَها يمرُّ حذفُ `ip_hash IS NULL` من المُرشَّحِ خضراءَ** لأنّ الأرضيّةَ وحدَها تحمي الحديثات، وهو عينُ «يُحذَفُ صفٌّ ما زالَ يحملُ عنواناً» · **سقفٌ = صفرٍ ⇒ صفرُ حذف** (تحلُّ محلَّ «حسابٌ بجلسةٍ واحدة»، وهي خضراءُ عندَ أيِّ سقفٍ ≥ ١، والبرهانُ الوحيدُ على الفرعِ الذي يُفرِّغُ سجلَّ الدخول) · **جلستانِ تتشاركانِ `ended_at` على حدِّ السقفِ ⇒ مجموعةُ المعرِّفاتِ الباقيةِ بعدَ التشغيلتَينِ واحدة** (عدُّ الصفوفِ أخضرُ على ترتيبٍ يتغيّر) · مُجهَّلةٌ داخلَ الأرضيّةِ لا تُحذَف · **حسابانِ فوقَ السقفِ في تشغيلةٍ واحدة، أحدُهما محظورٌ والآخَرُ بـ`user_id` أكبرُ فيُعالَجُ بعدَه** ⇒ المحظورُ لم يتغيّرْ والآخَرُ نزل — ⚠️ **وهي وحدَها ما يقيسُ SC-002 على مستوى المنصّة** ويعضُّ إعفاءً بعدَ السحب
- [X] T031 [US2] أكِّدْ في `backend/tests/Feature/Identity/AuthSessionCapTest.php` — (أ) SC-003: عددُ `status='active'` على المنصّةِ كلِّها قبلَ = بعدَ، ⛔ **بحسابَينِ على الأقلّ وأحدُهما ليس الحسابَ الذي تُعالِجُه المهمّة** (عدُّ حسابٍ واحدٍ أخضرُ على بناءٍ يطردُ غيرَه)، **ويُكرَّرُ حولَ المكنسةِ في `AuthSessionRetentionTest`** لأنّ SC-003 ادّعاءٌ عن البابَين. (ب) SC-004: مُهيَّأٌ يمتدُّ سبعةَ أشهرٍ ⇒ عددُ الأشهرِ المتمايزةِ في `ended_at` **لكلِّ الصفوفِ الباقيةِ** (المُجهَّلةُ والحديثةُ والنشطةُ معاً) **≥ ٣** — ⚠️ **والمعيارُ محفوظٌ بالأرضيّةِ لا بالسقف**، فإن هبطَت إلى مستوى المدّةِ سقط

**Checkpoint**: US1 + US2 = المنتَجُ الأدنى.

---

## Phase 5: User Story 3 — المشغِّلُ يضبطُ الأرقامَ الثلاثةَ من شاشةٍ واحدة (P2)

- [X] T032 [US3] أضِفِ الحقلَينِ الرقميَّينِ (السقفُ والأرضيّة) إلى `backend/app/Modules/Tenancy/Filament/Pages/ManagePlatformSettings.php` في قسمِ «الأجهزة والحسابات» القائم — ⚠️ **قسمٌ واحدٌ لا قسمان**: الأرقامُ تُقرَأُ وتُحرَّرُ معاً، وتعليقُ `billing.transfer` في `PlatformSettings` يكتبُ هذه القاعدةَ بنصِّها
- [X] T033 [US3] أضِفْ حقلَ مدّةِ الاحتفاظِ إلى `backend/app/Modules/Tenancy/Filament/Pages/ManagePlatformSettings.php` **مُمرَّراً على `Compliance\Actions\SaveDataCategory` للصفَّينِ كليهما**، **وأرضيّةً دنيا ثمانيةَ أيّامٍ في حارسِ تلك الـAction** — ⛔ **الأرضيّةُ قرارُ مالكٍ ٢٠٢٦-٠٩-٢٢ وسببُها مقيس**: `playback_grants.issued_ip_hash` نفسُ قيمةِ عنوانِ الجلسةِ ومفهرسٌ على `auth_session_id` وبلا صفِّ كتالوجٍ ولا كنسة، و`PruneExpiredGrantsJob` يحذِفُ بعدَ `expires_at + 7d` — فمدّةٌ دونَ الثمانيةِ تضعُ الجلسةَ المُجهَّلةَ والمنحةَ الحيّةَ في نافذةٍ واحدةٍ فيُستردُّ العنوانُ بـ`JOIN`، و`minRetainDays()` افتراضُها **١** اليوم. ⚠️ **وهذا أوّلُ استدعاءِ Action عبرَ حدِّ وحدةٍ من لوحةِ Filament في الشجرة (مقيس: ٢٥ استدعاءً كلُّها من وحدتِها)، فيُسمّى قراراً لا سهواً**: الملفُّ يستوردُ اليومَ `Payments\Support\BillingSettings` و`LiveSessions\Enums\ClassSessionType` — قراءاتٍ عابرةً للحدّ — والبديلُ كتابةُ العمودِ خامّاً، وحارسُ `SaveDataCategory:57` يرفضُ الصفرَ بتعليقٍ يُسمّيه «الغلطةُ التي تمحو المنصّةَ في ليلة». ⛔ **والمدّةُ تبقى مخزَّنةً في `data_categories.retain_days`**: `DataCategoryResource` يُرسِلُها ومعها `retention_label_ar`، وصفٌّ بلا مدّةٍ يُطبَعُ «يُحفظ ما دام الحساب قائماً» — **إشعارُ خصوصيّةٍ يكذِب**
- [X] T034 [US3] اكتبْ حالاتِ الشاشةِ في `backend/tests/Feature/Tenancy/` بجوارِ اختباراتِ الصفحةِ القائمة — **الأرقامُ الثلاثةُ لا اثنان**: رفعُ السقفِ **إلى ما دونَ عددِ صفوفِ المُهيَّأ** ⇒ يبقى العددُ الجديد (سقفٌ فوقَ العددِ لا يُفرَّقُ عن مهمّةٍ لا تحذِف) · **رفعُ الأرضيّةِ من ١٨٠ إلى ٣٦٥ ⇒ جلساتٌ بعمرِ ٢٠٠ يومٍ كانت تُحذَفُ صارت باقية** (بدونَها يمرُّ حقلٌ يكتبُ مفتاحاً خاطئاً أو مهمّةٌ تُثبِّتُ ١٨٠) · **تقصيرُ المدّةِ إلى ٣٠ ⇒ جلسةٌ بعمرِ ٦٠ تُجهَّلُ وأخرى بعمرِ ٢٠ لا تُجهَّلُ في التشغيلةِ نفسِها** (بلا الضابطِ الثاني تمرُّ ذراعٌ تُجهِّلُ كلَّ شيء) · ⛔ **وحالةُ الرفض**: `retain_days = 0` وأخرى `= 3` ⇒ خطأُ نموذجٍ **والعمودُ لم يتغيّرْ** — وهذا **وحدَه** ما يُفرِّقُ المرورَ على `SaveDataCategory` عن `update()` خامّ · ⛔ **وSC-007 تُقاسُ بالقيمةِ لا بغيابِ الخطأ**: بلا صفٍّ واحدٍ في `platform_settings` ستّونَ مُجهَّلةً عبرَتِ الأرضيّةَ ⇒ يبقى **٥٠ بالضبط** — ⚠️ **والحزمةُ كلُّها تعملُ بلا صفوفِ إعداداتٍ أصلاً** (`TestCatalogueSeeder` لا يُنادي `PlatformSettingsSeeder`)، فتأكيدُ «بلا خطأ» وحدَه مكرّرٌ لكلِّ حالةٍ في الطور · ⚠️ **وFR-011**: لقطةٌ لـ`retain_days` بمفتاحِ الفئةِ قبلَ الحفظِ وبعدَه ⇒ **صفّانِ فقط تغيّرا**

---

## Phase 6: Polish & Cross-Cutting

- [X] T035 اكتبْ حارسَ المصدرِ في `backend/tests/Feature/Identity/SqliteGreenFormsTest.php` — يقرأُ `EnforceAuthSessionCapJob.php` و`AuthSessionRetention.php` و`IdentityPersonalData.php`، **ويُجرِّدُ التعليقاتِ أوّلاً** (القاعدةُ مكتوبةٌ بجوارِ الشيفرةِ، ومسحٌ خامٌّ يسقُطُ على شرحِه — دَينُ `TrustScoreJobIsolationTest`)، ويرفضُ: `NOT IN (SELECT` يُسمّي الجدولَ نفسَه · `CONCAT(` · `||` داخلَ نصِّ `DB::`/`Raw` · و`groupBy` بلا `select`/`selectRaw` صريحٍ قبلَه. ⛔ **وهذا هو الأداةُ الوحيدةُ التي ترى الأربعة**: كلُّها خضراءُ على SQLite بنصِّ المهامِّ نفسِها، فلا تشغيلٌ يبلغُها
- [X] T036 امشِ على `specs/038-session-log-retention/quickstart.md` من § ٠ إلى § ١٤ على قاعدةِ التطويرِ وسجِّلِ الأرقامَ فيه — ⚠️ **وأقدمُ صفٍّ في التطويرِ عمرُه ٤٧ يوماً وفي الإنتاجِ ١٩**، فكلُّ قياسٍ على قاعدةٍ حقيقيّةٍ اليومَ يُخرِجُ أصفاراً **وهي نفسُها ما يُخرِجُه بناءٌ لا ذراعَ فيه**: المُهيَّأُ المُشيَّخُ هو الفارقُ الوحيد
- [X] T037 [P] شغِّلْ `./vendor/bin/pint` و`./vendor/bin/phpstan analyse` من `backend/` — ⚠️ **والمحلّلُ يقرأُ `Database/Migrations` لكلِّ وحدةٍ ليُنمِّطَ الخصائص**، والشجرةُ نظيفةٌ على المستوى ٨ وتبقى كذلك
- [X] T038 [P] أضِفْ بنداً إلى `CLAUDE.md` تحتَ المزالق — **أربعةُ أشياءَ في سطر**: `->groupBy()->get()` هو `select *` ⇒ **ERROR 1055** تحتَ `ONLY_FULL_GROUP_BY` الذي يفرضُه `'strict' => true` · `||` رابطُ نصوصٍ على SQLite وعاملُ «أو» على MySQL · `DELETE … NOT IN (SELECT … FROM نفسِ الجدول)` هو **ERROR 1093** · وفئةُ كتالوجٍ بـ`erasure_mode` مخالفٍ **تُطفِئُ المحوَ في وحدتِها كلِّها بلا خطأٍ ظاهر**. ⚠️ الأربعةُ **خضراءُ على الحزمةِ كلِّها**، وهذا وحدَه سببُ كتابتِها
- [X] T039 ادفعِ الفرعَ ودعِ الشرائحَ الأربعَ في `.github/workflows/ci.yml` تُشغِّلُ الحزمة — ⛔ **ولا تُشغَّلُ حزمتانِ محلّيّاً معاً**: تتقاسمانِ قرصَ الاختبارِ وتصنعانِ إخفاقاً كاذباً. ⚠️ **وبعدَ T038 لا قبلَها** فالبندُ يُدفَعُ مع الفرع
- [ ] T040 بعدَ النشر، أعِدْ على الإنتاجِ `specs/038-session-log-retention/quickstart.md` § ١ و§ ٦ بعدَ أوّلِ ليلةِ مكنسة — ⛔ **وهذه الخطوةُ لا بديلَ لها**: عطبُ القرارِ ٤ (اصطدامُ `unique(user_id, fingerprint_hash)`) وعطبُ القرارِ ٥ (ERROR 1093) وعطبُ ١٠٥٥ **لا يظهرُ أيٌّ منها إلّا على MySQL**، وCI كلُّها SQLite. المقياسان: البصماتُ المكرّرةُ = صفر · الجلساتُ المُجهَّلةُ المُشيرةُ إلى بصمةٍ حقيقيّةٍ = صفر

---

## Dependencies & Execution Order

### اعتمادُ الأطوار

- **Phase 1**: بلا اعتماد — ويسبقُ كلَّ اختبار
- **Phase 2**: بعدَ Setup — **يُعيقُ كلَّ القصص**؛ وT008–T010 **التزامةٌ واحدة**
- **Phase 3 (US1)**: بعدَ Foundational
- **Phase 4 (US2)**: بعدَ Foundational · **ويعتمدُ على US1 سلوكيّاً**، ويبقى مقيساً وحدَه بمُهيَّأٍ يُفرِّغُ `ip_hash` باستعلام
- **Phase 5 (US3)**: بعدَ US1 وUS2
- **Phase 6**: بعدَ الثلاث — وT040 **بعدَ النشر** لا قبلَه

### داخلَ الملفِّ الواحد — لا `[P]` البتّة

| الملفّ | المهامّ |
|---|---|
| `IdentityPersonalData.php` | T010 · T013 · T016 · T017 · T018 |
| `AuthSessionFactory.php` | T001 · T002 |
| `EnforceAuthSessionCapJob.php` | T026 · T027 · T028 |
| `ManagePlatformSettings.php` | T032 · T033 |
| `AuthSessionCapTest.php` | T030 · T031 |
| `AuthSessionRetentionTest.php` | T020 · T021 |
| `quickstart.md` | T023 · T036 (طورانِ مختلفانِ فلا تصادم) |

### فرصُ التوازي

- **Phase 1**: T004 مع T001–T003 (ملفٌّ مختلف)
- **Phase 2**: T005 **ثمّ** T006 (اعتمادٌ لا توازٍ) · T011 مع T012
- **Phase 3**: T019 · T023 مع T020–T022
- **Phase 4**: T024 **قبلَ** T025 — اعتمادٌ صريح، ولا `[P]` على أيٍّ منهما
- **Phase 6**: T037 · T038 معاً

---

## Implementation Strategy

### المنتَجُ الأدنى = **US1 + US2 معاً**

1. Phase 1 → Phase 2 → Phase 3 → **Phase 4**
2. قِفْ وقِسْ: `quickstart.md` § ٣–٧ على قاعدةٍ مُشيَّخة
3. ⛔ **ولا تُشحَنُ US1 وحدَها**: التجهيلُ **لا يُنقِصُ صفّاً واحداً**. ⛔ **ولا US2 وحدَها**: مُرشَّحُها `ip_hash IS NULL`، فبلا ذراعِ التجهيلِ لا مُرشَّحَ أصلاً — أو أسوأُ، يُحذَفُ صفٌّ ما زالَ يحملُ عنواناً
4. US3 بعدَها — الافتراضاتُ المشحونةُ تجعلُ الميزةَ صحيحةً قبلَ أن يفتحَ المشغِّلُ اللوحة

### الأثرُ على الإنتاجِ في أوّلِ ليلة

**أقربُ إلى صفرٍ منه إلى شيء، ويُقالُ قبلَ الشحنِ لا بعدَه**: أقدمُ صفٍّ في الإنتاجِ عمرُه ١٩ يوماً والمدّةُ ٩٠، فذراعُ العمرِ **لا تبلغُ صفّاً واحداً**؛ والأرضيّةُ ١٨٠ فالسقفُ لا يبلغُ شيئاً كذلك. **الميزةُ تُغلِقُ ثغرةً قبلَ أن تعضّ، ولا تُقاسُ بما تحذِفُه اليومَ بل بما يستحيلُ معها أن ينمو.**

---

## Notes

- `[P]` = ملفّاتٌ مختلفةٌ **بلا اعتماد** — والاعتمادُ يُسقِطُ العلامةَ ولو اختلفَ الملفّ (T006 · T025)
- **صفرُ تغييرٍ تحتَ `frontend/`** — مقيسٌ بـ`git diff --name-only main... -- frontend/` (T023) لا مفترَض
- **ولا نقطةَ نهايةٍ جديدةٌ ولا صلاحيّةٌ جديدة**: السطحُ العامُّ الوحيدُ الذي تزيدُه الميزةُ هو `LegalHoldDirectory`
- **وأربعةُ أشكالٍ تمرُّ خضراءَ على SQLite وتنفجرُ على MySQL وحدَها** (T012 · T026 ×٣). مَن يقرأُ سطرَ المهمّةِ ويكتبُ الشكلَ البديهيَّ يُسقِطُ الإنتاجَ — وT035 وT040 هما ما يراهما
- **وثلاثةُ مخازنِ عناوينَ خارجَ النطاقِ ومكتوبةٌ في المواصفة** (T003): `activity_log` · `terms_consents` · `playback_grants`. فـSC-001 ادّعاءٌ عن **الصفِّ وصفِّ جهازِه**، لا عن قاعدةِ البياناتِ كلِّها
