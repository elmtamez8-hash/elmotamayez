# Tasks: ٠٣٨ — سجلُّ الجلساتِ والأجهزةِ يُجهَّلُ بالعمر، ويُسقَفُ بالعدد

**Input**: `specs/038-session-log-retention/` — spec.md · plan.md (**النسخةُ الثانية**) · research.md (ر-١…ر-١٢) · data-model.md · contracts/legal-hold-directory.md · quickstart.md (٠…١٤) · review-findings.md

**Tests**: **مطلوبةٌ صراحةً**. و`quickstart.md` § ٠ يُقرَأُ قبلَ كتابةِ حالةٍ واحدة — فيه ثلاثةُ ألغامٍ في المُهيَّأِ تُبطِلُ كلَّ تأكيدٍ فوقَها.

**Organization**: بالقصص، وقبلَها طوران. **US1 وUS2 معاً هما المنتَجُ الأدنى** بنصِّ المواصفة: التجهيلُ وحدَه **لا يُنقِصُ صفّاً واحداً**، والسقفُ وحدَه يحذِفُ صفوفاً ما زالت تحملُ عناوين.

---

## ⛔ الخطّةُ التي تُفكَّكُ هنا هي **النسخةُ الثانية**

مراجعةُ ستِّ وكلاءَ أسقطَت من النسخةِ الأولى **سبعةَ ادّعاءاتٍ عن الشجرةِ وقراراً معماريّاً كاملاً**، وكلُّه في [review-findings.md](./review-findings.md) بأسماءِ الملفّاتِ وأرقامِ الأسطر. فلا سطرَ في هذا الملفِّ منقولٌ من نثرِ الخطّة: كلُّ موضعٍ فُتِحَ وقُرِئَ على الشجرةِ في ٢٠٢٦-٠٩-٢٢.

### ما قِيسَ أثناءَ كتابةِ هذه المهامّ — وما صحّحَه في الخطّةِ نفسِها

| الموضع | ما قِيس | الأثرُ على المهامّ |
|---|---|---|
| `app/Modules/Identity/Support/IdentityPersonalData.php:260` | `expire()` جسدُها `return 0;` تحتَ تعليقٍ يقول «لا شيءَ في هذه الوحدةِ ينتهي بساعة» | التعليقُ **يُعادُ كتابتُه**، لا يُضافُ تحتَه (T015) |
| `app/Modules/Identity/Models/Device.php:33` | `$fillable` = `user_id · fingerprint_hash · label · last_seen_at` | `firstOrCreate` المقبرةِ يكتبُ `label` فعلاً — لا صمتَ مثلَ ٠١٣ (T009) |
| `app/Modules/Tenancy/Support/PlatformSettings.php:88` | `'auth.device_limits' => 'media.device_limits'` — البادئةُ `auth.` قائمةٌ ومسارُها في `config/media.php` | المفتاحانِ الجديدانِ `auth.` ومسارُهما ملفُّ إعدادٍ **جديد** (T022 · T023) |
| `app/Modules/Compliance/Support/ExportFieldAllowlist.php:52` | `ip_address`/`ip_hash`/`user_agent` **غائبةٌ عن المنعِ عن قصد**، بتعليقٍ يقولُ لماذا | `ip_hash` **يُصدَّر**، و`fingerprint_hash` وحدَه يُمنَع (T011) |
| `frontend/src/lib/sessions.ts:24` · `settings/security/page.tsx:106` | الحمولةُ `device: { uuid, label }`، والشاشةُ تقرأُ `session.device.label` **ولا تبني على `device.uuid`** أيَّ زرّ | صفرُ تغييرٍ في الواجهة — ويُقاسُ لا يُدَّعى (T021) |
| `app/Modules/*/Filament/` كلُّها | **صفرُ استدعاءٍ لـAction من وحدةٍ أخرى**. `CreatePlan.php:10` ينادي Action **من وحدتِه** | `ManagePlatformSettings` ⟵ `Compliance\Actions\SaveDataCategory` **أوّلُ عبورٍ من نوعِه، ويُسمّى قراراً** (T031) |
| `tests/Feature/Compliance/ErasureCompletenessTest.php` | **لا يلفُّ على `describe()`** — يبني مُهيَّأً بالاسمِ كـ`ExportCompletenessTest` | الحارسانِ اللذانِ يعضّانِ يبقيانِ `PersonalDataContractCoverageTest` و`CategoryRegistryCoverageTest` |
| `app/Modules/Identity/Support/IdentityPersonalData.php:280` | `alreadyAnonymised()` = `str_starts_with($user->email, 'anonymised+')` | الذراعانِ **فوقَه** (T012)، ولا يُضعَّفُ هو |

### ⛔ تناقضٌ في نصِّ المواصفةِ اكتشفَه تفكيكُ المهامّ — ويُصحَّحُ أوّلاً

**US1 · السيناريو ٢** و**FR-008** يقولانِ إنّ الصفَّ المُجهَّلَ «يُقرَأُ كما كانَ قبلَ التجهيلِ **تماماً**: اسمُ الجهازِ المقروءُ وبابُ الدخولِ والتاريخان». **وFR-013 تنقُضُ نصفَ ذلك**: إعادةُ التوجيهِ إلى المقبرةِ تُغيِّرُ `device.label` إلى «جهازٌ لم تعدْ تفاصيلُه محفوظة».

**والتصميمُ هو الصحيحُ لا النصّ**: تلك الجملةُ **هي بعينِها** جملةُ FR-008 الصريحةُ التي تقولُ إنّ التفاصيلَ لم تعدْ محفوظة — تصلُ الشاشةَ عبرَ حقلٍ قائمٍ بصفرِ تغييرٍ في الواجهة. فالمواصفةُ تُرقَّعُ (T003) ويُقاسُ الشكلُ الحقيقيُّ لا «مطابقٌ تماماً» (T018).

---

## Phase 1: Setup — المُهيَّأُ قبلَ كلِّ قياس

**الغرض**: كلُّ تأكيدٍ يُكتَبُ فوقَ مُهيَّأٍ معطوبٍ أخضرُ على بناءٍ لا ذراعَ فيه. **هذا الطورُ قبلَ الاختبارِ الأوّل، لا في التلميع.**

- [ ] T001 اكتبْ `ip_hash` في `backend/database/factories/Modules/Identity/AuthSessionFactory.php` — ⛔ **المصنعُ لا يكتبُه إطلاقاً اليوم، و`ip_hash IS NULL` هي علامةُ «جُهِّلَ سلفاً»**: فكلُّ صفٍّ مبنيٍّ بالمصنعِ **مولودٌ مُجهَّلاً**، وكلُّ حالةِ تجهيلٍ فوقَه تمرُّ خضراءَ على بناءٍ لا ذراعَ فيه. قيمةٌ بستّةٍ وستّينَ محرفاً كـ`hash('sha256', ...)` لا نصٌّ قصير
- [ ] T002 اشتقَّ `device_id` من `user_id` في `backend/database/factories/Modules/Identity/AuthSessionFactory.php` — ⛔ **العمودانِ مصنعانِ مستقلّانِ اليوم**، وهو شكلُ `ChapterFactory` الذي دفعَ المستودَعُ ثمنَه: جلسةُ شخصٍ وجهازُ آخر، فاختبارُ الحظرِ القضائيِّ يُعفي صاحبَ الجلسةِ ويُجهِّلُ جهازَ غيرِه **ويمرُّ أخضر**. ⚠️ **والمفتاحُ المُشتَقُّ يُسمّى بعدَ والدِه** في مصفوفةِ `definition()`: `expandAttributes()` يمشي بالترتيبِ ولا يُعطي المُغلَقةَ إلّا ما حَلَّ قبلَها
- [ ] T003 [P] رقِّعْ `specs/038-session-log-retention/spec.md` — US1 السيناريو ٢ وتذييلُ FR-008: «يُقرَأُ كما كانَ **إلّا اسمَ الجهاز**، فهو يصيرُ جملةَ FR-008 الصريحة». ⛔ **ويُكتَبُ التصحيحُ ولا يُحذَفُ الأصل** كما فُعِلَ بسبعةِ ادّعاءاتِ النسخةِ الأولى — القارئُ يرى لماذا صارَ هكذا

---

## Phase 2: Foundational — العقدُ والكتالوجُ والمحو

**الغرض**: إعلانُ الفئتَينِ يجرُّ معه التصديرَ والمحوَ (FR-012)، والثلاثةُ تُشحَنُ في طورٍ واحدٍ أو يُحمِّرُ `PersonalDataContractCoverageTest` بينَها.

**⚠️ CRITICAL**: لا قصّةَ تبدأُ قبلَ اكتمالِ هذا الطور.

- [ ] T004 [P] اكتبْ `backend/app/Shared/Contracts/LegalHoldDirectory.php` بالتوقيعِ الحرفيِّ في [contracts/legal-hold-directory.md](./contracts/legal-hold-directory.md): `heldUserIds(): array` وحدَها، **أعدادٌ خروجاً ولا نوعَ من `Compliance` في التوقيع** — قاعدةُ `OutstandingCreditsDirectory` و`SettlementClearance` نفسُها. ⚠️ **ودليلٌ لا قدرة**: ١٦ من ٢٥ ملفّاً في `Shared/Contracts/` هي `*Directory` و**صفرٌ منها واجهةُ قدرةٍ بصيغةِ فعل**
- [ ] T005 [P] اكتبْ `backend/app/Modules/Compliance/Support/EloquentLegalHoldDirectory.php` — سطرٌ واحدٌ فوقَ `LegalHold::query()->inForce()->pluck('subject_user_id')`، بجوارِ `EloquentTeacherOffboardingDirectory.php` في المجلّدِ نفسِه
- [ ] T006 اربِطِ العقدَ في `backend/app/Modules/Compliance/ComplianceServiceProvider.php` — والوحدةُ التي تُعلِنُ الحظرَ هي التي تُنفِّذُ الدليل، فالهُويّةُ لا تستوردُ `LegalHold` (المبدأُ الثالث)
- [ ] T007 أضِفِ الصفَّينِ `auth_session` و`device` إلى `backend/database/seeders/DataCategorySeeder.php::categories()` — `table_name`/`column_name` = `auth_sessions`/`ip_hash` و`devices`/`fingerprint_hash` · `owning_module` = `identity` · `subject_roles` بالثلاثةِ جميعاً · `retain_days` = **٩٠** · `expiry_behaviour` = `Anonymise`. ⛔ **و`erasure_mode` = `Anonymise` ليس خياراً**: `ExecuteDataErasure::modeFor()` يجمعُ أوضاعَ فئاتِ الوحدةِ ويشترطُ أن تكونَ **واحدة**، فقيمةٌ مخالفةٌ تُرجِعُ `Retain` **وتُطفِئُ المحوَ في `Identity` كلِّها بلا خطأٍ ظاهر** — والخمسُ القائمةُ `Anonymise` (مقيس). ⛔ **و`retain_days` عددٌ لا `null`**: `expires()` تشترطُ العمودَينِ معاً، وصفٌّ بمدّةٍ فارغةٍ تتخطّاه المكنسةُ كلِّيّاً **ويُطبَعُ على «خصوصيّتي» «يُحفظ ما دام الحساب قائماً»** — إشعارٌ كاذب. ⚠️ و`label`/`purpose` عمودانِ قابلانِ للترجمةِ (٠٥٥) فيُكتَبانِ بذلك الشكل
- [ ] T008 اكتبْ ترحيلَ الإملاءِ الخلفيِّ للصفَّينِ تحتَ `backend/database/migrations/` — ⛔ **يُنادي `run()` لا `seedMissing()`**: الأخيرةُ **غيرُ موجودةٍ على هذا الباذرِ أصلاً** و`run()` هو `firstOrCreate` على المفتاحِ وحدَه، فلا يمسُّ صفّاً قائماً. والسابقةُ `2026_09_16_000200_backfill_guardian_link_data_category.php` في الوحدةِ نفسِها تكتبُ السببَ بنصِّه. ⚠️ **واسمُ الملفِّ وحدَه لا يكفي**: ترحيلٌ يُخفِقُ يأخذُ معه كلَّ ما خلفَه في الطابور — كما حدثَ لصفِّ الكتالوجِ وقالبِ التنبيهِ في نشرِ `course_waitlist_entries`
- [ ] T009 اكتبْ `backend/app/Modules/Identity/Support/AuthSessionRetention.php` — بدائيّاتُ التجهيلِ الثلاث: صفُّ المقبرةِ `firstOrCreate(['user_id' => $u, 'fingerprint_hash' => 'anonymised:u'.$u], ['label' => 'جهازٌ لم تعدْ تفاصيلُه محفوظة'])` · تجهيلُ صفوفِ الجلساتِ · تجهيلُ صفوفِ الأجهزة. ⛔ **والقيمةُ تُبنى في PHP** على سابقةِ `Anonymiser::email()`: **لا `||`** — عاملُ «أو» منطقيٍّ على MySQL بلا `PIPES_AS_CONCAT` (و`config/database.php:60` فيه `'strict' => true` بلا `modes`)، فتُكتَبُ `'1'` في كلِّ صفٍّ **فتصطدمُ بـ`unique(user_id, fingerprint_hash)`** عندَ ثاني جهازٍ وتُرجِعُ معاملةَ المحوِ كلَّها، **ولا تتقارَبُ** لأنّ `'1' NOT LIKE 'anonymised:%'` — **وعلى SQLite حيثُ تعملُ الحزمةُ كلُّها تعملُ صحيحة**. ⚠️ **ولا `CONCAT()`** كذلك: وصلَت SQLite في ٣.٤٤ وقد يحملُ عاملُ CI أقدمَ منها. ⚠️ **وصيغةُ المقبرةِ فريدةٌ بالبناءِ ولا تُطابَقُ في `DeviceRegistry::resolve()`** — البصمةُ الحقيقيّةُ SHA-256 من ٦٤ محرفاً
- [ ] T010 أضِفِ المفتاحَينِ إلى `describe()` **وذراعَي التصديرِ إلى `export()`** في `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — **في التزامةٍ واحدةٍ لا اثنتَين**: `PersonalDataContractCoverageTest` يُقارِنُ المُعلَنَ بالمُخرَجِ فعلاً، فمفتاحٌ مُعلَنٌ بلا ذراعِ تصديرٍ **يُسقِطُ البناءَ بينَهما**. ⛔ **والمفتاحُ يُخرَجُ فارغاً ولا يُحذَف** (SC-008) — تهجئةُ `date_of_birth` في الملفِّ عينِه. الحقول: `auth_session` ⇐ `surface · status · ended_reason · device_label · started_at · last_active_at · ended_at · ip_hash` · `device` ⇐ `label · created_at · last_seen_at`. ⚠️ **و`devices.first_seen_at` غيرُ موجود** — كانَ في النسخةِ الأولى؛ العمودانِ `last_seen_at` و`timestamps()`
- [ ] T011 [P] أضِفْ `fingerprint_hash` إلى `forbiddenKeys()` في `backend/app/Modules/Compliance/Support/ExportFieldAllowlist.php` **بسببِه مكتوباً بجوارِه** — بصمةُ آلةٍ لا يقرؤُها صاحبُها ولا تُثبِتُ له شيئاً. ⛔ **و`ip_hash` لا يُمنَع، وهذا قرارٌ يُكتَبُ لا سهو**: تعليقُ الملفِّ نفسِه (`:52`) يقولُ إنّ `ip_address`/`ip_hash`/`user_agent` غائبةٌ عن المنعِ **عن قصد** «لأنّها بياناتُ صاحبِها»، والمواصفةُ تفتحُ بأنّ «أين دخلَ حسابُك» لم يكنْ يصلُ صاحبَه
- [ ] T012 اكتبْ ذراعَي المحوِ في `erase()` داخلَ `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — `auth_sessions` ⇒ `ip_hash = NULL · session_id = NULL · device_id ← المقبرة` لكلِّ صفوفِ المستخدِم (نشطةً كانت أو منتهية) · `devices` ⇒ `fingerprint_hash ← 'anonymised:'.$id`. ⛔ **فوقَ حارسِ `alreadyAnonymised()` لا تحتَه** (FR-014): `erase()` يرجِعُ `0` قبلَ المعاملةِ لحسابٍ بريدُه يحملُ العلامة، فكتابتُهما تحتَه تترُكُ **كلَّ حسابٍ مُحِيَ قبلَ اليومِ** محتفِظاً بعنوانِه وبصماتِه إلى الأبد. ⚠️ **ولا يُضعَّفُ الحارسُ نفسُه** — هو ما يجعلُ حلقةَ `$done < $limit` في `ExecuteDataErasure` تنتهي
- [ ] T013 اكتبْ ترحيلَ المرّةِ الواحدةِ لمن مُحِيَ قبلَ اليومِ تحتَ `backend/database/migrations/` — ⛔ **بـ`DB::table()` ولا نموذجَ واحد**: ترحيلٌ يتكلّمُ مخطَّطَ تاريخِه ونموذجٌ يتكلّمُ اليوم، و`2026_08_29_003100_backfill_referral_catalogue_action` أسقطَ **٦٩٠ من ٦٩٠** اختباراً بهذا عينِه. ⛔ **و`chunkById` لا `chunk`**: مُرشِّحُ `ip_hash IS NOT NULL` ينكمِشُ تحتَ الترقيمِ بالإزاحةِ فتتخطّى كلُّ صفحةٍ ما أصلحَته سابقتُها **وتُبلِّغُ بالنجاح**. ⛔ **وصفُّ المقبرةِ يُكتَبُ بـ`uuid` وطوابعِ وقتٍ صريحةٍ**: الإدراجُ الخامُّ لا يُقلِعُ النموذجَ فلا يعملُ `HasUuid`، وMySQL يُنزِلُ انتهاكَ NOT NULL إلى تحذيرٍ ويخزِّنُ `''` — فيصطدمُ كلُّ صفٍّ لاحقٍ على `unique(uuid)` ويُقرَأُ «مسجَّلٌ سلفاً»
- [ ] T014 اكتبْ حالةَ «المحوُ لم يُطفَأْ في الوحدةِ كلِّها» في `backend/tests/Feature/Compliance/ErasureModeAgreementTest.php` أو ملفٍّ جديدٍ بجوارِه — ⛔ **تُقاسُ أوّلاً** (quickstart § ٢): `modeFor()` كاملُ السكوتِ عندَ الإخفاق، فحالةٌ تُثبِتُ أنّ الوضعَ المُجمَّعَ لوحدةِ `identity` ما زالَ **واحداً** هي الوحيدةُ التي تُحمِّرُ على صفٍّ كُتِبَ بوضعٍ مخالف

**Checkpoint**: الفئتانِ مُعلَنتانِ، والتصديرُ والمحوُ يصلانِهما، والحارسانِ خضراوان — و`expire()` ما زالت `return 0`.

---

## Phase 3: User Story 1 — الطالبةُ تقرأُ سجلَّها الأمنيَّ بعدَ التنظيف (P1) 🎯 نصفُ الـMVP

**الهدف**: الجلسةُ المنتهيةُ التي تجاوزَتِ المدّةَ تفقِدُ «من أين» وتحتفظُ بـ«متى ولماذا»، **ولا يبقى طريقٌ إلى البصمةِ عبرَ صفِّ الجهاز**.

**Independent Test**: تُشيَّخُ جلسةٌ منتهيةٌ لحسابٍ واحدٍ **باستعلامٍ لا داخلَ `create()`**، تُشغَّلُ المكنسة، ثمّ تُقرأُ حمولةُ الشاشة: الصفُّ قائمٌ بتاريخَيه وسببِ انتهائِه، و`ip_hash` و`session_id` فارغان، و`device_id` يُشيرُ إلى المقبرة — ومعه **ضابطٌ موجَبٌ في التشغيلةِ نفسِها**: جلسةٌ حديثةٌ لم تُمَسّ.

- [ ] T015 [US1] أعِدْ كتابةَ جسدِ `expire()` وتعليقِها في `backend/app/Modules/Identity/Support/IdentityPersonalData.php:260` — ⛔ **التعليقُ يُعادُ كتابتُه لا يُضافُ تحتَه**: نصُّه اليومَ «**لا شيءَ في هذه الوحدةِ ينتهي بساعة**» وهو يصيرُ كذباً بهذه المهمّة، وتعليقٌ يُناقِضُ السطرَ تحتَه أسوأُ من لا تعليق. تتفرّعُ على `$category` وترُدُّ `0` لما سواهما
- [ ] T016 [US1] اكتبْ ذراعَ الجلسةِ في `expire()` داخلَ الملفِّ نفسِه في `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — المُرشَّحون: `status = 'ended' AND ended_at < :before AND ip_hash IS NOT NULL AND user_id NOT IN (:exempt) LIMIT :batch`، ولكلِّ صفٍّ في PHP: `device_id ← المقبرة · ip_hash ← NULL · session_id ← NULL`. ⛔ **`ended_at` لا `created_at`**، وهو انحرافٌ عن تعليقِ العقدِ يُكتَبُ بدلَ أن يُسكَتَ عنه: جلسةٌ بُدِئَت قبلَ سنةٍ وانتهَت أمسِ دليلٌ عمرُه يوم. ⛔ **و`session_id` يُمحى مع `ip_hash`** (FR-015): أُضيفَ ٢٠٢٦-٠٩-١٧ وهو **غائبٌ عن كلِّ جردٍ كُتِبَ لهذا الجدول**، ومُعرِّفٌ لكلِّ متصفّح. ⚠️ **و`ip_hash IS NOT NULL` هو المُرشِّحُ والموقِفُ في آن** — وهو أيضاً ما يجعلُ صفّاً وُلِدَ بلا عنوانٍ (`StartAuthSession` يكتبُ `null` حينَ لا عنوانَ للطلب) لا يُختارُ ولا يحتاجُ أن يُختار. ⚠️ **والنشطةُ لا تُمَسُّ إطلاقاً** (FR-003): `status = 'ended'` أوّلُ شرط
- [ ] T017 [US1] اكتبْ ذراعَ الجهازِ في `expire()` داخلَ الملفِّ نفسِه في `backend/app/Modules/Identity/Support/IdentityPersonalData.php` — `WHERE fingerprint_hash NOT LIKE 'anonymised:%' AND user_id NOT IN (:exempt) AND created_at < :before AND NOT EXISTS (جلسةٌ active) AND NOT EXISTS (جلسةٌ COALESCE(ended_at, created_at) >= :before) LIMIT :batch` ⇒ `fingerprint_hash ← 'anonymised:'.$id` **مبنيّةً في PHP** (T009). ⛔ **`created_at < :before` يُغلِقُ سباقاً حقيقيّاً**: `StartAuthSession` يكتبُ الجهازَ ثمّ يسكُّ الرمزَ ثمّ يكتبُ الجلسة، وبينَهما الجهازُ **بصفرِ جلسات** فيصدُقُ عليه الشرطانِ **فراغاً** — وتجهيلُه يكسِرُ مطابقةَ `firstOrNew` فتصيرُ الآلةُ الواحدةُ آلتَينِ في حدِّ الأجهزة، أي **طردُ الطالبِ من الحاسوبِ الذي يجلسُ أمامَه**. ⛔ **و`COALESCE(ended_at, created_at)`**: `ended_at` قابلٌ للفراغِ حتّى على صفٍّ `ended`، و`NULL >= :before` فارغةٌ فلا يُرى الصفُّ — فتُجهَّلُ بصمةُ جهازٍ عليه جلسةٌ حديثة. ⚠️ **ولا يُقرَأُ `last_seen_at`**: قابلٌ للفراغ، وشرطٌ عليه يتخطّى الصفَّ الفارغَ إلى الأبد
- [ ] T018 [US1] اكتبْ `backend/tests/Feature/Identity/AuthSessionRetentionTest.php` — الحالاتُ من `quickstart.md` § ٣–٦ و٨–١٠: تجهيلُ القديمةِ · **ضابطٌ موجَبٌ بجوارِ كلِّ نفي** (الحديثةُ لم تُمَسّ · النشطةُ لم تُمَسّ) · المقبرةُ واحدةٌ لكلِّ مستخدِمٍ ولا يُوصَلُ منها إلى بصمة · الحظرُ القضائيُّ يُوقِفُ الذراعَينِ معاً · حدُّ الأجهزةِ لم يتحرّك · المحوُ يصلُ الجدولَينِ ولِمن مُحِيَ قبلَ اليوم. ⛔ **والتشييخُ باستعلامٍ لا داخلَ `create()`**: `created_at` ليس في `$fillable` فصفٌّ «قديمٌ» مكتوبٌ في المصفوفةِ **يُولَدُ اليوم**، وكلُّ تأكيدِ احتفاظٍ فوقَه أخضرُ على بياناتٍ أصغرَ من أن تُكنَس. ⚠️ **وشكلُ الحمولةِ يُؤكَّدُ كما هو لا «مطابقاً تماماً»**: اسمُ الجهازِ يصيرُ جملةَ المقبرة (T003)
- [ ] T019 [US1] شغِّلِ المكنسةَ **مرّتَينِ** في كلِّ حالةِ تجهيلٍ في `backend/tests/Feature/Identity/AuthSessionRetentionTest.php` — ⛔ **تشغيلةٌ واحدةٌ خضراءُ إلى الأبدِ وتُثبِتُ العكس**، وهي قاعدةُ `RollupIdempotencyTest` و`RetentionSweepIdempotencyTest` المكتوبةُ في المستودَع. والثانيةُ تُبلِّغُ **صفراً** (SC-006)
- [ ] T020 [US1] وسِّعْ `backend/tests/Feature/Compliance/RetentionSweepIdempotencyTest.php` بتأكيدٍ على `rows_anonymised` — ⚠️ **الملفُّ قائمٌ لهذه المهمّةِ بالذات ولا وثيقةَ في النسخةِ الأولى ذكرَتْه**، **وتأكيدُه اليومَ يُغفِلُ هذا العمودَ**: فسلوكُ `Anonymise` يُعادُ عدُّه كلَّ ليلةٍ ولا شيءَ يُحمِّر
- [ ] T021 [P] [US1] اقِسْ «صفرُ تغييرٍ في ملفّاتِ الواجهة» (FR-008) وسجِّلْه في `specs/038-session-log-retention/quickstart.md` § ١٣ — ⛔ **قياسٌ لا ادّعاء**: `git diff --stat frontend/` فارغٌ، **و**الحمولةُ `frontend/src/lib/sessions.ts:24` تحملُ `device: { uuid, label }` والشاشةُ `settings/security/page.tsx:106` تقرأُ `session.device.label` **ولا تبني على `device.uuid`** زرّاً ولا تجميعاً — فصفُّ المقبرةِ يُعرَضُ سطراً مقروءاً ولا يفتحُ باباً. ⚠️ **وشارةٌ تُضافُ لحالةٍ لا تُغيِّرُ حرفاً على الشاشةِ هي نفسُها ما يُقرَأُ عطباً**

**Checkpoint**: US1 تعملُ وحدَها — الجدولُ نظيفُ المحتوى وحجمُه لم ينقُصْ صفّاً واحداً، وهذا بالضبطِ ما تُكمِلُه US2.

---

## Phase 4: User Story 2 — الجدولُ يتوقّفُ عن النموِّ بلا سقف (P1) 🎯 نصفُ الـMVP

**الهدف**: لكلِّ مستخدِمٍ أحدثُ N جلسةٍ **مُجهَّلةٍ سلفاً وعبرَتِ الأرضيّة**، وما قبلَها يخرج. والنشطةُ والحديثةُ خارجُ العدِّ بنصِّ FR-003 وFR-004.

**Independent Test**: حسابٌ بجلساتٍ منتهيةٍ **مُجهَّلةٍ سلفاً** تفوقُ السقف، **ومعها حديثاتٌ ونشطاتٌ تفوقُ السقفَ وحدَها**، تُشغَّلُ المهمّة، ثمّ ثلاثُ عدّاتٍ: الحديثاتُ كلُّها باقية · النشطاتُ كلُّها باقية · المُجهَّلاتُ وحدَها نزلَت إلى السقفِ والباقياتُ منها هي الأحدث.

**⚠️ يعتمدُ على US1**: التجهيلُ هو ما يُنتِجُ مُرشَّحَ السقف. ويُختبَرُ مستقلّاً بمُهيَّأٍ يُشيِّخُ ويُفرِّغُ `ip_hash` باستعلام.

- [ ] T022 [P] [US2] اكتبْ `backend/config/auth_sessions.php` بالافتراضَين — السقفُ **٥٠** والأرضيّةُ **١٨٠ يوماً**، بتعليقٍ يقولُ إنّه احتياطيُّ قاعدةٍ لم يُبذَرْ فيها شيء. ⚠️ **وكلُّ مسارٍ يجبُ أن يُرجِعَ قيمةً غيرَ فارغة**: الخريطةُ تُقرَنُ بمسارِ إعدادٍ، ومسارٌ يُرجِعُ فارغاً يُسقِطُ حارسَ الإعدادات
- [ ] T023 [US2] أضِفِ المفتاحَينِ إلى `KEYS` في `backend/app/Modules/Tenancy/Support/PlatformSettings.php` — `'auth.auth_session_cap_per_user' => 'auth_sessions.cap_per_user'` و`'auth.auth_session_cap_min_age_days' => 'auth_sessions.cap_min_age_days'`، بجوارِ `auth.device_limits`. ⛔ **بدونِهما هما غائبانِ عن `all()` وعن `flush()`** — كلتاهما تلفُّ على `array_keys(self::KEYS)` — فلا تراهما اللوحةُ ولا يُبطِلُهما مسحُ الذاكرة، **والقراءةُ `rememberForever`**. ⚠️ **والبادئةُ `auth.` لا `identity.`**: الوحدةُ لها `auth.device_limits` و`auth.two_factor_grace_days`، وبادئةٌ ثالثةٌ تُفرِّقُ أرقامَ شاشةٍ واحدةٍ على قسمَين
- [ ] T024 [US2] اكتبْ `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php` على قالبِ `TransferDataOwnershipJob.php` في المجلّدِ عينِه — ⛔ **الاكتشافُ استعلامٌ والحذفُ استعلامٌ ثانٍ، والمعرِّفاتُ تُمرَّرُ قيَماً**: `GROUP BY user_id HAVING count(*) > :cap` ثمّ لكلِّ مستخدِمٍ `pluck('id')` ثمّ `whereIn` — **و`DELETE … WHERE id NOT IN (SELECT … FROM auth_sessions …)` هو MySQL ERROR 1093**، وSQLite يُعيدُ كتابتَه إلى `rowid in (…)` **فيمرُّ أخضرَ إلى الأبد**. ⛔ **والمُرشَّحُ `status='ended' AND ip_hash IS NULL AND ended_at < :floor`** — أي ما جُهِّلَ سلفاً وعبرَ الأرضيّة: `auth_sessions` سجلُّ الدخولِ الوحيدُ في المنتَج، وسقفٌ بالعددِ وحدَه يُبلَغُ من لوحةِ مفاتيحٍ حيّةٍ في **عشرِ دقائق** تحتَ `throttle:auth`، فتمحو الكنسةُ أثرَ الاقتحامِ الأوّل. ⛔ **`ORDER BY ended_at DESC, id DESC`** لا `created_at`: الثانيةُ دقّةُ الأخيرِ فدخولانِ في ثانيةٍ يتعادلان ويتغيّرُ الباقي بينَ تشغيلَينِ تُقارِنُهما SC-006 — **وادّعاءُ النسخةِ الأولى أنّ `auth_sessions_eviction_index` يخدمُ هذا الترتيبَ خطأ**، فالفهرسُ `(user_id, status, device_id, created_at)` و`device_id` بينَ البادئةِ وعمودِ الترتيب. ⚠️ **وسقفٌ ≤ صفرٍ يُقرَأُ «لا سقف»** ويرجِعُ بلا حذف: مشغِّلٌ يُفرِّغُ الحقلَ يقصدُ إيقافَ الميزةِ لا تفريغَ الجدول. ⚠️ **ولا حذفَ لصفِّ جهازٍ البتّة** (FR-006): لا مفتاحَ أجنبيَّ على `device_id`، و`$this->device->label` على إشارةٍ مكسورةٍ خطأُ ٥٠٠ **للطلبِ كلِّه** لا للصفِّ وحدَه
- [ ] T025 [US2] اسألِ `LegalHoldDirectory` **قبلَ كلِّ دفعةٍ** داخلَ `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php`، **والإعفاءُ في الـ`WHERE`** — ⛔ تخطّي صفوفٍ **بعدَ** سحبِها يُرجِعُ عدداً دونَ الدفعةِ فتنتهي الحلقةُ، **وكلُّ مستخدِمٍ بعدَه لا يُعالَجُ لا الليلةَ ولا أيَّ ليلة**. ⛔ **وقائمةٌ فارغةٌ تعني «لا حظر» لا «لا تحذِفْ شيئاً»، وإخفاقُ النداءِ يُوقِفُ الدفعةَ** — اتّجاهُ الخطأِ في الثانيةِ آمنٌ وفي الأولى لا يُعكَس. وهي تهجئةُ `ExecuteDataErasure` نفسُها («البابُ الثالث» في تعليقِها)
- [ ] T026 [US2] أضِفْ إلى `backend/app/Modules/Identity/Jobs/EnforceAuthSessionCapJob.php`: `WithoutOverlapping` **بمفتاحٍ خاصٍّ بها** مع `expireAfter()` · **سقفَ دفعاتٍ خاصّاً بها** لا ثابتَ المكنسة (ذاك سقفٌ **لكلِّ فئة** وهذا يمشي على المستخدِمين) · `try/catch` **لكلِّ مستخدِم** · وسطراً مكتوباً بما فعلَته. ⛔ **`expireAfter()` هو النصفُ الحامل**: قفلٌ لا ينتهي يعني أنّ السقفَ لا يعملُ بعدَ أوّلِ عاملٍ يُقتَل — ⚠️ **و`withoutOverlapping()` على `Schedule::job()` يحرسُ الدفعَ لا التشغيل**، وهو مللي ثانيةٍ حولَ الدفع. ⚠️ **وليلةٌ بلا سجلٍّ لا يُفرَّقُ فيها بينَ «لا عمل» و«لم تعمَلْ منذُ مارس»**
- [ ] T027 [US2] جدوِلِ المهمّةَ في `backend/routes/console.php` — `Schedule::job(new EnforceAuthSessionCapJob, 'compliance')->dailyAt('04:15')`، بجوارِ `TransferDataOwnershipJob` (`:407`). ⛔ **بعدَ مكنسةِ ٣:٣٠ لا قبلَها، والترتيبُ لازمٌ لا تفضيليّ**: التجهيلُ هو ما يُنتِجُ مُرشَّحَ السقف، فمهمّةٌ تسبقُ المكنسةَ تعملُ على مُرشَّحِ الأمس. ⚠️ **ومكنسةٌ مكتوبةٌ ولا تعملُ** هي البندُ الذي سجّلَه هذا المستودَعُ مرّتَينِ من قبل (`ReconcileAssetStatus` · طابورُ `maintenance`) — فتُقاسُ بأنّها في `schedule:list`
- [ ] T028 [US2] اكتبْ `backend/tests/Feature/Identity/AuthSessionCapTest.php` — ⚠️ **ملفٌّ مستقلٌّ عن `AuthSessionRetentionTest` عن قصد**: المكنسةُ والمهمّةُ بابانِ مختلفانِ ويُشغَّلانِ مستقلَّين، وخلطُهما يجعلُ US2 غيرَ قابلةٍ للقياسِ وحدَها. الحالاتُ من `quickstart.md` § ٧: الثلاثُ عدّاتٍ · حسابٌ دونَ السقفِ لا يتغيّر · حسابٌ بجلسةٍ واحدةٍ لا ينزلُ إلى صفر · **مُجهَّلةٌ داخلَ الأرضيّةِ لا تُحذَف** · الحظرُ يُعفي · تشغيلتانِ ⇒ حالةٌ واحدةٌ والثانيةُ صفر
- [ ] T029 [US2] أكِّدْ في الملفِّ نفسِه أنّ SC-003 محفوظةٌ **بالضبط**: عددُ `status='active'` على المنصّةِ كلِّها قبلَ التشغيلِ = بعدَه في `backend/tests/Feature/Identity/AuthSessionCapTest.php` — ⛔ **وهذا التأكيدُ يُكتَبُ على مستوى الجدولِ لا على حسابِ المُهيَّأ**: «المكنسةُ لم تُخرِجْ أحداً من حسابِه» ادّعاءٌ عن كلِّ المنصّة، وحالةٌ تعُدُّ حساباً واحداً تمرُّ على بناءٍ يطردُ غيرَه

**Checkpoint**: US1 + US2 = المنتَجُ الأدنى. الجدولُ نظيفُ المحتوى **وحجمُه محدود**، والأرقامُ ما زالت افتراضاتٍ مشحونة.

---

## Phase 5: User Story 3 — المشغِّلُ يضبطُ الأرقامَ الثلاثةَ من شاشةٍ واحدة (P2)

**الهدف**: مدّةُ الاحتفاظِ وسقفُ الجلساتِ وأرضيّةُ العمرِ تُكتَبُ كلُّها من `ManagePlatformSettings`، ويظهرُ أثرُها في التشغيلةِ التالية بلا إصدار.

**Independent Test**: يُغيَّرُ كلُّ رقمٍ من الشاشة، تُشغَّلُ المكنسةُ والمهمّة، ويُقاسُ أنّ السلوكَ تبِعَ الرقمَ الجديد — وعلى قاعدةٍ لم تُضبَطْ فيها قيمةٌ تعملُ بالافتراضاتِ ولا تُخفِق (SC-007).

- [ ] T030 [US3] أضِفِ الحقلَينِ الرقميَّينِ (السقفُ والأرضيّة) إلى `backend/app/Modules/Tenancy/Filament/Pages/ManagePlatformSettings.php` في قسمٍ واحدٍ مع حدِّ الأجهزة — ⚠️ **قسمٌ واحدٌ لا قسمان**: الأرقامُ الثلاثةُ تُقرَأُ معاً وتُحرَّرُ معاً، وتفريقُها هو ما يجعلُ بعضَها يُملأُ والباقيَ يُنسى (تعليقُ `billing.transfer` في `PlatformSettings` يكتبُ هذه القاعدةَ بنصِّها)
- [ ] T031 [US3] أضِفْ حقلَ مدّةِ الاحتفاظِ إلى `backend/app/Modules/Tenancy/Filament/Pages/ManagePlatformSettings.php` **مُمرَّراً على `Compliance\Actions\SaveDataCategory` للصفَّينِ كليهما** — ⚠️ **وهذا أوّلُ استدعاءِ Action عبرَ حدِّ وحدةٍ من لوحةِ Filament في هذه الشجرة (مقيس)، فيُسمّى قراراً لا سهواً**: الملفُّ نفسُه يستوردُ اليومَ `Payments\Support\BillingSettings` و`LiveSessions\Enums\ClassSessionType` — قراءاتٌ عابرةٌ للحدّ — والبديلُ هنا كتابةُ العمودِ خامّاً، **وحارسُ `SaveDataCategory` يقولُ في تعليقِه إنّ خفضَ المدّةِ «ليس فوراً بل محوُ كلِّ ما مضى في ليلة»** وSQL خامٌّ يتخطّاه. ⛔ **والمدّةُ تبقى مخزَّنةً في `data_categories.retain_days`**: `DataCategoryResource` يُرسِلُ `retain_days` **ومعه `retention_label_ar`**، وصفٌّ بلا مدّةٍ يُطبَعُ «يُحفظ ما دام الحساب قائماً» على «خصوصيّتي» — **إشعارُ خصوصيّةٍ يكذِب**. طلبُ المالكِ كانَ عن **مكانِ الضبطِ** لا مكانِ التخزين
- [ ] T032 [US3] اكتبْ حالاتِ الشاشةِ في `backend/tests/Feature/Tenancy/` بجوارِ اختباراتِ الصفحةِ القائمة — رفعُ السقفِ ⇒ التشغيلةُ التاليةُ تُبقي العددَ الجديد · تقصيرُ المدّةِ ⇒ تُجهَّلُ جلساتٌ كانت داخلَ القديمةِ · **وقاعدةٌ بلا صفٍّ واحدٍ في `platform_settings` تعملُ بالافتراضاتِ وتنتهي بلا خطأ** (SC-007). ⚠️ **والتأكيدُ على السلوكِ لا على الصفِّ المكتوب**: «كُتِبَ ٢٠ في الجدول» أخضرُ على مكنسةٍ لا تقرأُ الصفَّ أصلاً

**Checkpoint**: الثلاثُ قصصٍ تعملُ مستقلّاتٍ، والأرقامُ بيدِ المشغِّل.

---

## Phase 6: Polish & Cross-Cutting

- [ ] T033 امشِ على `specs/038-session-log-retention/quickstart.md` من § ٠ إلى § ١٤ على قاعدةِ التطويرِ وسجِّلِ الأرقامَ المقيسةَ في الملفِّ نفسِه — ⚠️ **وأقدمُ صفٍّ في التطويرِ عمرُه ٤٧ يوماً وفي الإنتاجِ ١٩**، فكلُّ قياسٍ على قاعدةٍ حقيقيّةٍ اليومَ يُخرِجُ أصفاراً **وهي نفسُها ما يُخرِجُه بناءٌ لا ذراعَ فيه**: المُهيَّأُ المُشيَّخُ هو الفارقُ الوحيدُ بينَ القياسَين
- [ ] T034 [P] شغِّلْ `./vendor/bin/pint` و`./vendor/bin/phpstan analyse` من `backend/` — ⚠️ **والمحلّلُ يقرأُ `Database/Migrations` لكلِّ وحدةٍ ليُنمِّطَ الخصائص**، والشجرةُ نظيفةٌ على المستوى ٨ وتبقى كذلك
- [ ] T035 ادفعِ الفرعَ ودعِ الشرائحَ الأربعَ تُشغِّلُ الحزمةَ على CI في `.github/workflows/ci.yml` — ⛔ **ولا تُشغَّلُ حزمتانِ محلّيّاً معاً**: تتقاسمانِ قرصَ الاختبارِ وتصنعانِ إخفاقاً كاذباً
- [ ] T036 [P] أضِفْ بنداً إلى `CLAUDE.md` تحتَ المزالق — **ثلاثةُ أشياءَ في سطرٍ واحد**: `||` رابطُ نصوصٍ على SQLite وعاملُ «أو» على MySQL · `DELETE … NOT IN (SELECT … FROM نفسِ الجدول)` هو ERROR 1093 وSQLite تُعيدُ كتابتَه · **وفئةُ كتالوجٍ بـ`erasure_mode` مخالفٍ تُطفِئُ المحوَ في وحدتِها كلِّها بلا خطأٍ ظاهر**. ⚠️ الثلاثةُ **خضراءُ على الحزمةِ كلِّها**، وهذا وحدَه سببُ كتابتِها

---

## Dependencies & Execution Order

### اعتمادُ الأطوار

- **Phase 1 (Setup)**: بلا اعتماد — ويسبقُ كلَّ اختبار، لا يليه
- **Phase 2 (Foundational)**: يعتمدُ على Setup — **يُعيقُ كلَّ القصص**
- **Phase 3 (US1)**: بعدَ Foundational
- **Phase 4 (US2)**: بعدَ Foundational · **ويعتمدُ على US1 سلوكيّاً** (التجهيلُ يُنتِجُ المُرشَّح)، ويبقى مقيساً وحدَه بمُهيَّأٍ يُفرِّغُ `ip_hash` باستعلام
- **Phase 5 (US3)**: بعدَ US1 وUS2 — لا معنى لضبطِ رقمٍ لا يقرؤُه أحد
- **Phase 6 (Polish)**: بعدَ الثلاث

### داخلَ الملفِّ الواحد — لا `[P]` البتّة

| الملفّ | المهامّ |
|---|---|
| `IdentityPersonalData.php` | T010 · T012 · T015 · T016 · T017 |
| `AuthSessionFactory.php` | T001 · T002 |
| `EnforceAuthSessionCapJob.php` | T024 · T025 · T026 |
| `ManagePlatformSettings.php` | T030 · T031 |
| `AuthSessionCapTest.php` | T028 · T029 |
| `AuthSessionRetentionTest.php` | T018 · T019 |

### فرصُ التوازي

- **Phase 1**: T003 مع T001/T002
- **Phase 2**: T004 · T005 معاً (ملفّانِ جديدانِ مستقلّان)، وT011 مع T007/T008
- **Phase 3**: T021 مع T018–T020
- **Phase 4**: T022 قبلَ كلِّ شيءٍ آخرَ في الطور
- **Phase 6**: T034 · T036 معاً

```bash
# Phase 2 — العقدُ ومُنفِّذُه
Task: "اكتبْ backend/app/Shared/Contracts/LegalHoldDirectory.php"
Task: "اكتبْ backend/app/Modules/Compliance/Support/EloquentLegalHoldDirectory.php"

# Phase 3 — القياسُ والواجهة
Task: "اكتبْ backend/tests/Feature/Identity/AuthSessionRetentionTest.php"
Task: "اقِسْ صفرَ تغييرٍ في frontend/ وسجِّلْه في quickstart.md § ١٣"
```

---

## Implementation Strategy

### المنتَجُ الأدنى = **US1 + US2 معاً**

1. Phase 1 → Phase 2 → Phase 3 → **Phase 4**
2. قِفْ وقِسْ: `quickstart.md` § ٣–٧ على قاعدةٍ مُشيَّخة
3. ⛔ **ولا تُشحَنُ US1 وحدَها**: التجهيلُ **لا يُنقِصُ صفّاً واحداً**، فشحنُه منفرداً يُنظِّفُ محتوى الجدولِ ويترُكُ حجمَه ينمو إلى الأبد — «نصفُ المشكلةِ عولِجَ ونصفُها سُمّيَ مُعالَجاً» بنصِّ المواصفة. ⛔ **ولا US2 وحدَها**: مُرشَّحُها `ip_hash IS NULL`، وبلا ذراعِ التجهيلِ لا يُوجَدُ مُرشَّحٌ أصلاً — أو أسوأُ، يُحذَفُ صفٌّ ما زالَ يحملُ عنواناً
4. US3 بعدَها — الافتراضانِ المشحونانِ يجعلانِ الميزةَ صحيحةً قبلَ أن يفتحَ المشغِّلُ اللوحةَ أصلاً

### الأثرُ على الإنتاجِ في أوّلِ ليلة

**أقربُ إلى صفرٍ منه إلى شيء، ويُقالُ قبلَ الشحنِ لا بعدَه**: أقدمُ صفٍّ في الإنتاجِ عمرُه ١٩ يوماً والمدّةُ ٩٠، فذراعُ العمرِ **لا تبلغُ صفّاً واحداً**؛ والأرضيّةُ ١٨٠ فالسقفُ لا يبلغُ شيئاً كذلك. **الميزةُ تُغلِقُ ثغرةً قبلَ أن تعضّ، ولا تُقاسُ بما تحذِفُه اليومَ بل بما يستحيلُ معها أن ينمو.**

---

## Notes

- `[P]` = ملفّاتٌ مختلفةٌ بلا اعتماد
- **صفرُ تغييرٍ تحتَ `frontend/`** — وهو مقيسٌ (T021) لا مفترَض
- **ولا نقطةَ نهايةٍ جديدةٌ ولا صلاحيّةٌ جديدة**: السطحُ العامُّ الوحيدُ الذي تزيدُه الميزةُ هو `LegalHoldDirectory`
- **وثلاثةُ أشكالٍ تمرُّ خضراءَ على SQLite وتنفجرُ على MySQL وحدَها** (T009 · T024 · T013). مَن يقرأُ سطرَ المهمّةِ وحدَه ويكتبُ الشكلَ البديهيَّ يُسقِطُ الإنتاج
