# Tasks: الإدارةُ تملكُ التسجيل

**Feature**: 034 · `specs/034-admin-owns-enrolment/`

**Input**: [spec.md](./spec.md) · [plan.md](./plan.md) · [research.md](./research.md) · [data-model.md](./data-model.md) · [contracts/README.md](./contracts/README.md) · [quickstart.md](./quickstart.md)

**Tests**: **مطلوبة**. المبدأُ الرابعُ يوجبُ خُضرةَ البوّابات، والمواصفةُ تمسُّ «التسجيلَ وتقييدَ الدروس» — أحدَ الثمانيةِ الحرجة. وقسمُ «OPTIONAL» في القالبِ لا يسري.

**Status**: المواصفةُ والخطّةُ **مُثبَّتتانِ بعدَ مسحٍ كاملٍ** (2026-09-11): خمسةُ قراراتِ مالكٍ حُسِمَت، وأربعةُ ادّعاءاتٍ «مقيسةٍ» صُحِّحَت، ولم يبقَ في المستنداتِ السبعةِ سطرٌ يصفُ حالاً سابقاً على أنّه الحال.

**Organization**: مُرتَّبةٌ بالقصص. ⛔ **و`US2` لا تُشحَنُ قبلَ `US1`**: «لا يُغلَقُ بابٌ قبلَ بناءِ البابِ الذي يحلُّ محلَّه».

---

## ⛔ ستّةُ قياساتٍ تحكمُ هذه المهامَّ كلَّها

هذه ليست تحذيراتٍ عامّة. كلُّ واحدٍ منها **قُلِبَ به بندٌ في الخطّةِ بعدَ قياسِه في الشجرة**، ونسيانُه يُعيدُ العطبَ الذي أُزيلَ من المستند.

| القياس | الأثرُ على المهامّ |
|---|---|
| **اكتشافُ لوحةِ الإدارةِ سطرٌ لكلِّ وحدة، ولا سطرَ لـ`Learning`** | `T003`. بدونَه الشاشتانِ **ملفّانِ لا شاشتان**: لا مسار، لا خطأ، والصلاحيّةُ تحرسُ العدمَ وتمرُّ في كلِّ اختبارٍ لها |
| **التسجيلُ يكتبُه مستمِعٌ مُصطفٌّ بعدَ تثبيتِ المعاملة** | `T020`–`T023`. كتابةُ العضويّةِ داخلَ اعتمادِ الطلبِ **مستحيلةٌ لا صعبة** — لا تسجيلَ بعد |
| **`joinableCohortsExist()` يردُّ «لا» على المكتمِلِ وعلى الذي بلا مجموعاتٍ بالقيمةِ نفسِها** | `T010` · `T046`. حارسٌ بشرطٍ واحدٍ **يمنعُ بيعَ كلِّ محتوًى مسجَّل** |
| **`WorkspaceContext` يُحَلُّ لموظَّفِ المنصّةِ من `last_workspace_id`** | `T012` · `T017` · `T024` · `T038`. ومساحةٌ واحدةٌ في التركيبةِ **تمرُّ على البناءِ المعطوبِ كلِّه** |
| **`BelongsToWorkspace` لا يملأُ العمودَ إلّا إن حُلَّ السياق، وسياقُ الطالبِ `null` دائماً** | `T042` · `T045`. وإلّا فانتهاكُ NOT NULL في كلِّ تسجيلٍ في الدَّور |
| **صفحةُ Filament لا تستدعي سياسةً، وقائمتُها لا تستدعي سياسةَ الصفّ** | `T016` · `T050`. البابُ `canAccess()` **والقطعُ على الاستعلام** — وإخفاءُ زرٍّ ليسَ حراسة |

---

## Format: `[ID] [P?] [Story] Description`

- **[P]**: قابلةٌ للتوازي — ملفٌّ مختلفٌ ولا تبعيّةَ على مهمّةٍ غيرِ مُنجَزة
- **[Story]**: `US1` · `US2` · `US3` · `US4`

## Path Conventions

`backend/app/Modules/…` · `backend/app/Providers/…` · `frontend/src/…` · `backend/tests/Feature/…`

---

## Phase 1: Setup — ما لا تُرى الشاشةُ بدونِه

**Purpose**: ثلاثةُ أسطرٍ لا يكتشفُ غيابَها شيء.

- [ ] T001 [P] أضِفْ `COHORTS_ASSIGN = 'cohorts.assign'` إلى `backend/app/Modules/Tenancy/Support/Permissions.php` وإلى `all()`، **ولا تضعْه في أيِّ مصفوفةِ دورِ مستأجرٍ في `RolePermissionMatrix`** — المجموعةُ المنصّيّةُ مشتقّةٌ بالطرح، فالغيابُ هو التصنيف. ⚠️ **واكتبْ بجانبَه أنّ حاملَه مديرُ المنصّةِ وحدَه بقرارِ المالك (2026-09-11)**: صلاحيّةٌ لا يحملُها دورٌ منصّيٌّ منصوصٌ عليه تمرُّ عبرَ `Gate::before` وحدَه، وهذا **قرارٌ لا أثرٌ جانبيّ** — بلا هذه الجملةِ يمنحُها القارئُ التالي لدورٍ فيرمي `Tenancy\Models\Role`.
- [ ] T002 [P] أنشِئْ مجلَّدَ `backend/app/Modules/Learning/Filament/Pages/` وأضِفْ إليه `.gitkeep`. ⚠️ `discoverPages()` على مجلَّدٍ غيرِ موجودٍ **لا يفعلُ شيئاً بصمت**.
- [ ] T003 أضِفْ سطرَ اكتشافِ صفحاتِ `Learning` إلى `backend/app/Providers/Filament/AdminPanelProvider.php`، تحتَ سطرِ `Identity` وفوقَ `->pages([`. ⚠️ اقرأِ التعليقَ المكتوبَ هناكَ أصلاً قبلَ أن تكتبَ: «صفحةٌ في مجلَّدٍ لا يكتشفُه أحدٌ شاشةٌ لا وجودَ لها — لا مسارَ ولا خطأَ في أيِّ مكان، وصلاحيتُها تحرسُ العدمَ وهي تمرُّ في كلِّ اختبارٍ لها».
- [ ] T004 [P] أضِفْ مسارَ هجراتِ `Learning` إلى `backend/phpstan.neon` إن لم يكنْ مذكوراً — Larastan يقرؤُها ليُنمِّطَ خصائصَ النماذج، والجدولُ الجديدُ في `T042` يسكنُها.

---

## Phase 2: Foundational — ما تشترِكُ فيه قصّتان فأكثر

**⛔ BLOCKING**: لا تبدأْ أيَّ قصّةٍ قبلَ اكتمالِ هذه المرحلة.

- [ ] T005 [P] أضِفْ `CohortMembershipEvent::ASSIGNED = 'assigned'` في `backend/app/Modules/Learning/Models/CohortMembershipEvent.php`. **لا هجرة** — العمودُ `string(24)` على المحرّكَينِ بلا `enum` ولا قيدِ تحقّق (قِيسَ).
- [ ] T006 اشتقَّ نوعَ الحدثِ **داخلَ** `backend/app/Modules/Learning/Support/CohortMembershipWriter.php`: «لا عضويّةَ قائمة ⇒ `ASSIGNED`، وإلّا `TRANSFERRED`»، في الموضعِ الذي تُقرَأُ فيه العضويّةُ القائمةُ أصلاً داخلَ المعاملة. ⚠️ **لا تجعلْه وسيطاً يقرّرُه المُنادي**: قراءةُ «هل له عضويّةٌ؟» خارجَ المعاملةِ جوابٌ قد يتغيّرُ قبلَ الكتابة، وثابتٌ يُعلَنُ بلا كاتبٍ هو `ClassSessionStatus::Interrupted` نفسُها. ⚠️ **ويُصلِحُ هذا كذبةً قائمةً**: بابُ المدرّسِ «أضِفْ عضواً» يكتبُ اليومَ «نُقِل» على عضويّةٍ أولى.
- [ ] T007 [P] أضِفْ ملاحظةَ الإسقاطِ وسيطاً اختياريّاً إلى `MoveMember::handle()` في `backend/app/Modules/Learning/Actions/MoveMember.php` ومرِّرْها إلى `PendingTransfer::drop()`، بقيمتِها الحاليّةِ افتراضاً فلا يتغيّرُ مُنادٍ قائم. ⚠️ النصُّ اليومَ مثبَّتٌ حرفيّاً «نقلك المدرّس إلى مجموعة مباشرةً» — **وموظَّفُ الإدارةِ ليسَ المدرّس**، فهي جملةٌ كاذبةٌ يقرؤُها الطالبُ على المسارِ الجديد.
- [ ] T008 [P] أضِفْ **ثلاثةَ** أنواعِ إشعاراتٍ في `backend/app/Modules/Notifications/Support/NotificationType.php`: إسنادٌ إلى مجموعة (FR-006) · إسقاطُ طلبِ انتقال (FR-008) · باقةٌ أُنشِئَت باسمِك (FR-019)، ومعها ذراعُ كلٍّ منها في `match` التسميات. ⚠️ **ولا تمسَّ `targetsGuardians()`**: `WhatsAppDefaultsTest` يؤكّدُ العددَ **بالضبط — اقرأِ التوكيدَ في الملفِّ لا هذه الجملة**؛ وإدخالُ إشعارِ الإسنادِ فيها قرارٌ يُتَّخَذُ صراحةً مع تحريكِ الرقمِ وصلاحيّةِ الوليِّ المقابلة، لا أثراً جانبيّاً.
- [ ] T009 أضِفْ صفوفَ القوالبِ الثلاثةَ إلى `backend/database/seeders/NotificationTemplateSeeder.php` (في **الوضعَين**)، و**هجرةَ ردمٍ واحدةً** تستدعي `seedMissing()` تحتَ `backend/app/Modules/Notifications/Database/Migrations/`. ⚠️ **`seedMissing()` لا `run()`**: كلُّ قالبٍ قابلٌ للتحريرِ من `/admin`، و`updateOrCreate` في مسارِ النشرِ يمسحُ ما ضبطَه المشغِّلُ في كلِّ إصدار. ⚠️ **وهجرةٌ واحدةٌ للثلاثةِ** لا ثلاث. وإشعارٌ بلا قالبٍ **يسقطُ صامتاً**: لا خطأ، ولا سطرَ سجلّ.
- [ ] T010 أضِفْ سؤالاً ثانياً إلى `backend/app/Shared/Contracts/CohortDirectory.php` و`backend/app/Modules/Learning/Support/EloquentCohortDirectory.php`: «هل للكورسِ مجموعةٌ **صالحةٌ للإسناد**؟» (مفتوحةٌ **أو مغلقةٌ**، وغيرُ مكتملةٍ وغيرُ مؤرشَفة) — إلى جانبِ «صالحةٌ للانضمام» القائم (FR-030). ⚠️ **هما سؤالانِ لا سؤال**: المجموعةُ المغلَقةُ غيرُ المكتمِلةِ **وجهةٌ مشروعةٌ للإدارة** ولا يجوزُ للطالبِ الانضمامُ إليها — وأربعةُ قرّاءَ يسألونَ اليومَ باسمٍ واحد. وأضِفْ صيغةً **جماعيّةً** (قائمةُ معرِّفاتٍ ⇒ قائمة) في النداءِ نفسِه: شارةُ «مكتمل» على بطاقاتِ السوقِ تُسأَلُ لكلِّ صفّ، و`N+1` بالبناء.
- [ ] T011أ [P] وسِّعِ اتّحادَ أنواعِ الأحداثِ **وخريطةَ تسمياتِها** في `frontend/src/lib/cohorts.ts` بـ`assigned`. ⚠️ **الخريطةُ لها قيمةٌ احتياطيّةٌ تطبعُ المفتاحَ نفسَه**، فإضافةُ الحدثِ في الخلفيّةِ وحدَها تُبقي `tsc` أخضرَ وتطبعُ `assigned` بحروفٍ لاتينيّةٍ في سجلٍّ عربيٍّ يقرؤُه المدرّس.
- [ ] T011 [P] اختبارٌ في `backend/tests/Feature/Learning/AssignableVsJoinableTest.php`: مجموعةٌ مغلقةٌ غيرُ مكتملةٍ ⇒ **صالحةٌ للإسنادِ وغيرُ صالحةٍ للانضمام**؛ ومجموعةٌ مكتملةٌ ⇒ لا هذه ولا تلك؛ وكورسٌ بلا مجموعاتٍ ⇒ **«لا مجموعاتِ أصلاً» تُميَّزُ عن «لا صالحة»**. ⚠️ الحالةُ الأخيرةُ هي التي يسقطُ عليها `T046` إن كُتِبَ بشرطٍ واحد.

---

## Phase 3: US1 (P1) — الإدارةُ تُسنِدُ طالباً إلى مجموعة

**Goal**: شاشةٌ تُسنِد، وبابُ اعتمادٍ يُسنِدُ في اللحظةِ نفسِها.

**Independent Test**: طالبٌ مسجَّلٌ بلا مجموعة ⇒ يُسنَدُ من `/admin` ⇒ عضويّةٌ واحدة، وسطرُ تاريخٍ يُسمّي الموظَّفَ والوقت.

- [ ] T012 [P] [US1] أضِفْ فرعَ **المنصّةِ أوّلاً** إلى `backend/app/Modules/Learning/Policies/CohortPolicy.php` — **فوقَ** `belongsToCurrentWorkspace()`، على سابقةِ `OrderPolicy::platformReads()`. ⚠️ **سياقٌ يُحَلُّ ولا يطابقُ رفضٌ كذلك**: موظَّفٌ يملكُ مساحةً يُرَدُّ بـ403 عن كلِّ مجموعةٍ خارجَها — الطبقةُ الثانيةُ من طبقاتِ ٠٢٤ الخمس.
- [ ] T013 [US1] أنشِئْ `backend/app/Modules/Learning/Filament/Pages/AssignStudentToCohort.php`: **الكورسُ وسيطُ تركيبٍ مطلوبٌ لا مُرشِّحٌ افتراضُه «الكلّ»**. ⚠️ بلا ذلكَ فالحالُ الافتراضيّةُ **مسحُ كلِّ تسجيلٍ على المنصّة** مع نصفِ ضمٍّ لكلِّ صفّ. (وازِنْ أوّلاً بينَ صفحةٍ مستقلّةٍ وبينَ إضافةِ الإسنادِ إلى `CourseResource/RelationManagers/EnrollmentsRelationManager.php` — سابقةٌ قائمةٌ مربوطةٌ بالكورسِ بالبناء.)
- [ ] T014 [US1] استعلامُ «مسجَّلٌ بلا مجموعة» في الشاشةِ نفسِها: تسجيلاتُ الكورسِ + **غيابُ** عضويّةٍ مفتوحةٍ لذلك الطالبِ في ذلك الكورس. ⚠️ **الفهارسُ القائمةُ تكفي، فلا تُضِفْ فهرساً**؛ لكنْ لاحِظْ أنّ تجاوزَ النطاقِ يُسقِطُ العمودَ الأوّلَ من كلِّ فهرسٍ مركَّبٍ يبدأُ بالمساحة — فالكورسُ هو ما يجبُ أن يُربَط، وهو سببٌ ثانٍ لـ`T013`.
- [ ] T015 [US1] أعمدةُ الشاشةِ **بلا استعلامٍ داخلَ الصفّ**: خياراتُ المُنتقي تُحمَّلُ **مرّةً واحدةً** عندَ التركيب؛ و«له مجموعةٌ / أيّها» باستعلامٍ فرعيٍّ مرتبطٍ — **لا `with()`**: لا علاقةَ بينَ `Enrollment` وجدولِ العضويّاتِ والمفتاحُ مركَّب، فالضمُّ المُسبَقُ يردُّ صفوفاً خاطئةً بصمت. ⚠️ **واسمُ الطالبِ يُحمَّلُ بلا تقييدِ أعمدة**: الاسمُ سمةٌ مشتقّةٌ من عمودَينِ آخرَين، وتقييدُ الأعمدةِ يرسمُ **اسماً فارغاً** لكلِّ صفٍّ بلا خطأ — شُحِنَ في ستّةِ مواضعَ في هذه الشجرة. واقرأِ الأسماءَ جملةً بالدالّةِ الجماعيّةِ القائمةِ في دليلِ المجموعات.
- [ ] T016 [US1] `canAccess()` على الصفحةِ تسألُ `COHORTS_ASSIGN` **صراحةً**، و**كلُّ كتابةٍ** تسألُ الصلاحيّةَ ثانيةً قبلَ نداءِ الفعل، ومعها **التوثيقُ الثنائيُّ** على سابقةِ اعتمادِ الطلبِ (FR-009). ⚠️ صفحةُ Filament **لا تستدعي سياسةً إطلاقاً**، و`/admin` لا يمرُّ من وسيطِ التوثيقِ الثنائيّ. ⚠️ **والشاشةُ مرئيّةٌ لكلِّ موظَّفِ منصّةٍ إن لم تُحرَس**: قَبولُ اللوحةِ هو «مديرُ منصّةٍ **أو أيُّ صفٍّ في `platform_staff`**» — ومسؤولُ الامتثالِ منهم، ولا يحملُ من هذا شيئاً.
- [ ] T017 [US1] كلُّ قراءةٍ في الشاشةِ تُعلِنُ تجاوزَ النطاقِ **ومعها تعليقُها**، **ويُكرَّرُ التجاوزُ داخلَ كلِّ ضمٍّ مُسبَق** — التجاوزُ لكلِّ نموذجٍ لا للاستعلامِ كلِّه (السابقةُ المكتوبة: `GrantCreditSubscription`). ⚠️ وأصلِحْ `backend/app/Filament/Resources/EnrollmentResource.php::getEloquentQuery()` في المرورِ نفسِه — **بلا تجاوزٍ اليوم**، فيرى الموظَّفُ قائمةً قصيرةً صامتةً واسمَ مساحةٍ فارغاً لكلِّ صفٍّ غريب.
- [ ] T018 [US1] `AdminAssignsCohortTest` في `backend/tests/Feature/Learning/`: **مساحتا عملٍ وموظَّفٌ يملكُ إحداهما** (`last_workspace_id` مضبوطٌ عمداً) يُسنِدُ في **الأخرى**. ⚠️ سطرٌ واحدٌ كهذا كشفَ طبقاتِ ٠٢٤ الخمسَ كلَّها؛ وتركيبةٌ بموظَّفٍ لا مساحةَ له — وهي كلُّ تركيبةٍ قائمةٍ اليوم — **تمرُّ على البناءِ المعطوب**.
- [ ] T019 [US1] `AdminAssignPermissionTest` في `backend/tests/Feature/Learning/`: **حدّانِ يُقاسانِ لا واحد** — (١) حاملُ **أعلى دورِ مستأجرٍ** ⇒ 403 (نصُّ الدستور)؛ (٢) **موظَّفُ منصّةٍ ليسَ مديرَها** ⇒ 403. ⚠️ **واختبارُ سماحٍ فاعلُه مديرُ منصّةٍ لا يبرهنُ شيئاً**: `Gate::before` يُمرِّرُه فوقَ كلِّ سياسة، فيمرُّ الاختبارُ والصلاحيّةُ محذوفةٌ من الشجرة. وهو `taxonomy.manage` من جهةِ السماح.
- [ ] T020 [US1] وسِّعْ `ApproveOrder::handle()` في `backend/app/Modules/Payments/Actions/ApproveOrder.php` ليأخذَ المجموعةَ المختارة، **واحرسْ في الفعلِ لا في الشاشة** (FR-016ب): لاعتمادِ الطلبِ بابانِ — زرُّ اللوحةِ والمسارُ البرمجيُّ — والتعليقُ في الملفِّ نفسِه يقولُها.
- [ ] T021 [US1] **طالِبْ بالمقعدِ قبلَ قبضِ المال** (FR-024أ): مُطالبةٌ ذرّيّةٌ بتحديثٍ شرطيٍّ على عدّادِ المجموعة، فإن فاتَ المقعدُ **يُرفَضُ الاعتمادُ ويبقى الطلبُ معلَّقاً** بجملةٍ يقرؤُها الموظَّفُ تقولُ إنّ المقعدَ راح (قرارُ المالك). ⚠️ **وقراءةُ `isJoinable()` ليست مُطالَبة**: موظَّفانِ يمرّانِ منها معاً ويقبضانِ معاً وأحدُ الطالبَينِ بلا مجموعة. ⚠️ **ولا `lockForUpdate()`** — لا أثرَ له على SQLite، فاختبارٌ مبنيٌّ عليه يمرُّ محلّيّاً ولا يبرهنُ شيئاً عن MySQL.
- [ ] T021أ [US1] ⛔ **المقعدُ يُطالَبُ به مرّةً واحدةً في المسارِ كلِّه.** `CohortMembershipWriter::open()` **يزيدُ العدّادَ بنفسِه** — فإن طالبَ `T021` بالمقعدِ ثمّ كتبَ `T023` العضويّةَ عبرَه، **زادَ العدّادُ مرّتَينِ لكلِّ طلبٍ مُعتمَد**: السعةُ تتسرّبُ مقعداً في كلِّ اعتماد، والزيادةُ الثانيةُ قد **ترفضُ** بـ«مكتملة» **بعدَ أن قُبِضَ المال**. فأضِفْ إلى الكاتبِ وسيطاً يقولُ إنّ المقعدَ مُطالَبٌ به سلفاً، ويُمرَّرُ من `AssignCohortFromOrder` وحدَه فيتخطّى الزيادةَ ولا شيءَ غيرَها (الإغلاقُ والتحريرُ والفهرسُ الفريدُ كما هي). ⚠️ **وحالةُ العُطلِ تُكتَبُ لا تُفترَض**: مقعدٌ مُطالَبٌ به ومستمِعٌ ماتَ = مقعدٌ محجوزٌ بلا عضويّة — عائلةُ «حالةٌ عالقةٌ لا يكنسُها شيء». تُغطّى بإعادةِ محاولةِ المستمِعِ المُصطفِّ وبجدولِ المهامِّ الفاشلة، **وبشرطِ أن تكونَ إعادةُ النداءِ بلا أثرٍ إن كانتِ العضويّةُ مكتوبةً أصلاً**.
- [ ] T022 [US1] احفظِ المجموعةَ المختارةَ **على الطلبِ** في `backend/app/Modules/Payments/Data/SubscriptionIntent.php` وحقلِ البياناتِ الذي يقرؤُه على `backend/app/Modules/Payments/Models/Order.php` — نمطٌ قائمٌ على الطلبِ نفسِه، **ولا جدولَ جديد**.
- [ ] T023 [US1] `backend/app/Modules/Learning/Listeners/AssignCohortFromOrder.php`: على حدثِ **إنشاءِ التسجيل**، يقرأُ الاختيارَ المحفوظَ ويكتبُ العضويّةَ بفاعلِ الموظَّفِ وحدثِ `ASSIGNED`. ⚠️ **على إنشاءِ التسجيلِ لا على اعتمادِ الدفع**: الثاني مهمّةٌ مُصطفّةٌ ثانيةٌ **تتسابقُ** مع كاتبِ التسجيلِ بلا ترتيبٍ مضمونٍ بينَهما. ⚠️ ومستمِعٌ في `Learning` **لا يستوردُ `Payments`** ولا يلمسُ جداولَها — `ContextIsolationTest` يُسقِطُ البناءَ على ذلك.
- [ ] T024 [US1] مُنتقي المجموعةِ في `backend/app/Modules/Payments/Filament/Resources/OrderResource.php` — **قراءةٌ متجاوِزةُ النطاقِ مع إعادةِ شرطِ الكورسِ بيدِك**. ⚠️ **هذه شاشةٌ قائمةٌ لا جديدة**، وهي الطبقةُ السادسةُ لعيبِ ٠٢٤: موظَّفٌ يملكُ مساحةً يفتحُ طلبَ مساحةٍ أخرى فيجدُ المُنتقيَ **فارغاً** — ولأنّ الاختيارَ مطلوبٌ، **لا يستطيعُ اعتمادَ طلبٍ مدفوعٍ إطلاقاً**.
- [ ] T025 [US1] أرسِلْ إشعارَ الإسنادِ (FR-006) من `backend/app/Modules/Notifications/Listeners/NotifyStudentCohortAssigned.php` (مستمِعٌ على فتحِ العضويّة، مربوطٌ في `backend/app/Modules/Notifications/NotificationsServiceProvider.php`)، بمواعيدِ المجموعة. ⚠️ **لا مستمِعَ إشعاراتٍ على هذا الحدثِ اليوم** — الوحيدُ عليه يُحرِّرُ المقاعد، و`MoveMember` لا يُبلِّغُ أحداً. فهو مستمِعٌ جديدٌ لا «كما يُبلَّغُ اليوم».
- [ ] T026 [US1] أرسِلْ إشعارَ إسقاطِ طلبِ الانتقالِ (FR-008) من `backend/app/Modules/Learning/Support/PendingTransfer.php` عبرَ حدثٍ يلتقطُه `backend/app/Modules/Notifications/Listeners/`. ⚠️ السببُ يُكتَبُ اليومَ في جدولٍ **قارئاه مساران إداريّان للمدرّس** — فالجملةُ موجودةٌ ولا يقرؤُها الطالبُ أبداً.
- [ ] T027 [US1] بابُ الاشتراك: اكتبِ العضويّةَ الناتجةَ عن الاعتمادِ **بفاعلِ الموظَّفِ المعتمِدِ وحدثِ `ASSIGNED`** في `backend/app/Modules/Payments/Listeners/ActivateSubscription.php` (قرارُ المالك). ⚠️ **بدونَها SC-002 يفشلُ دائماً مهما صحَّ التنفيذ**: السجلُّ يقولُ «انضمَّ الطالب» عن عضويّةٍ أنشأَها اعتمادُ الإدارة. **والصفحةُ العامّةُ لا تُمَسّ** — اختيارُ الطالبِ لمجموعتِه عندَ الاشتراكِ باقٍ بقرارِ المالك.
- [ ] T028 [P] [US1] `ApproveWithCohortTest` في `backend/tests/Feature/Payments/`: طلبانِ على آخرِ مقعدٍ يُعتمَدانِ تباعاً ⇒ **الأوّلُ يمرُّ، والثاني يُرفَضُ ويبقى معلَّقاً، ولا يُقبَضُ منه مال**؛ وطلبٌ لحصّةٍ فرديّةٍ أو لكورسٍ بلا مجموعاتٍ ⇒ **يُعتمَدُ بلا مجموعة** (FR-016). ⚠️ الحالةُ الثانيةُ هي التي تمنعُ «طلباً مدفوعاً لا يُعتمَدُ أبداً».
- [ ] T029 [P] [US1] `AdminScreenBudgetTest` في `backend/tests/Feature/Learning/`: تركيبتانِ بحجمَينِ مختلفَين، والكلفةُ **لا تنمو بالصفوف**، **ويُؤكَّدُ أنّ الحقولَ حاضرةٌ كذلك**. ⚠️ اختبارٌ يقيسُ الاستعلاماتِ وحدَها **يقرأُ إسقاطَ الضمِّ المُسبَقِ تحسيناً**: الحقلُ يغيبُ، والصفحةُ أرخصُ باستعلام، والقائمةُ بلا أسماء.

**Checkpoint**: الإسنادُ يعملُ من البابَين — الشاشةِ والاعتماد — قبلَ أن يُغلَقَ أيُّ باب.

---

## Phase 4: US2 (P2) — يُغلَقُ الانضمامُ الذاتيُّ ويُفتَحُ المنهج

**⛔ لا تبدأْ قبلَ `Checkpoint` مرحلةِ US1.**

**Independent Test**: طالبٌ يُرسِلُ الانضمامَ مباشرةً ⇒ لا عضويّةَ ولا مقعدَ ولا سطرَ تاريخ؛ ومنهجُه مفتوحٌ بجملةٍ تقولُ مَن يُسنِد.

- [ ] T030 [US2] احذِفْ مسارَ الانضمامِ من `backend/app/Modules/Learning/routes/api.php` و`CohortController::join()`. ⚠️ **و`JoinCohort` الفعلُ يبقى**: مُنادِيه `ActivateSubscription` — بابُ الإدارةِ نفسُه. (وكانَ مكتوباً في `research.md` أنّ `ApproveOrder` يستدعيه، وهو خطأٌ صُحِّحَ بالقياس.)
- [ ] T031 [P] [US2] `SelfJoinClosedTest` في `backend/tests/Feature/Learning/`: **التوكيدُ على الكتابةِ لا على ٤٠٤** — صفرُ عضويّات، صفرُ مقاعدَ مستهلَكة، صفرُ صفوفِ تاريخ. ⚠️ ولا اختبارَ واحداً يمسُّ هذا المسارَ اليوم (قِيسَ: صفرُ نداءاتٍ عليه في الحزمةِ كلِّها)، فهذا الملفُّ هو الحارسُ الوحيد.
- [ ] T032 [US2] `CohortGate::locks()` ⇒ `false` في `backend/app/Modules/Learning/Support/CohortGate.php`، و`message()` تتغيّرُ إلى «لم تُسنَدْ بعد، والإدارةُ هي مَن يُسنِد». ⚠️ **و`describe()` و`joinableCohortsExist()` تبقيان**: الحمولةُ هي التي تُنتِجُ الجملة، والشاشةُ تُميِّزُ «مجموعاتٌ متاحةٌ ولستَ فيها» من «لا مجموعةَ أصلاً» — وهما جملتانِ مختلفتانِ للطالب.
- [ ] T033 [US2] احذِفْ فرعَي `NO_COHORT` **كلَيهما** من `backend/app/Modules/Learning/Support/LessonGate.php` — الدرسُ المفردُ **ومشيُ الشجرة** — ومعهما التعليقانِ اللذانِ يشرحانِ ترتيباً صارَ كذباً، وثابتُ `LessonAccess::NO_COHORT`. ⚠️ **حذفُ واحدٍ وتركُ الآخرِ يُبقي رمزاً بقارئٍ وبلا كاتب** — وهو الشكلُ الذي عاشَ به `ClassSessionStatus::Interrupted` طوراً كاملاً وكلُّ قارئٍ يظنُّه منفَّذاً.
- [ ] T034 [US2] `backend/tests/Feature/Learning/CohortGateSafetyValveTest.php`: **ثلاثٌ تنقلبُ واثنتانِ تُحذَفانِ بجملةٍ تقولُ لماذا** (نتيجتُهما لم تعُدْ قابلةً للإنتاجِ إطلاقاً)، والحالةُ التي تؤكّدُ كلمةَ «مدرّسك» في جملةِ البوّابةِ تتحرّكُ إلى «الإدارة». ⚠️ **وهذا أوجبُ ما يُراجَعُ في الطلبِ كلِّه**: اختبارٌ حارسٌ يُعدَّلُ في الطلبِ الذي يُغيِّرُ ما يحرسُه هو الشكلُ الذي يمرُّ منه عطبٌ حقيقيٌّ باسمِ «تحديثِ التوكيدات».
- [ ] T035 [P] [US2] `frontend/src/lib/cohorts.ts`: احذِفْ `join()`. و`frontend/src/components/courses/CohortPicker.tsx` يصيرُ **عرضاً بلا زرّ**، و**يقرأُ جملةَ البوّابةِ من الحمولة** ولا يُعيدُ اشتقاقَ «مفتوحةٌ وغيرُ مكتملة» بـTypeScript — إملاءانِ لسؤالٍ واحدٍ هو ما جعلَ تسجيلاً مدفوعاً غيرَ قابلٍ للفتحِ في ٠١٨.
- [ ] T036 [P] [US2] `frontend/src/components/courses/CohortPicker.test.tsx` — **سبعُ حالاتٍ تقودُ زرَّ الانضمام**، تنقلبُ في الطلبِ نفسِه. ⚠️ الملفُّ كانَ غائباً عن قائمةِ الخطّة؛ وبدونَه `npm test` أحمرُ عندَ أوّلِ حذف.
- [ ] T037 [P] [US2] صفحةُ `frontend/src/app/(app)/(shell)/enrollments/[course]/`: جملةُ «لم تُسنَدْ بعد» ظاهرةً فوقَ المنهجِ **المفتوح**. ⚠️ رفضٌ صامتٌ أو قائمةٌ بلا تفسيرٍ **يُقرَآنِ عُطلاً** (FR-012).

---

## Phase 5: US3 (P3) — باقةٌ تُنشِئُها الإدارةُ بالنيابة

**Independent Test**: موظَّفٌ يُنشئُ باقةً لمدرّسٍ منصوصٍ عليه ⇒ تظهرُ في باقاتِ ذلكَ المدرّسِ ويصلُه إشعار.

- [ ] T038 [US3] ⚠️ أضِفْ تجاوزَ النطاقِ إلى استعلامِ الكورسِ في `SavePlan::resolveCoverage()` (`backend/app/Modules/Payments/Actions/SavePlan.php`)، **مع إبقاءِ شرطِ مساحةِ المدرّسِ الصريحِ وتعليقٍ يشرح**. الشرطُ الصريحُ هو الحارسُ؛ والنطاقُ يُضيفُ شرطاً ثانياً بمساحةِ **الموظَّفِ** ⇒ صفرُ صفوفٍ ⇒ «هذا الكورس غير موجود عندك» عن كورسِ **كلِّ** مدرّس. (وكُتِبَ في `research.md` أنّ فخَّ ٠٢٤ لا يعضُّ هنا، وهو خطأٌ صُحِّح.)
- [ ] T039 [US3] `backend/app/Modules/Payments/Actions/CreatePlanForTeacher.php`: يلفُّ `SavePlan` ثمّ `SetPlanPrice` ويُطلِقُ إشعارَ المدرّسِ (المبدأُ II — الترتيبُ وما يحدثُ إن فشلَ الثاني قرارُ أعمال). ⚠️ **ويرفضُ قبلَهما**: مساحةً ليسَ فيها عضوٌ بدورٍ تدريسيّ، ومدرّساً **غادرَ المنصّةَ** (الدليلُ قائمٌ وله قارئٌ واحدٌ اليوم). ⚠️ **ولا يُمرَّرُ السعرُ داخلَ بياناتِ `SavePlan`**: يرفضُه هناكَ مَن لا يملكُ صلاحيّةَ التسعير، ولا يُكتَبُ العمودُ منه أصلاً — فيُفحَصُ في موضعٍ ويُسقَطُ صامتاً في آخر.
- [ ] T040 [US3] `PlanResource` في `backend/app/Modules/Payments/Filament/Resources/`: `canCreate()` خلفَ صلاحيّةِ التسعيرِ نفسِها، و**نموذجُ إنشاءٍ كاملٌ بحقلِ مدرّسٍ مطلوب**. ⚠️ حقولُ المدرّسِ في النموذجِ القائمِ **عرضٌ لا تحرير** بنصِّ وصفِها، فهي ليست «حقلاً يُضاف» بل نموذجٌ جديد. ⚠️ **ولا صلاحيّةَ ثانية**: مَن يُسعِّرُ هو مَن يُنشئُ بالنيابة، واختراعُ ثانيةٍ جوابٌ ثانٍ لسؤالٍ له جواب.
- [ ] T041 [P] [US3] `AdminCreatesPlanForTeacherTest` في `backend/tests/Feature/Payments/`: **مساحتا عملٍ وموظَّفٌ يملكُ إحداهما** يُنشئُ باقةَ **كورسٍ واحد** للأخرى ⇒ تنجح، والمدرّسُ يُبلَّغ. ⚠️ هذه هي الحالةُ التي يسقطُ عليها `T038`، ولا تراها تركيبةٌ بمساحةٍ واحدة.

---

## Phase 6: US4 (P4) — الكورسُ المكتمِلُ لا يُباع، ويُفتَحُ له دَور

⚠️ **أكبرُ بندٍ في المرحلة** — أكبرُ من US1 وUS2 مجتمعتَين، ونقطةُ تقسيمٍ نظيفةٌ إن أُريدَ شحنُ ما قبلَها أوّلاً.

**Independent Test**: كورسٌ كلُّ مجموعاتِه مكتملة ⇒ لا طلبَ شراءٍ يُقبَل، ويُقبَلُ تسجيلٌ في الدَّورِ بلا مال؛ وكورسٌ بلا مجموعاتٍ ⇒ الشراءُ يعمل.

- [ ] T042 [US4] هجرةُ `course_waitlist_entries` تحتَ `backend/app/Modules/Learning/Database/Migrations/`: `uuid` · `student_user_id` · `course_id` · `workspace_id` · `invited_at` · `closed_at` · **`closed_slot` بقيمةٍ افتراضيّةٍ صفر** · `timestamps()`، و`unique(student_user_id, course_id, closed_slot)` **و`index(course_id, closed_slot, created_at)`**. ⚠️ **الفهرسُ الثاني ليسَ زينة**: الفريدُ طالبٌ-أوّلاً وقراءةُ الشاشةِ كورسٌ-أوّلاً ⇒ مسحُ جدولٍ وفرزٌ في الذاكرةِ على الجدولِ الأسرعِ نموّاً في المرحلة. ⚠️ **ولا عمودَ `position`**: رقمُ موضعٍ مخزَّنٌ يحتاجُ إعادةَ ترقيمٍ عندَ كلِّ خروج. ⚠️ **و`closed_slot` عمودٌ يُكتَبُ لا فهرسٌ جزئيّ** — MySQL لا يملكُ فهارسَ جزئيّةً أصلاً، و`NULL` لا يساوي `NULL` على المحرّكَين.
- [ ] T043 [P] [US4] `backend/app/Modules/Learning/Models/CourseWaitlistEntry.php` — **جسر**: `BelongsToWorkspace` + `HasUuid`، ومعه إمّا `timestamps()` أو إعلانُ أنّ لا عمودَ تحديثٍ له (Eloquent يكتبُه عندَ الإدراجِ فتفشلُ الجملةُ على المحرّكَين). وأضِفْ حالتَه إلى `backend/tests/Feature/Tenancy/WorkspaceIsolationTest.php` **في الطلبِ نفسِه** (شرطُ المبدأ I). ⚠️ **والتصنيفُ لا يحرسُ الطالب**: نطاقُ المستأجِرِ **خاملٌ** لمن هو عضوٌ في لا مساحة — والحراسةُ شرطٌ صريحٌ بصاحبِ الصفّ.
- [ ] T044 [P] [US4] أضِفْ صفَّ صنفِ بياناتٍ لـ`course_waitlist_entries` في `backend/database/seeders/DataCategorySeeder.php` + هجرةَ ردمِه تحتَ `backend/app/Modules/Compliance/Database/Migrations/`، ومَشياتِه الثلاثَ (تصديرٌ · انتهاءٌ · حذف) في `backend/app/Modules/Learning/Support/LearningPersonalData.php`. ⚠️ **حارسُ التغطيةِ لكلِّ وحدةٍ لا يرى جدولاً جديداً داخلَ وحدةٍ مسجَّلة** — `announcements.author_user_id` شُحِنَ هكذا و١٩١٦ اختباراً بقيَت خضراء.
- [ ] T045 [US4] `JoinWaitlist` في `backend/app/Modules/Learning/Actions/` + `JoinWaitlistRequest` + مسارُ `POST /courses/{course}/waitlist` خلفَ **حدٍّ مُسمّى** (سابقةُ حدِّ كتابةِ المجموعات؛ والحدُّ السطريُّ ممنوعٌ في هذه الشجرة). **والمُسجَّلُ: الطالبُ نفسُه أو وليٌّ عن ابنٍ تُثبِتُه علاقةُ الوليّ** (FR-026أ، قرارُ المالك). ⚠️ **و`workspace_id` يُكتَبُ من الكورسِ صراحةً**: سياقُ الطالبِ `null` دائماً فالملءُ التلقائيُّ لا يقعُ أبداً. ⚠️ **ولا ربطَ ضمنيّاً لصفِّ الدَّور**: يُحَلُّ المُعرِّفُ داخلَ الفعلِ **بعدَ** سؤالِ الملكيّة. ⚠️ **ولا موضعَ في الحمولة**: رقمٌ يُرسَلُ يُقرَأُ حجزاً، وFR-027 تمنعُه.
- [ ] T046 [US4] حارسُ FR-023 في `backend/app/Modules/Payments/Actions/CreateOrder.php`: **شرطانِ لا شرط** — «له مجموعاتٌ أصلاً» **و**«لا واحدةَ صالحةً للإسناد» (من `T010`). ⚠️ بشرطٍ واحدٍ **يُمنَعُ بيعُ كلِّ محتوًى مسجَّل**، وهي FR-025 مقلوبةً على مسارِ المال بحارسٍ من سطرٍ واحدٍ يبدو صحيحاً.
- [ ] T047 [US4] ⚠️ **والبابُ الثاني**: الحارسُ نفسُه في `backend/app/Modules/Learning/Http/Controllers/EnrollmentController.php` — الكورسُ المجّانيُّ **لا يمرُّ من إنشاءِ الطلبِ إطلاقاً**، فبدونِه كورسٌ مجّانيٌّ مكتمِلٌ يقبلُ عدداً بلا حدّ.
- [ ] T048 [US4] `InviteFromWaitlist` في `backend/app/Modules/Learning/Actions/`: **يُطالِبُ كلَّ صفٍّ بتحديثٍ شرطيٍّ يشترطُ أنّه لم يُدعَ بعد**، ويعدُّ الصفوفَ المتأثّرة. ⚠️ قراءةُ «الأوائلِ N» ثمّ كتابتُها هي القراءةُ-ثمّ-الكتابةُ التي دفعَ ثمنَها هذا المستودعُ خمسَ مرّات: موظَّفانِ يفتحانِ مجموعتَينِ معاً يقرآنِ القائمةَ نفسَها ويدعوانِ الأشخاصَ أنفسَهم. ⚠️ **وعددُ المقاعدِ يُحسَبُ في PHP لا في SQL**: طرحُ عمودَينِ بلا إشارةٍ **يرمي ERROR 1690 على MySQL** ولا يُنتِجُ شيئاً على SQLite — فلا اختبارَ محلّيٌّ يراه، والسعةُ قد تكونُ أقلَّ من العددِ الحاليِّ لأنّ تعديلَها لا يشترطُ أرضيّة.
- [ ] T049 [P] [US4] `LeaveWaitlistOnEnrolment` في `backend/app/Modules/Learning/Listeners/`: على حدثِ إنشاءِ التسجيل، **تحديثٌ شرطيٌّ واحدٌ** يختِمُ الصفَّ (FR-028)، يخدمُه الفهرسُ الفريدُ بالضبط. ⚠️ **لا استثناءً وقتَ القراءة**: ذاكَ استعلامٌ فرعيٌّ مرتبطٌ في كلِّ فتحِ صفحة، **ولا يُختَمُ الصفُّ أبداً** فيفقدُ `closed_at` معناه.
- [ ] T050 [US4] `backend/app/Modules/Learning/Filament/Pages/CourseWaitlist.php`: قائمةٌ بترتيبِ **الوقتِ ثمّ المُعرِّف**، والرقمُ من **فهرسِ الصفِّ في الصفحة** لا من عدٍّ لكلِّ صفّ. ومعها `canAccess()` والتوثيقُ الثنائيُّ كما في `T016`، وتجاوزُ النطاقِ كما في `T017`. ⚠️ **وتقولُ الشاشةُ صراحةً إنّ الدَّورَ لا يحجزُ مقعداً ولا يَعِدُ به** (FR-027). ⚠️ **وهي شاشةٌ تحملُ أسماءَ طلابٍ عبرَ كلِّ المساحات** — فحراستُها ليست شكليّة.
- [ ] T051 [P] [US4] صفحةُ الكورسِ العامّةُ (`frontend/src/app/(public)/courses/[slug]/page.tsx` و`frontend/src/components/marketplace/CohortList.tsx`) وقارئُها `backend/app/Modules/Marketplace/Actions/Public/ReadPublicCourse.php`: **لا زرَّ شراءٍ** إن اكتملَ الكورسُ، وجملةُ اكتمال، وزرُّ الدَّور. **والقرارُ يُقرَأُ من الحمولةِ** المبنيّةِ من مجموعاتِ الكورسِ المُحمَّلةِ أصلاً في القارئِ العامّ — لا استعلامَ ثانياً، ولا اشتقاقَ في TypeScript، **والحقلُ يُضافُ إلى قائمةِ الحقولِ المسموحةِ صراحةً**. ⚠️ وكلُّ اسمِ معدودٍ عربيٍّ جديدٍ يمرُّ من مُساعِدِ العدِّ **بصيغةِ `other` مصرَّحاً بها**.
- [ ] T052 [P] [US4] `CourseWaitlistTest` في `backend/tests/Feature/Learning/`: الترتيبُ — **ومنه تسجيلانِ في الثانيةِ نفسِها** — · الخروجُ عندَ التسجيلِ · **لا دعوةَ مرّتَين** تحتَ مُطالبتَينِ متزامنتَين · وكورسٌ بلا مجموعاتٍ **يُباعُ** · وكورسٌ مجّانيٌّ مكتمِلٌ **يُرفَض**. ⚠️ الحالةُ قبلَ الأخيرةِ هي التي تسقطُ إن كُتِبَ `T046` بشرطٍ واحد، والأخيرةُ هي التي تسقطُ إن نُسِيَ `T047`.

---

## Phase 7: Polish — ما لا تكتشفُ غيابَه بوّابة

- [ ] T053 [P] `docs/README.md`: الصلاحيّةُ الجديدةُ · المسارُ المحذوفُ (مذكورٌ اليومَ حيّاً بحدِّه) · مسارُ الدَّور. و`docs/erd.md`: الجدولُ الجديد. ⚠️ سيرُ العملِ في الدستورِ يشترطُهما، **ولم يذكرْهما أيُّ مستندٍ من الخمسةِ قبلَ المراجعة**.
- [ ] T054 راجِعْ `specs/021-course-cohort-hub/spec.md` مرّةً أخيرة: **ثمانيةُ مواضعَ عُدِّلَت** (FR-028 الأمُّ · FR-028هـ · FR-006 · Q1 · الافتراضانِ ٢ و٥ · سيناريوها US3). أيُّ جملةٍ بقيَت تصفُ انضماماً ذاتيّاً **متطلَّبٌ حيٌّ يناقضُ ما شُحِن**.
- [ ] T055 البوّابات — **مرّةً واحدةً في آخرِ المرحلة**، لا بعدَ كلِّ ملفّ:
  - `php vendor/bin/pest tests/Feature/Learning tests/Feature/Payments tests/Feature/Tenancy tests/Feature/Notifications`
  - `./vendor/bin/pint --test` ثمّ `./vendor/bin/phpstan analyse`
  - `npx tsc --noEmit` ثمّ `npm test`

  ⚠️ **ولا تُشغَّلُ حزمتا pest معاً** — تتشاركانِ قرصَ الاختبارِ فتصنعانِ فشلاً كاذباً.

---

## Dependencies

```text
Setup (T001–T004)
   ↓
Foundational (T005–T011)   ⛔ حاجزٌ لكلِّ القصص
   ↓
US1 (T012–T029)   ⛔ يسبقُ US2 — لا يُغلَقُ بابٌ قبلَ بناءِ بديلِه
   ↓
US2 (T030–T037)

US3 (T038–T041)   ← مستقلّةٌ عن US1/US2، تُشحَنُ متى شِيءَ بعدَ Foundational
US4 (T042–T052)   ← تعتمدُ على T010 وحدَها من Foundational
   ↓
Polish (T053–T055)
```

## MVP

**US1 وحدَها.** الإسنادُ يعملُ، والانضمامُ الذاتيُّ ما زالَ مفتوحاً — **وهذا شحنٌ آمنٌ بذاتِه**: بابٌ زائدٌ لا عطب. و`US2` هي التي تحتاجُ `US1` لا العكس.

## التوازي

- `T001` · `T002` · `T004` معاً.
- `T005` · `T007` · `T008` معاً؛ ثمّ `T006` بعدَ `T005`، و`T009` بعدَ `T008`.
- `T028` · `T029` · `T031` · `T036` · `T041` · `T052` — اختباراتٌ في ملفّاتٍ مختلفة.
- `T043` · `T044` معاً بعدَ `T042`.
