# Tasks: ٠٣٥ — الرصيدُ بالحصص

**Input**: `specs/035-session-credit-attendance/` — spec.md · plan.md · research.md (R1–R18) · data-model.md · contracts/README.md · quickstart.md

**Tests**: مطلوبةٌ صراحةً. ⚠️ **وخطرُ هذه الشحنةِ الأوّلُ ليسَ اختباراً يرسب، بل اختباراً يمرُّ أخضرَ على صفر** — ولذلك الطورُ الثاني كلُّه تجهيزاتٌ وحرّاس، **وكلُّ مهمّةِ اختبارٍ هنا تقولُ صراحةً ما الذي يجعلُها ترسبُ لو لم تُنفَّذِ الميزةُ أصلاً**.

**Organization**: بالقصص، مع طورٍ مُعيقٍ قبلَها. ⚠️ **والقصصُ الثلاثُ تُشحَنُ معاً أو لا تُشحَن** (FR-021). التقسيمُ **ترتيبُ بناءٍ ومراجعةٍ لا خطّةُ إصدار**.

---

## مراجعةُ الوكلاءِ الستّة (2026-09-13) — ما تغيَّرَ في هذه النسخة

ستّةُ وكلاءَ قرؤوا المهامَّ على الشجرة. **خمسُ نتائجَ لقيَها أكثرُ من وكيلٍ من بابَينِ مختلفَين**، وقرارانِ رفعاهما إلى المالكِ وحُسِما:

- ⛔ **العذر**: الحالةُ `Excused` لا كاتبَ لها غيرُ `OverrideAttendance`، ونافذتُه `ends_at + attendanceEditWindowHours` — **بعدَ** تثبيتِ الحكم. القرار: **الاثنان** — عمودٌ على الحجزِ قبلَ القفلِ هو الطريقُ العاديّ، والعكسُ في نافذةِ التعديلِ استثناءٌ بصلاحيّة.
- ⛔ **أجرُ المدرّس**: `attended_seats` لا يصلحُ للأجرِ لأنّ المتخلّفَ الصامتَ يُخصَمُ منه **والمدرّسُ يأخذ** (FR-008د · صفّ ٣). القرار: **عمودان** — `attended_seats` للعرضِ و`charged_seats` للأجر.

وستَّ عشرةَ مهمّةً جديدةً دخلَت، وسبعُ مهامٍّ كانَ فيها ادّعاءٌ عن الشجرةِ **لم ينجُ من `grep`**، مكتوبةٌ في مواضعِها.

---

## نطاقٌ سقطَ قبلَ كتابةِ هذه المهامّ (2026-09-12)

⛔ **قسمُ المقامِ والشهادةِ كلُّه خارجُ الشحنة**. **لا مهمّةَ واحدةً هنا تلمسُ** `CourseProgress` · `ResyncCourseProgress` · `EloquentProgressImpact` · `PreviewPublishImpact` · `Lesson::scopeProgressEligible()` · `CourseDuration`. و**T071 يقيسُ ذلك**. ⚠️ **تصحيحٌ**: `Lesson::accessTo()` لا وجودَ له؛ الدالّةُ `Enrollment::accessTo()` وهي سطرٌ يفوّضُ إلى `LessonGate::for()` — وT036 **يُضيِّقُها** بفرعِ رفضٍ جديد. التضييقُ مشروع، والحارسُ الذي يُثبِتُ أنّه لم يتّسعْ هو T071.

---

## Phase 1: المخطَّط — تسعةُ ترحيلاتٍ لا ثمانية

**Purpose**: الجداولُ والأعمدةُ أوّلاً. لا منطقَ هنا.

- [X] T001 [P] أضِفْ `held_credits` (`integer` · NOT NULL · default 0) إلى `credit_balances` في ترحيلٍ تحت `backend/app/Modules/Payments/Database/Migrations/` — ⚠️ **مُوقَّعٌ لا غيرُ مُوقَّع**، والسببُ بجانبَه: التعبيرُ الذي يرفعُ `ERROR 1690` على MySQL هو تعبيرُ **T053** لا تعبيرُ الأرضيّةِ القائمة — `remaining_credits - held_credits - 1 >= …` عندَ رصيدٍ صفرٍ يُقيَّمُ غيرَ موقَّعٍ فينفجر، **على أوّلِ حجزٍ لأيِّ طالبٍ برصيدٍ فارغ**، وSQLite لا تُظهِرُه أبداً. الجيرانُ الثلاثةُ في `2026_08_08_000500_create_credit_tables.php:81-86` موقَّعونَ لهذا السببِ نفسِه.
- [X] T002 [P] أنشِئْ جدولَ `credit_holds` في ترحيلٍ تحت `backend/app/Modules/Payments/Database/Migrations/` بأعمدةِ data-model §١ — ⚠️ **`hold_seq` (`unsignedInteger` NOT NULL default 0) ثالثاً في المفتاحِ الفريد**: المقعدُ يُحيا شرعاً بعدَ إطلاقِه، ومفتاحٌ بعمودَينِ يُنتِجُ إمّا حصّةً ببلاش أو خطأَ خادمٍ على إعادةِ حجزٍ مشروعة.
- [X] T003 [P] أضِفْ ثلاثةَ فهارسَ لـ`credit_holds` في نفسِ ترحيلِ T002: **`index(class_session_id, settled_at)` أوّلاً** · `index(credit_balance_id, settled_at)` · `index(student_user_id, settled_at)` — ⚠️ **تصحيحٌ من المراجعة**: الفهرسُ الأوّلُ في النسخةِ السابقةِ كانَ `(settled_at, credit_balance_id)`، **وتسعةٌ من عشرةِ قراءاتٍ في هذه الشحنةِ مفتاحُها الحصّةُ لا الرصيد** (الختمُ عندَ الإغلاقِ · إلغاءُ الحصّة · التجميد · الإلغاءُ الفرديّ · كنسُ غيرِ المؤهَّلين · نهايةُ الاشتراك · النقل · الهجر). بدونَه تُقرَأُ **كلُّ حجوزِ المنصّةِ الحيّةِ** لختمِ حصّةٍ واحدةٍ من عشرينَ مقعداً.
- [X] T004 [P] أنشِئْ جدولَ `session_unlocks` بأعمدةِ data-model §٢ و`unique(student_user_id, class_session_id)` تحت `backend/app/Modules/Payments/Database/Migrations/` — ⚠️ **تحت Payments وليسَ LiveSessions** (مُثبَتٌ في `tests/Feature/Settlement/ContextIsolationTest.php:44-54`: قائمةُ جداولِ كلِّ سياقٍ تُشتقُّ بـ`preg_match_all` على `Schema::create` في هجراتِ وحدتِه)، **و`reason` يحملُ أربعَ قيمٍ لا ثلاثاً**: `consent` · `subscription` · `removed_from_room` · **`charged_absence`** (الرابعةُ سقطَت من data-model وتدخلُ هنا). والقراءةُ العكسيّةُ («من فتحَ هذه الحصّة») **غيرُ مفهرَسةٍ عمداً** — اكتبْ ذلكَ بجانبَ الجدولِ ليُضيفَه أوّلُ من يحتاجُها.
- [X] T005 [P] أضِفْ `credit_verdict_at` (`timestamp` · nullable) إلى `attendances` تحت `backend/app/Modules/LiveSessions/Database/Migrations/`.
- [X] T006 [P] أضِفْ **ثلاثةَ أعمدة** إلى `class_sessions` تحت `backend/app/Modules/LiveSessions/Database/Migrations/`: `attended_seats` · **`charged_seats`** · `verdict_stay_seconds` (`unsignedInteger` · nullable) — ⛔ **قرارُ المالك (2026-09-13)**: الأوّلُ للعرضِ والثاني للأجر. **فارغٌ يعني «لم يُحسَبْ بعد»، لا صفراً.**
- [X] T007 [P] ⛔ **جديدةٌ من المراجعة**: أضِفْ `excused_at` (`timestamp` nullable) و`excused_by_user_id` (nullable) إلى `session_bookings` تحت `backend/app/Modules/LiveSessions/Database/Migrations/` — ⚠️ **بدونَها الصفُّ الرابعُ في FR-008د غيرُ قابلٍ للتنفيذِ أصلاً**: `AttendanceStatus::Excused` لا كاتبَ له في `app/` غيرُ `OverrideAttendance`، ونافذتُه تبدأُ عندَ `ends_at + attendanceEditWindowHours` (`OverrideAttendance.php:49`) — **بعدَ** أن يكونَ `CloseClassSession` قد ثبَّتَ الحكمَ وخصمَ. العذرُ حقيقةٌ عن **الحجز** تُكتَبُ قبلَ القفل.
- [X] T008 أنشِئْ نموذجَي `CreditHold` و`SessionUnlock` في `backend/app/Modules/Payments/Models/` بـ`HasUuid` و`BelongsToWorkspace`، وفي نفسِ المهمّة: ⚠️ **أخرِجْ `stay_seconds` من `$fillable` في `Attendance.php`** — والتصحيحُ من المراجعة: **ثلاثةُ كتّابٍ يمرّونَ بالإسنادِ الجماعيِّ اليومَ فعلاً** (`RecordPresencePing.php:70` · `CloseClassSession.php:129` · `MarkAbsenteesJob.php:75`، كلُّها `firstOrCreate`) ولا تنكسرُ إلّا لأنّ العمودَ `->default(0)`؛ اكتبْ ذلكَ بجانبَه وإلّا أزالَ التاليَ الافتراضيَّ فكسرَ الثلاثة. ⛔ **والأعمدةُ الأربعةُ الجديدةُ لا تدخلُ `$fillable` ولا `UpdateClassSessionRequest`**: يكتبُها تحديثٌ شرطيّ، وهي قاعدةُ `captured_order_id` — قابلةً للإسنادِ الجماعيِّ تصيرُ باباً ثانياً يكتبُ به المدرّسُ أجرَ نفسِه بـ`PUT`.
- [X] T009 [P] أضِفِ الجدولَينِ الجديدَينِ إلى `backend/app/Modules/Payments/Support/PaymentsPersonalData.php` (تصدير · محو · انتهاء) وأنشِئْ ترحيلَ صفوفِ `data_categories` لهما — ⚠️ **تصحيحٌ**: `DataCategorySeeder` **لا يملكُ `seedMissing()`** وليسَ محتاجاً إليه؛ `run()` فيه `firstOrCreate` على المفتاحِ أصلاً (`database/seeders/DataCategorySeeder.php:50`) ويحرسُ نفسَه من التحويلِ اللغويِّ (`:38-41`). نادِ `run()` ولا تخترعْ وضعاً ثانياً. **والحارسُ لكلِّ وحدة، فجدولٌ جديدٌ داخلَ وحدةٍ مسجَّلةٍ لا يراه أحد.**

---

## Phase 2: الطورُ المُعيق — لا شيءَ يُترجَمُ ولا **يقيسُ** شيئاً قبلَه

**Purpose**: العقودُ والحمّالاتُ التي بدونِها لا يُترجَمُ الكود، **والحرّاسُ الذين بدونَهم يمرُّ كلُّ توكيدٍ في الشحنةِ أخضرَ على صفر**.

- [X] T010 استخرجْ قوائمَ النصوصِ المحظورةِ فعليّاً من `backend/tests/Feature/Settlement/ContextIsolationTest.php` (⚠️ **المسارُ مصحَّحٌ — لا وجودَ لمجلّدِ `Architecture/`**)، ثمّ اكتبِ الأسماءَ المختارةَ للعقدَينِ والحدثَينِ في `contracts/README.md` بعدَ مطابقتِها. ⚠️ **وثلاثةُ فخاخٍ مقيسةٍ في المطابقة**: (أ) فرعُ Community يقرأُ الملفَّ **خاماً بلا تجريدِ تعليقات** (`:251`) بينما فرعا Learning وLiveSessions يجرّدانِ (`:693` · `:711`) — فتعليقٌ يشرحُ الغيابَ في `ConversationPolicy` يُسقِطُ البناء؛ (ب) FR-032 يضيفُ مفرداتِ حضورٍ إلى ملفِّ حقولِ المدرّسِ في اللحظةِ نفسِها؛ (ج) ⛔ **كلمةُ «فتح» مأخوذةٌ أصلاً**: `UnlockRule` · `UnlockExemption` · `UnlockResolver` · `UnlockReader` · `UnlockDirectory` · `GrantUnlockExemption` كلُّها ٠٠٨ بمعنىً آخرَ تماماً، **وT038 يجعلُ المعنيَينِ يلتقيانِ في مُسنَدٍ واحد**. إمّا تسميةٌ أخرى (`session_content_access` · `GrantSessionContentAccess`) أو تمييزٌ مكتوبٌ في رأسِ الملفَّينِ في الاتّجاهَين — **وFR-031 رفضَ «تجميد» لهذا السببِ بعينِه**.
- [X] T011 أنشِئْ `backend/app/Shared/Contracts/SessionContentAccess.php` بالتوقيعاتِ في contracts/README §١ — ⚠️ **ونوعُ الجوابِ في `App\Shared\Data\` لا في `App\Modules\Payments\Data\`**: السابقةُ `Shared\Contracts\SettlementClearance.php:8,28` يُرجِعُ `App\Shared\Data\SettlementStanding`؛ ونوعٌ تحت Payments يُذكَرُ اسمُه في موردِ LiveSessions وموردِ Learning **فيُسقِطُ جدارَ العزلِ على السلسلةِ المجرَّدة**. ⚠️ **وللعرضِ صورةٌ جُمليّةٌ (`stampAll`) كما للبوّابةِ**: `unlockOfferFor` المفردُ وحدَه استعلامٌ لكلِّ صفٍّ على `/class-sessions`.
- [X] T012 أنشِئْ **عقدَ الكتابةِ** لحجزِ الرصيدِ في `backend/app/Shared/Contracts/` بالاسمِ الذي أقرَّه T010 — ⚠️ **وحُجّتُه تُعادُ كتابتُها**: «أوّلُ عقدِ كتابةٍ في المستودع» **ادّعاءٌ لم ينجُ من `grep`** — العقودُ ثمانيةَ عشرَ، و`PersonalDataOwner::erase()` و`::expire()` (`app/Shared/Contracts/PersonalDataOwner.php:81,104`) يكتبانِ ويحذفانِ عبرَ إحدى عشرةَ وحدة. السابقةُ موجودة؛ **والقرارُ يُبرَّرُ بحدِّ المعاملةِ وحدَه**: الحجزُ يقعُ داخلَ معاملةِ `BookSeat` ليُرَدَّ المقعدُ إن رُفِضَ الرصيد. ⛔ **والرفضُ جوابٌ لا استثناء**: يُرجِعُ كائنَ نتيجةٍ في `App\Shared\Data\` يحملُ `granted` و`first_release_at`، ولا يرمي `Payments\Exceptions\InsufficientCreditsException` — **`BookSeat` لا يجوزُ أن يسمّيَه** (سماحُ LiveSessions كلُّه سطرٌ حرفيٌّ واحد: `ContextIsolationTest.php:685`).
- [X] T013 ⛔ **جديدةٌ من المراجعة — الحمّالةُ المفقودة**: أنشِئْ حدثَي الحجزِ والإفراجِ تحت `backend/app/Modules/Payments/Events/`، ومستمعَيهما تحت `Listeners/`، واربِطْهما بـ`Event::listen()` في `PaymentsServiceProvider::boot()` (الصيغةُ في `:135-217`)، **وأنشِئِ الكنسَ الدوريَّ لحجوزاتٍ لم تُختَمْ** + سطرَه في `routes/console.php` بـ`WithoutOverlapping` **و`expireAfter()`** — ⚠️ **ثلاثُ مهامٍّ في النسخةِ السابقةِ تتّكئُ على هذا الكنسِ في حُجّةِ سلامتِها (T003 · T054 · T059) ولا مهمّةَ واحدةً تُنشِئُه**، وT057 تطلبُ «فُكَّ الحجزَ» في ستّةِ مواضعَ تحت LiveSessions **لا يجوزُ لأيٍّ منها أن يسمّيَ Payments**. بلا هذه المهمّةِ لا ناقلَ أصلاً. **و`expireAfter()` هو الشطرُ الحامل**: قفلٌ بلا انتهاءٍ يعني عاملاً قُتِلَ مرّةً فلا يعملُ الكنسُ ثانيةً أبداً.
- [X] T014 [P] أضِفْ **`charged_seats` عدداً صحيحاً** إلى `backend/app/Modules/LiveSessions/Events/SessionDelivered.php` — ⚠️ **عدداً لا نموذجاً**، والسابقةُ في الملفِّ نفسِه: `billableSeats` و`subscriptionSeats` كلاهما قيمةٌ مجرَّدةٌ لهذا السببِ بعينِه (`:36-40`). ⛔ **والعددُ يُقرَأُ من الصفِّ بعدَ نجاحِ مطالبةِ T028، لا من `$session`**: الحدثُ يحملُ `Dispatchable` وحدَه بلا `SerializesModels`، والتحديثُ الشرطيُّ **لا يُحدِّثُ النموذجَ في الذاكرة**.
- [X] T015 ⛔ أصلِحْ `deliverBillableSession()` في `backend/tests/Pest.php:613-628` — ⚠️ **والتشخيصُ صحيحٌ والعلاجُ في النسخةِ السابقةِ لم يكنْ قابلاً للتنفيذ**: التوقيعُ `(ClassSession, User $teacherUser)` **لا يعرفُ الطلّابَ أصلاً**. الشكلُ الصحيح: مُعامِلٌ ثالثٌ `?array $present = null` يختمُ صفوفَ من سُمّوا وحدَهم، ويُبقي ختمَ المضيفِ كما هو. ⛔ **ولا يجوزُ ختمُ كلِّ الصفوف**: `SeatNotAttendanceTest::markRegisterWith()` يكتبُ `stay_seconds = 0` عمداً قبلَ النداء.
- [X] T016 ⛔ **جديدةٌ**: مُرَّ على **مواضعِ النداءِ الخمسةِ كلِّها** لـ`deliverBillableSession()` — `ConsumptionChainTest` (×٥) · `SeatNotAttendanceTest` (×٢) · `SubscriptionCoveredSeatTest` (×٥) · `SubscriptionReconcileTest` (×١) · `UnbilledDeliveryTest` (×٤) — فكلٌّ منها يُصرِّحُ بمن حضرَ. ⚠️ **وهذا هو المقصودُ**: تجهيزةٌ تعني «حضر» عليها أن **تقولَ ذلك**؛ وثلاثةٌ من الخمسةِ لم تكنْ في أيِّ مهمّةٍ في النسخةِ السابقة، وتوكيداتُها كلُّها تصيرُ «صفرٌ يساوي صفراً» لحظةَ نزولِ T030.
- [X] T017 ⛔ **اقلِبْ** — لا تحذفْ — الاختبارَينِ اللذَينِ يوكّدانِ عكسَ FR-002، **وهما في مجلّدَينِ مختلفَين**: `backend/tests/Feature/Payments/SeatNotAttendanceTest.php` (⚠️ **تعطيلُه جزئيٌّ بالفعل عندَ `:33` — لا تلمسْه**) و`backend/tests/Feature/LiveSessions/AttendanceHasNoFinancialEffectTest.php` (⚠️ **هذا صاحبُ `Queue::fake()` الشاملِ عندَ `:39`**، واستبدالُه بـ`fakeSessionTimeline()` يخصُّه هو). وفي كلٍّ تعليقٌ يقولُ متى ولماذا انقلبَ الحكم. ⚠️ **وحالتُه الثانيةُ («مشاهدةُ التسجيلِ لا تقلبُ حالةً») تبقى صحيحةً بعدَ ٠٣٥ ولا تُقلَب.**
- [X] T018 ⛔ في `backend/app/Modules/Payments/Jobs/ReconcileCreditBalancesJob.php`: **أضِفْ ثابتاً رابعاً** — `credit_balances.held_credits` يساوي عددَ حجوزاتِ الرصيدِ غيرِ المختومةِ لكلِّ رصيد، **وصفرُ حجزٍ حيٍّ على حصّةٍ في حالةٍ نهائيّة** — ⚠️ **وتصحيحُ الحُجّة**: النسخةُ السابقةُ قالت إنّ الثابتَ القائمَ سيصيرُ ضجيجاً، **وقياسٌ يقولُ لا**: `sessionsAgainstEntries()` يَعُدُّ الصفوفَ (`COUNT(*)`) لا القيَم، وقيدُ الصفرِ في T031 يُبقي العدَّ مطابقاً. **الثغرةُ الحقيقيّةُ أنّ لا ثابتَ في المنتَجِ كلِّه يقرأُ `credit_holds`** — وعدّادٌ مُشتقٌّ يكتبُه مستمعٌ **وكنسٌ** على الصفوفِ نفسِها بالتصميمِ بلا ثابتٍ يقرأُه هو التعريفُ الحرفيُّ للانحرافِ الصامت. **ويُقاسُ بليلتَينِ متتاليتَينِ لا بليلةٍ واحدة** (SC-010).
- [X] T019 [P] أصلِحِ البذورَ التي تكتبُ حجوزاً وحضوراً تحت `backend/database/seeders/` لتكتبَ الأعمدةَ الجديدة — ⚠️ **وتصحيحُ الحُجّة**: قياسٌ يقولُ إنّ التقريرَ الليليَّ **لا يحمرُّ** على بياناتِ البذور (`charged_at` لا يكتبُه بذرٌ واحد)، فالسببُ هو **واقعيّةُ التجهيزةِ لا إنقاذُ البناء** — و⛔ **بجانبِ ذلك سببٌ يُسقِطُ البناءَ فعلاً**: قوالبُ الإشعاراتِ وأصنافُ البياناتِ تُبذَرُ قبلَ كلِّ اختبارِ ميزة.
- [X] T020 [P] اربِطِ العقدَينِ في `backend/app/Modules/Payments/PaymentsServiceProvider.php` — ⚠️ **`scoped()` لا `bind()` ولا `singleton()`**، والحُجّتانِ مكتوبتانِ في الملفِّ نفسِه عندَ `:118-125`.
- [X] T021 ⛔ **نُقِلَت إلى هنا من الطورِ الثالث**: اعدُدْ أنواعَ الإشعاراتِ التي يحتاجُها هذا العملُ واكتبِ العددَ في `spec.md` — ⚠️ **T022 كانَ يعتمدُ على مهمّةٍ في طورٍ لاحقٍ فلا يمكنُ تأليفُه**. والعددُ الذي يجبُ قراءتُه **٢٧ لا ٢٥** (`tests/Feature/Notifications/WhatsAppDefaultsTest.php:53` — `toHaveCount(27)`): **اقرإِ التوكيدَ لا النثر**، فنثرُ `CLAUDE.md` أخطأَ في هذا الرقمِ ثلاثَ مرّاتٍ مسجَّلة. **وأيُّ نوعٍ يُخاطِبُ وليَّ الأمرِ يُحرِّكُ ذلكَ التوكيدَ**، وهو `toBe` صريح.
- [X] T022 [P] ⛔ **النتيجةُ المقيسة: لا قالبَ ناقصاً ولا ترحيلَ** — T021 عَدَّ **صفرَ أنواعٍ جديدة** (السببُ في `spec.md` · FR-034أ)، و٠٣٥ يمرُّ بالدفترِ نفسِه فتُطلِقُ عتباتُه إشعاراتِ الرصيدِ القائمةَ بلا حرف. وبقيَ نصُّ المهمّةِ كما كُتِبَ: أضِفْ ترحيلَ القوالبِ الناقصةِ بـ`seedMissing()` (`firstOrCreate`) تحت `backend/app/Modules/Notifications/Database/Migrations/` للأنواعِ التي سمّاها T021 — ⚠️ **`run()` ممنوعٌ في مسارِ النشر**، ورسالةٌ بلا قالبٍ **تُسقَطُ صامتةً**.

**Checkpoint**: يُترجَمُ الكود، **وأيُّ توكيدٍ يُكتَبُ بعدَه يقيسُ ما يدّعي أنّه يقيسُه**.

---

## Phase 3: US1 — الخصمُ بالحضورِ أو بالتقصير (P1)

**Goal**: مُلغيةٌ في الوقتِ لا يُخصَمُ منها ومحتواها مقفول؛ ومتخلّفٌ صامتٌ يُخصَمُ منه **ويُفتَحُ له المحتوى بلا موافقة**؛ ومعذورٌ لا يُخصَمُ منه.

**Independent Test**: حصّةٌ واحدةٌ و**ستّةُ** طلّابٍ في **تجهيزةٍ واحدة**.

### اختباراتٌ تُكتَبُ أوّلاً

- [X] T023 [P] [US1] اكتبْ `backend/tests/Feature/Payments/SessionVerdictTest.php`: **ستُّ حالاتٍ لا خمس** — حضر · أخطرَ في الوقت · تخلّفَ صامتاً · عُذِرَ قبلَ القفل · **أُخرِجَ تأديبيّاً** · لم تُسلَّمِ الحصّة — بثلاثةِ توكيداتٍ لكلٍّ: **رصيدُ الطالب · مستحقُّ المدرّس · حالةُ المحتوى**. ⛔ **وثلاثُ حالاتٍ إضافيّةٍ سمّتْها المراجعة**: (أ) **الحصّةُ الفرديّة** ومعها غيابٌ صامتٌ — وهي أهمُّ سيناريو في القصّةِ كلِّها (SC-014)؛ (ب) **إلغاءٌ قبلَ الموعدِ بدقيقةٍ وإلغاءٌ بعدَه بدقيقة** ⇒ جوابانِ مختلفان (SC-015)، وبلا الزوجِ لا شيءَ يقيسُ الحدَّ؛ (ج) **قيدُ السجلِّ** (FR-012) يُوكَّدُ هنا لا في ملفٍّ آخر؛ ⛔ (د) **حالتا التأجيلِ من T029**: طلبٌ بقيَ معلَّقاً حتّى فاتَ موعدُ الإلغاءِ ⇒ لا خصمٌ ومحتوىً مقفول، **وطلبٌ رُفِضَ قبلَ الموعدِ ثمّ غابَ صاحبُه ⇒ يُخصَم** — والزوجُ هو ما يقيسُ الحدَّ، وواحدةٌ منهما وحدَها تمرُّ على أيِّ تنفيذ. ⚠️ **وما يجعلُه يرسبُ على بناءٍ بلا ميزة**: الحالاتُ السّتُّ في تجهيزةٍ واحدةٍ تُنتِجُ اليومَ **جواباً واحداً** لستّةِ أشخاص.
- [X] T024 [P] [US1] اكتبْ `backend/tests/Feature/Payments/UnlockIdempotencyTest.php` لـSC-003 — ⛔ **والشكلُ في النسخةِ السابقةِ («الخصوماتُ تساوي المفتوحات») هو عينُ الأجوفِ الذي حذَّرَ منه**: على بناءٍ بلا ميزةٍ صفرٌ يساوي صفراً، وعلى عينِ العطبِ الذي يصفُه FR-023 صفرٌ يساوي صفراً كذلك — **المساواةُ ثابتةٌ تحتَ الفشل**. الشكلُ الصحيحُ ثلاثةُ توكيداتٍ وشاهد: صفُّ `session_unlocks` **واحد**؛ قيدٌ واحدٌ بـ`source_type = 'session_unlock'` **وقيدُ المقعدِ `class_session` قائمٌ لم يُمَسّ** — وهو التوكيدُ الذي تحتاجُه FR-023 ولا تستطيعُ المساواةُ صنعَه؛ والرصيدُ نزلَ **واحداً** بقراءةٍ قبلَ وبعد. ⛔ **والضغطةُ الثانيةُ متزامنةٌ لا متتالية**: المتتاليةُ ترجعُ من القراءةِ قبلَ الكتابةِ فتمرُّ على بناءٍ بلا مطالبةٍ إطلاقاً — وهذا المستودعُ دفعَ ثمنَه مرّتَين. ⚠️ **ويُقاسُ على صفوفِ `reason = consent` وحدَها**: صفوفُ T040 بصفرِ حصص.
- [X] T025 [P] [US1] اكتبْ `backend/tests/Feature/Learning/SessionContentGateTest.php`: الأبوابُ الستّةُ كلٌّ على حِدة — ⛔ **والحالاتُ المميِّزةُ هي الموجِبةُ لا السالبة**: `CancelledInWindow` **مرفوضٌ اليومَ عندَ كلِّ بابٍ أصلاً** (`BookingStatus::occupiesSeat()` = `Booked` وحدَها)، فسالبةٌ مبنيّةٌ عليه تمرُّ على بناءٍ بلا بوّابة. الثلاثُ التي تبيتُ: **مُلغٍ في الوقتِ وقد فتحَ ⇒ مسموحٌ عندَ الستّةِ** · **مسجَّلٌ لم يجلسْ قطُّ وقد فتحَ ⇒ مسموح** · **معذورٌ ⇒ مرفوضٌ عندَ الستّة**. ⚠️ **وبابُ التسجيلِ يُقاسُ عبرَ الصورةِ الجُمليّةِ أيضاً** (T036).
- [X] T026 [P] [US1] اكتبْ حالةَ **القراءةِ لا الكتابة** في `backend/tests/Feature/Community/RoomThreadUnlockTest.php` — ⛔ **وصاحبُ الحالةِ طالبٌ لم يحُزْ مقعداً قطُّ**: المعذورُ الذي يفتحُ يستطيعُ الكتابةَ **بسببِ مقعدِه**، فتجهيزةٌ به لا تقيسُ FR-013ب في شيء.

### التنفيذ

- [X] T027 [US1] ⛔ ثبِّتِ الحكمَ في `backend/app/Modules/LiveSessions/Actions/CloseClassSession.php`: `credit_verdict_at` **داخلَ `forceFill` القائمِ عندَ `:133`** لا في تحديثٍ ثانٍ (الحلقةُ تكلّفُ ٢–٣ استعلاماتٍ لكلِّ مقعدٍ بالفعل). ⛔ **واجعلْ نقلةَ الحالةِ نفسَها تحديثاً شرطيّاً** (`:58` قراءةٌ ثمّ `:67` كتابة، ولها مُرسِلانِ: `CloseClassSessionJob` و`CloseStaleSessionsJob`) — **والكاسبُ وحدَه يصلُ إلى كتلةِ الإرسالِ عندَ `:91`**. ⚠️ **ولا يُشتقُّ الحكمُ من `status` ولا من `auto_status` أبداً.**
- [X] T028 [US1] ⛔ اكتبِ الأعمدةَ الثلاثةَ على صفِّ الحصّةِ بتحديثٍ شرطيٍّ واحدٍ (`WHERE attended_seats IS NULL`) **ثمّ اقرأِ القيمةَ المثبَّتةَ من الصفِّ قبلَ تمريرِها للحدث** — ⚠️ **`charged_seats` هو ما يُحاسَبُ عليه المدرّس و`attended_seats` للعرضِ وحدَه** (قرارُ المالك 2026-09-13). ⛔ **والعدُّ يُشتَقُّ من الصفوفِ التي ختمَتْها الحلقةُ لتوِّها، وأيُّ صيغةِ استعلامٍ تحملُ `Attendance::scopeExcludingHost()`**: صفُّ المضيفِ موجودٌ **عمداً** و`wasDelivered():160` يقرأُه — فعدٌّ ساذجٌ بـ`stay_seconds >= $bar` يمنحُ المدرّسَ وحدةَ تدريسٍ إضافيّةً **عن حضورِه درسَ نفسِه، في كلِّ حصّة، للأبد**. ⚠️ **و`verdict_stay_seconds` هو ما يجعلُ SC-012 صحيحاً**: الرقمُ المُطبَّقُ فعلاً يُحفَظُ فلا يُعيدُ تحريكُ الإعدادِ الحكمَ على ماضٍ.
- [X] T029 [US1] ⛔ طبِّقْ **شرطَ التقصير** في نفسِ الحلقة: من لم يبلغِ الحدَّ يُخصَمُ منه **إلّا** إن كانَ حجزُه إلغاءً في الوقتِ · **أو `excused_at` مكتوباً** · **أو `removed_at` مكتوباً** · ⛔ **أو له طلبُ تأجيلٍ لم يُبَتَّ فيه قبلَ موعدِ الإلغاء** — ⛔ **والثالثةُ سقطَت من النسخةِ السابقةِ كلِّها**: FR-036 يقولُ إنّ من أُخرِجَ تأديبيّاً لا يُخصَمُ منه، و`CloseClassSession` **لا يذكرُ `removed_at` إطلاقاً** (مقيس)، فكانَ يُحاسَبُ كمتخلّفٍ صامتٍ — «يُعاقَبُ الطالبُ مرّتَين» بالحرف. ⛔ **والرابعةُ قرارُ المالك (2026-09-13) وهي بابُ إخطارٍ ثانٍ لم تذكرْه المواصفةُ قطُّ**: إعادةُ الجدولةِ **مشحونةٌ منذُ 2026-09-09**، و`RequestSessionReschedule.php:59` يقبلُ الطلبَ **حتّى تبدأَ الحصّةُ — أي بعدَ موعدِ الإلغاءِ كذلك**. فطالبٌ طلبَ التأجيلَ **ليحضرَ** وسكتَ المدرّسُ تقومُ الحصّةُ ويغيبُ هو، وقاعدةُ التقصيرِ تقرأُ ذلكَ تخلّفاً صامتاً — **وقد أخطرَ فعلاً، من البابِ الآخر**. الشرطُ يُقرَأُ من `session_reschedule_requests` لهذا الطالبِ على هذه الحصّة: `status = pending` **أو `decided_at` بعدَ موعدِ الإلغاء**. ⚠️ **والحدُّ هو موعدُ الإلغاءِ نفسُه لا بدايةُ الحصّة**: رفضٌ قبلَه يتركُ للطالبِ وقتاً يُلغي فيه فيُخصَمُ عاديّاً، **وطلبٌ قبلَ الحصّةِ بدقيقةٍ ليسَ إخطاراً بل بابُ إلغاءٍ مجّانيٍّ يُهدِرُ المقعدَ ولا يأخذُ المدرّسُ شيئاً** — وهو ما كُتِبَ FR-008ج لمنعِه. ⚠️ **والجوابُ يُقرَأُ من الأعمدةِ القائمةِ** (`is_billable` · حالةُ الحجز) لا من حسابٍ جديدٍ للموعد.
  - ⛔ **وأغلِقِ الشقَّ المقابلَ في نفسِ المهمّة: حصّةٌ نُقِلَت إلى وقتٍ أبكر.** موعدُ الإلغاءِ مشتقٌّ من `starts_at` (`ClassSession::cancellationDeadline()`)، و`DecideSessionRescheduleRequest.php:109` يُحرِّكُ `starts_at` عبرَ `UpdateClassSession` على **صفِّ الحصّةِ نفسِه** — والوقتُ المقترَحُ مرفوضٌ إن كانَ ماضياً وحدَه، **فالتقديمُ مسموح**. فمقترِحُ التقديمِ طالبٌ واحد، **وبقيّةُ أصحابِ المقاعدِ يستيقظونَ على موعدِ إلغاءٍ فاتَ قبلَ أن يُخلَق**. القاعدة: من كانَ حجزُه قائماً قبلَ نقلةٍ جعلَت الموعدَ الجديدَ ماضياً لحظةَ البتِّ (`to_starts_at − النافذة < decided_at`) **لا يُخصَمُ منه** — ⚠️ **شرطٌ لا يستطيعُ أحدٌ استيفاءَه ليسَ شرطاً، وهذا المستودعُ سجّلَ عائلةَ ذلكَ العطبِ ستَّ مرّات.** وقبلَ ٠٣٥ كانَ فواتُ الموعدِ بلا ثمنٍ على الطالب؛ **٠٣٥ هو ما يجعلُه حصّةً من رصيدِه.**
- [X] T030 [US1] ⛔ **جديدةٌ من المراجعة**: أنشِئْ `backend/app/Modules/LiveSessions/Actions/ExcuseBooking.php` — يكتبُ `excused_at`/`excused_by_user_id` على صفِّ الحجز، **مرفوضٌ بعدَ أن يُكتَبَ `room_closed_at`**، ويسألُ `EnrollmentDirectory` أنّ الطالبَ من طلّابِ هذا المدرّسِ قبلَ أن يكتبَ (NFR-001أ: مُعرِّفٌ مجرَّدٌ بابُ استكشافِ هويّة). ⚠️ **ولا يمسُّ `attendances.status`**: العذرُ التربويُّ والعذرُ الماليُّ اسمانِ لمعنيَين، وهذا هو الماليُّ.
- [X] T031 [US1] ⛔ **جديدةٌ — الذراعُ الاستثنائيّة (قرارُ المالك)**: في `backend/app/Modules/LiveSessions/Actions/OverrideAttendance.php` أضِفْ ذراعاً تكتبُ **قيداً عكسيّاً** حينَ تُعلَّمُ `Excused` على صفٍّ يحملُ `credit_verdict_at` — بصلاحيّةٍ مرتفعةٍ وحدَها، وعبرَ عقدِ الكتابةِ لا باستيراد. ⛔ **والقاعدةُ تُكتَبُ حيثُ تُقرَأ**: علامةُ المدرّسِ **تُعفي ولا تخصمُ أبداً** — وهذا تعديلٌ صريحٌ على FR-004 يُكتَبُ في `spec.md` في نفسِ التغيير. ⚠️ **ومصدرُ العكسِ مستقلٌّ في الدفتر** وإلّا ابتلعَه مفتاحُ عدمِ التكرار، **ومستحقُّ المدرّسِ عن ذلكَ المقعدِ يُنقَضُ معه** وإلّا دفعَتِ المنصّةُ من جيبِها ثمنَ عذرٍ.
- [X] T032 [US1] عدِّلْ `backend/app/Modules/Payments/Actions/ChargeSessionSeats.php` ليقرأَ الحكمَ المجمَّد — ⛔ **والقراءةُ مرّةً واحدةً للحصّةِ بـ`whereIn` مرفوعةً فوقَ الحلقةِ عندَ `:188` وتُمرَّرُ إلى `chargeOne()` كما يُمرَّرُ `$covered`**: الملفُّ يرفعُ نافذةَ الامتحانِ ومساحةَ العملِ والوقوفَ والتغطيةَ فوقَ الحلقةِ بثلاثِ فقراتٍ تشرحُ السبب، وقراءةٌ لكلِّ مقعدٍ تأكلُ ميزانيّةَ `ChargeQueryBudgetTest:139`. ⛔ **والفرعُ يُقرَأُ من الصفِّ بقراءةٍ طازجةٍ لا من `$session` ولا من الحدث**: `SessionDelivered` بلا `SerializesModels` يحملُ النموذجَ كما كانَ لحظةَ الإرسال، فمستمعٌ مُطابورٌ يقرأُ **فراغاً دائماً** ⇒ **كلُّ حصّةٍ ترجعُ لقاعدةِ ما قبلَ ٠٣٥ إلى الأبد**، بينما الكنسُ يُعيدُ الجلبَ فيجيبُ صحيحاً — **جوابانِ لسؤالٍ واحدٍ والغلطُ منهما هو الافتراضيّ**. والفراغُ الحقيقيُّ (العمودُ غيرُ موجودٍ بعد) يبقى ارتداداً لقاعدةِ ما قبلَ ٠٣٥. ⚠️ **وأعِدْ كتابةَ صدرِ الملفِّ في نفسِ التغيير**: `:46-51` يقولُ «ATTENDANCE IS NOT CONSULTED, ANYWHERE» ويسمّي ٠١٤·FR-025د وFR-025ب — وكلاهما منقوضٌ الآن، ويُكتَبُ ذلكَ في `spec.md` كذلك.
- [X] T033 [US1] ⛔ **الأرضيّة — أخطرُ تعديلٍ في الشحنة، وصيغتُه في النسخةِ السابقةِ كانت تُنتِجُ الضررَ الذي كُتِبَت لتمنعَه**: أضِفْ `subtractHeld` إلى `CreditMovement` (افتراضُه `false`) يُقرَأُ **داخلَ فرعِ `enforceFloor` في `applyToBalance()` وحدَه**، وأنشِئْ `canCommit()` **بجانبَ** `canAfford()` للقراءةِ القبليّة. ⛔ **و`canAfford()` و`isBlocked()` و`floorFor()` لا تُمَسّ**: `canAfford()` هي التهجئةُ **الوحيدة** (`CreditLedger.php:115`) **و`isBlocked()` تناديها عندَ `:154`** — وهي مصدرُ «محجوب» الذي تقرأُه `BookingEligibility` و`IssuePlaybackGrant` و`SubscriptionEligibility`. فطرحُ المحجوزِ هناكَ يعني: **طالبٌ رصيدُه ١ يحجزُ حصّةً ⇒ متاحُه صفرٌ ⇒ محجوبٌ ⇒ يُمنَعُ من غرفتِه ومن كلِّ درسٍ في كورسٍ دفعَ ثمنَه**. ⚠️ **وانتبهْ لقريبٍ آخرَ**: `applyToBalance` هو أيضاً مسارُ **الاسترداد** (`AdjustCredits.php:80` هو المُنتِجُ الآخرُ الوحيدُ لـ`enforceFloor: true`)، والاستردادُ ليسَ «التزاماً جديداً» فلا يطرحُ المحجوز.
- [X] T034 [US1] اكتبْ قيدَ الاستهلاكِ **بقيمةِ صفرٍ** لمن لم يُحمَّلْ في `ChargeSessionSeats` — ⚠️ نفسُ ما يفعلُه مقعدُ الاشتراكِ اليومَ بالضبط، وهو ما يُبقي العدَّ في الثابتِ الليليِّ مطابقاً.
- [X] T035 [US1] أنشِئْ `backend/app/Modules/Payments/Actions/UnlockSessionContent.php` — ⛔ **والشروطُ ٢–٦ من contracts §٢ تعيشُ هنا لا في المتحكِّم**: الفعلُ هو المدخلُ الواحدُ الذي يشاركُه البذرُ واللوحةُ والواجهة. صفُّ `session_unlocks` + قيدُ الدفترِ + **سطرُ `activity()`** في معاملةٍ واحدة (⚠️ بلا السطرِ يبقى FR-012 مُستوفىً على الورقِ وغيرَ مرئيٍّ في الشاشةِ الوحيدةِ التي يفتحُها المدقّق). ⛔ **ومصدرُ القيدِ مستقلٌّ (`session_unlock`)**: `ChargeSessionSeats.php:263` يشغلُ `class_session` بمُعرِّفِ الحصّةِ نفسِها (مقيس)، فالدفترُ يقرأُ التصادمَ «خُصِمَ قبلَ ذلك» ويُرجِعُ فراغاً، والفعلُ يقرأُ الفراغَ نجاحاً ⇒ **يُفتَحُ المحتوى بلا خصمٍ ولا سجلّ**. ⛔ **و`workspace_id` يُسنَدُ صراحةً من الحصّة**: الكاتبُ طلبُ طالبٍ، والطالبُ عضوٌ في لا مساحةَ عمل، فالملءُ التلقائيُّ في `BelongsToWorkspace.php:30` **لا يكتبُ شيئاً**. ⛔ **والمفتاحُ الفريدُ هو الحارسُ لا قراءةٌ قبليّة**: التقطْ تصادمَ المفتاحِ وأجِبْ ٤٠٩؛ **وفراغٌ من `CreditLedger::post()` يُطابَقُ على صفِّ الفتحِ** — صفٌّ موجودٌ ⇒ ٤٠٩، صفٌّ غائبٌ ⇒ **ارمِ**، ولا يُقرَأُ نجاحاً أبداً.
- [X] T036 [US1] ⛔ فعِّلْ **أرضيّةَ الرصيدِ** في مسارِ الفتحِ وحدَه (`enforceFloor: true`) بـ`canCommit()` من T033 — ⚠️ **و`zeroFloor` يبقى على افتراضِه `true`** (`CreditMovement.php:40`): حدُّ الائتمانِ مساحةُ تأجيلٍ لدَينٍ **نشأ**، لا لشراءٍ اختياريّ؛ وبـ`zeroFloor: false` في مساحةِ تأجيلٍ ينزلُ الرصيدُ إلى −١ و`isBlocked():158` يحجبُ بلا شرط — وهو الإقفالُ الذي يحذّرُ منه هذا البندُ نفسُه.
- [X] T037 [US1] نفِّذْ `backend/app/Modules/Payments/Support/EloquentSessionContentAccess.php` — الدالّاتُ والصورةُ الجُمليّةُ باستعلامٍ واحدٍ يُختَمُ على الصفوف. ⛔ **وكلُّ قراءةٍ وكتابةٍ على `session_unlocks` و`credit_holds` تُصرِّحُ بـ`withoutWorkspaceScope()` ومعها مُسنَدُ المُلكيّةِ `student_user_id` صراحةً** — النطاقُ ليسَ حارساً هنا في أيِّ اتّجاه: للطالبِ المُسجِّلِ نفسَه هو **عديمُ الأثر**، ولطالبٍ يحملُ `last_workspace_id` (و`workspace_members` يحملُ صفوفَ طلّابٍ فعلاً — مقيس) **يبيتُ في الاتّجاهِ الخطأ** فلا يرى فتحاً اشتراه من مدرّسٍ آخر ⇒ **محتواه المدفوعُ يُقفَلُ ثانيةً مع عرضٍ بأن يدفعَ مرّةً أخرى**. ⛔ **ونفسُ ارتدادِ T032 هنا**: `attended_seats IS NULL` ⇒ المقعدُ يُخوِّل — وإلّا كانَ الطالبُ في نافذةِ النشرِ **مخصوماً منه ومقفولاً عليه** معاً.
- [X] T038 [US1] أضِفْ نقطةَ الفتحِ في `backend/app/Modules/Payments/routes/api.php` — ⛔ **داخلَ مجموعةٍ تحملُ `throttle:billing` مسمّىً**: كلُّ طريقٍ يُحرِّكُ مالاً في هذه الوحدةِ يحملُه، والطريقُ الجديدُ **يصرفُ رصيدَ المستخدمِ بلا تراجُع** وكانَ سيُشحَنُ على سقفِ ٣٠٠ في الدقيقة — حلقةٌ واحدةٌ تمشي على كلِّ حصّةٍ مقفولةٍ في كورسٍ **فتستنزفُ الرصيدَ كلَّه**، وكلُّ نداءٍ حصّةٌ مختلفةٌ فالمفتاحُ الفريدُ لا يوقفُه. ⛔ **والمقطعُ `{sessionUuid}` ومُعامِلُ المتحكِّمِ `string`**: اسمُ المقطعِ هو ما يُفعِّلُ الربطَ الضمنيَّ مهما قالَ التعليقُ فوقَه، والربطُ الضمنيُّ يَحُلُّ أيَّ حصّةٍ على المنصّةِ لطالبٍ نطاقُه عديمُ الأثر. ⚠️ **والشرطُ الأوّلُ يجيبُ نفسَ الرفضِ الموحَّدِ للشرطِ الثاني** فلا يصيرُ عرّافاً. ⚠️ **والشرطُ الثاني يُقرَأُ «كانَ يوماً في المجموعة» لا «فيها اليوم»** — السابقةُ `ConversationPolicy.php:298` (`wasEverMember`) — وإلّا رُفِضَ المنتقلُ عن حصّةٍ جلسَها وخُصِمَ ثمنُها. ⚠️ **والرابعُ شقّان**: سُلِّمَت **ومادّتُها لم تُؤرشَفْ**.
- [X] T039 [US1] اجعلِ الأبوابَ الستّةَ تسألُ العقد — ⛔ **وثلاثةُ تصحيحاتٍ مقيسة**: (أ) `IssuePlaybackGrant` فيها **تهجئتانِ** للحقِّ نفسِه، `mayWatch():161` و`mayWatchMany():254`، والثانيةُ نسخةٌ جُمليّةٌ بيدٍ **وهي بابُ التسجيلِ في شاشةِ المنهج** — تعديلُ واحدةٍ وحدَها هو عينُ «بابانِ يختلفان» الذي جعلَ تسجيلاً مدفوعاً غيرَ قابلٍ للفتحِ في ٠١٨، **بالأدوارِ مقلوبة**؛ (ب) ⛔ **`exams` لا تحملُ `class_session_id` إطلاقاً** (مقيس: العمودُ في `assignments` و`unlock_rules` فقط) — فالربطُ عبرَ `lessons.class_session_id` لدرسٍ `reference_id`ـه هذا الامتحان، **ويُسألُ في `StartAttempt` وسياسةِ المحاولةِ لا في حمولةِ المنهجِ وحدَها**؛ (ج) ⛔ **لا سطرَ استيرادٍ ولا سلسلةَ `App\Modules\Payments` في Learning ولا في Community** — والتعليقاتُ تُجرَّدُ في فرعِ Learning **ولا تُجرَّدُ في فرعِ Community**. ⛔ **وتصحيحٌ رابعٌ ظهرَ بالقياس (2026-09-13)**: شرطُ «لم يُحكَمْ بعد» في `openableSessionIds()` كانَ `attended_seats IS NULL` وحدَه، فيفتحُ **كلَّ حصّةٍ لم تقعْ بعد** — ورقةُ الأسبوعِ القادمِ منشورةٌ اليومَ على السجلِّ كلِّه. صارَ `delivered_at IS NOT NULL AND attended_seats IS NULL`: نافذةُ النشرِ بالضبط، لا أكثر.
- [X] T040 [US1] في `backend/app/Modules/Community/Policies/ConversationPolicy.php`: الفتحُ يمنحُ **القراءةَ وحدَها** — ⛔ **والحارسُ رفضٌ لا تعليق**: `post()` ينادي `view()` عندَ `:70` ثمّ يسقطُ إلى `Response::allow()` عندَ `:190` في فرعِ الغرفة، وفحصُ المقعدِ الوحيدُ داخلَ `publicRoom()` أي داخلَ `view()`. فتوسيعُ القراءةِ **يمنحُ حقَّ الكتابةِ صامتاً**: من لم يحضرْ يدفعُ حصّةً فيصيرُ يكتبُ في خيطٍ يقرأُه كلُّ صاحبِ مقعد. الشكل: `publicRoom()` تصيرُ `مقعدٌ || العقد`، **و`post()` تحملُ رفضاً صريحاً** بجانبَ ذراعِ الإخراجِ عندَ `:139` لمن لا مقعدَ له ولا `chat.moderate`. ⛔ **والاستثناءُ هو جانبُ التدريسِ كلُّه لا `chat.moderate` وحدَه (مقيسٌ برسوبِ `RankFallbackTest`)**: المساعدُ يحملُ `chat.reply` بلا مقعدٍ ولا `chat.moderate`، وهو بالضبط مَن يراقبُ الغرفةَ والمدرّسُ يتكلّم — فرفضٌ مكتوبٌ على `chat.moderate` وحدَه يُسكِتُه. `runsTheRoom()` دالّةٌ واحدةٌ يقرأُها البابانِ حتى لا يختلفَ هجاءان.
- [X] T041 [US1] ⛔ في `backend/app/Modules/LiveSessions/Support/BookingEligibility.php`: تخطَّ شرطَ واجبِ الحصّةِ السابقةِ إذا كانَ الواجبُ نفسُه مقفولاً — ⛔ **والجوابُ يُحسَمُ داخلَ المرورِ الجُمليِّ القائمِ في `Assessments/Support/UnlockResolver.php:80,90`، لا باستعلامٍ جديدٍ من `BookingEligibility`**: هذه الدالّةُ يُعيدُ `BroadcastController::presence()` تشغيلَ سلسلتِها **في كلِّ نبضة**، وسقفُ الميزانيّةِ هناكَ **١٥ على استقرارٍ مقيسٍ عندَ ١٤** ومكتوبٌ فوقَه أنّه لا يُرفَع — فاستعلامٌ واحدٌ يملأُ الفراغَ واثنانِ يُحمِّرانِ البناء.
- [X] T042 [US2] ⛔ **جديدةٌ من المراجعة**: أضِفْ شرطَ الرصيدِ المتاحِ إلى `BookingEligibility::openingRefusal()` عبرَ عقدِ القراءة، وأرجِعْ `first_release_at` من القراءةِ نفسِها — ⛔ **`openingRefusal()` وحدَها، ولا تلمسْ `refusalReason()`**: الثانيةُ هي ما ينادي كنسُ غيرِ المؤهَّلينَ **وهو يُلغي مقاعدَ مدفوعة**. ⚠️ **وبدونَها يُعرَضُ المقعدُ على طالبٍ لا رصيدَ له ويُرفَضُ داخلَ `BookSeat` بعدَ الضغط**، وT064 يطلبُ من الواجهةِ أن تقولَ «متى يُفرَجُ أوّلُ حجز» بلا مُنتِجٍ لذلكَ التاريخِ في الخادم. ⛔ **الشرطُ في `openingRefusal()` وحدَها لا في `refusalReason()`** — الكنّاسُ الليليُّ يُلغي ما تردُّه الثانية، وكلُّ مقعدٍ محجوزٍ يُجمِّدُ حصّةً بالتعريف، فالطيُّ هناكَ يُصادِرُ كلَّ مقاعدِ المنصّة. ⚠️ **والمسارُ السعيدُ استعلامٌ واحد** (`availableFor()`) و`MIN(ends_at)` على فرعِ الرفضِ وحدَه: هذه السلسلةُ يُعيدُ نبضُ الحضورِ تشغيلَها وسقفُها ١٥ على استقرارٍ عندَ ١٤. ⚠️ **ولا يعملُ فعليّاً حتى T059/T060** يضعانِ التجميدَ في مسارِ الحجز.
- [X] T043 [US1] افتحْ محتوى الحصّةِ **تلقائيّاً بصفرِ حصصٍ** لثلاثِ حالات: `subscription` · `removed_from_room` · `charged_absence` — ⚠️ **و`reason` ليسَ زينة**: صفٌّ بصفرِ حصصٍ بلا سببٍ لا يُميَّزُ عن عطبٍ بعدَ شهر. والمشترِكُ لا يملكُ رصيداً أصلاً، فقفلٌ بثمنٍ لا يستطيعُ دفعَه **قفلٌ بلا مخرج**. ⛔ **نُفِّذَ بسببَينِ لا ثلاثة (2026-09-13)**: `charged_absence` **زائدٌ بالبناء** — المخصومُ يحملُ `credit_verdict_at`، والاستعلامُ الثاني في `openableSessionIds()` يفتحُ له أصلاً، فصفٌّ ثانٍ جوابٌ ثانٍ لسؤالٍ مُجاب. الباقيانِ هما اللذانِ **بلا مخرج**: المشترِكُ لا رصيدَ له، والمُخرَجُ أُعفِيَ بقرارِ المدرّسِ نفسِه. ⚠️ **وذراعُ `subscription` بلا اختبارٍ بعد**: تجهيزتُها مشترِكٌ أخطرَ في الوقتِ ولا رصيدَ له يشتري به — وهي تنتمي إلى T059 في المرحلةِ الرابعة. الذراعُ الأخرى (`removed_from_room`) مقيسةٌ في T023.
- [ ] T044 [P] [US1] وسِّعْ `backend/app/Modules/LiveSessions/Http/Resources/ClassSessionResource.php` وموردَ المنهجِ بـ`content_locked` · `unlock_offer|null` · `locked_session_count` — ⛔ **مختومةً من المُنادي، لا مسؤولاً عنها المورد**: الملفُّ يحملُ القاعدةَ بنفسِه عندَ `:35-44` فوقَ `unlock_open`/`unlock_reason` («STAMPED BY THE CALLER, NEVER ASKED HERE — a Resource runs once per row»)، والسابقةُ `UnlockDirectory::stampAll()`. **وفراغٌ يعني «لم يُسأَلْ» لا «مفتوح»**. ⚠️ **و`locked_session_count` يُشتقُّ من خريطةِ الوصولِ في `ReadCurriculum` بصفرِ استعلام**. ⚠️ **والمضيفُ لا يُسألُ أبداً**: المورِدُ يخدمُ تقويمَ المدرّسِ كذلك، و`mayOpenSessionContent(teacher)` كاذبةٌ في مساحةِ عملِه — وهو عطبُ `/eligibility` الذي طبعَ «لستَ مسجَّلاً عندَ هذا المدرّس» لصاحبِ الحصّة. ⚠️ **وبلا مبلغٍ نقديٍّ في أيٍّ منها.**
- [ ] T045 [P] [US1] ⛔ **جديدةٌ**: اختمْ في `ScheduleController::index()` و`::next()` و`FreezePeriodController::store()` — ⚠️ **`/schedule` يعشِّشُ `ClassSessionResource` داخلَ `SessionBookingResource:23` ولا يستدعي خاتماً واحداً** (الخاتمُ الوحيدُ في الوحدةِ عندَ `ClassSessionController.php:148`)، فعشرونَ صفَّ جدولٍ × أربعُ قراءاتٍ للعرض. **و`tests/Feature/LiveSessions/QueryBudgetTest.php:126` يُحمِّرُ البناءَ على هذا بالضبط** — وهو الشبكةُ تعمل، لا مفاجأة.
- [ ] T046 [P] [US1] ⛔ **جديدةٌ**: أضِفْ `classSessions.unlock()` وأنواعَ الحمولةِ الأربعةِ إلى `frontend/src/lib/class-sessions.ts` — ⚠️ **مقيسٌ: `locked_session_count` لا يردُ ولا مرّةً في `frontend/src` كلِّه**، وT047–T049 تفترضُ حمولةً لا شيءَ يجلبُها. **سطحٌ بلا نداءٍ ميزةٌ لا يملكُها أحد**، وهذا المستودعُ سجّلَ ثلاثَ حالاتٍ منها في يومٍ واحد.
- [ ] T047 [US1] أنشِئْ `frontend/src/app/(app)/(shell)/sessions/[uuid]/page.tsx` — حالةُ الحصّةِ · المقفولُ منها · زرُّ الفتحِ · طريقُ الشراء · **وزرُّ الغرفةِ ما دامتِ النافذةُ مفتوحة**. ⚠️ **والقفلُ يُقرَأُ من الحمولةِ ولا يُعادُ اشتقاقُه في TypeScript.**
- [ ] T048 [US1] ⛔ انقلِ الروابطَ **الخمسةَ** الطلّابيّةَ إلى `/sessions/{uuid}` — `dashboard/cards/UpcomingSessionsCard.tsx:135` · `schedule/page.tsx:162` · `components/sessions/NextSessionCountdown.tsx:189` · `components/courses/NextSessionHeader.tsx:172` · `components/courses/tabs/SessionsTab.tsx:94`. ⛔ **ورابطا `manage/sessions/page.tsx:705` و`manage/sessions/[uuid]/page.tsx:152` يبقيانِ على الغرفةِ عمداً** — شاشتا المدرّسِ ولهما صفحتُهما. ⚠️ **و`components/courses/NextSessionHeader.test.tsx:60` يوكّدُ الرابطَ القديمَ فيُحمِّرُ `npm test`**: يُعدَّلُ في نفسِ التغيير، ومعه تعليقُ `lib/notification-links.test.ts:12` («لا يوجدُ إلّا `/sessions/[uuid]/room`») الذي يصيرُ كاذباً.
- [ ] T049 [P] [US1] أنشِئْ `frontend/src/components/sessions/UnlockPanel.tsx` — الرقمُ والرصيدُ وما سيُفتَحُ **عدداً وأصنافاً لا عناوين**، وطريقُ الشراءِ **دائماً**، **وجملةٌ لمن فتحَ ثمّ أُرشِفَت المادّة** (FR-039ج، ولم يكنْ لها مهمّةٌ قطُّ). ⚠️ **قائمةُ عناوينِ أوراقِ ساعةٍ هي خطّةُ درسٍ لم يحضرْه صاحبُها.**
- [ ] T050 [P] [US1] أضِفْ رمزَ المنعِ السابعَ في `frontend/src/components/courses/LessonRow.tsx` و`frontend/src/lib/curriculum.ts` — ⚠️ **الرمزُ يُقرَأُ من الخادم**، والعددُ يمرُّ بـ`counted()` بخانةِ `other` صريحة.
- [ ] T051 [P] [US1] صحِّحْ `frontend/src/app/(public)/pricing/page.tsx` — تقولُ اليومَ «الخصمُ بالتسليم» وستكذبُ يومَ الشحن.
- [ ] T052 [P] [US1] اكتبْ `frontend/src/components/sessions/UnlockPanel.test.tsx` — **زرٌّ بلا رقمٍ ليسَ اختياراً**، والتوكيدُ على النصِّ المعروض.
- [ ] T053 [US1] أضِفْ صنفَ الفتحِ إلى `backend/app/Modules/Payments/Support/BillingAuditSubjects.php` — **الخريطةَ وحدَها؛ السطرُ الذي يُنتِجُ الصفوفَ في T035.**

**Checkpoint**: **الأبوابُ الستّةُ تسألُ سؤالاً واحداً**، والعذرُ له بابٌ في وقتِه.

---

## Phase 4: US2 — الحجزُ يحجزُ الرصيدَ ولا يخصمُه (P2)

**Goal**: طالبٌ برصيدِ ٣ يحجزُ ٣ ⇒ الرابعةُ تُرفَضُ بجملةٍ تقولُ **متى يُفرَجُ أوّلُ حجز**.

- [ ] T054 [P] [US2] اكتبْ `backend/tests/Feature/Payments/CreditHoldLifecycleTest.php`: وضعٌ · ختمٌ إلى خصم · ختمٌ إلى إفراج · ⛔ **وختمٌ مزدوجٌ متزامنٌ بردِّ نداءٍ بينَ القراءةِ والكتابة** ⇒ العدّادُ يتحرّكُ مرّةً واحدة — **والمتتالي يمرُّ على بناءٍ بلا تحديثٍ شرطيٍّ خالص، وهو كلُّ مضمونِ T057**. ⛔ **وحالةُ السباقِ في quickstart §٢ «إلزاميّة»**: رصيدٌ ١ وحجزانِ متزامنانِ على **حصّتَينِ مختلفتَين** (نفسُ الحصّةِ ترتدُّ على فهرسِ المقعدِ قبلَ أن تصلَ لحارسِ الرصيد) ⇒ واحدٌ يمرّ.
- [ ] T055 [P] [US2] اكتبْ حالةَ **المقعدِ المُحيا**: احجزْ ⇒ أُطلِقَ ⇒ خُتِمَ ⇒ أُحيِيَ ⇒ صفٌّ ثانٍ بـ`hold_seq = 1` بلا تصادمٍ وبلا صمت — ⚠️ **واذكرْ في المهمّةِ أنّ `reviveReleasedSeat()` له منادٍ واحدٌ اليومَ (`ClaimSubscriptionSeats.php:189`) وT058 يُعفيه**، فالمسارُ بلا بابٍ إنتاجيٍّ بعدُ والتجهيزةُ تُبنى باليد؛ بغيرِ ذلكَ يُقرَأُ أنّه يغطّي مساراً حيّاً.
- [X] T056 [US2] أنشِئْ `backend/app/Modules/Payments/Actions/PlaceCreditHold.php` — صفٌّ + `held_credits + 1` بتحديثٍ شرطيٍّ واحد، **والكاسبُ وحدَه يُحرِّكُ العدّاد**. ⛔ **و`hold_seq` يُحسَبُ عدَّ الصفوفِ القائمةِ لـ(الرصيد · الحصّة) داخلَ معاملةِ المُنادي** — لا مهمّةَ في النسخةِ السابقةِ سمَّت كاتبَه أصلاً. ⛔ **و`workspace_id` يُسنَدُ صراحةً**، والسببُ أنّ **الكاتبَ طلبُ طالبٍ** (والخاتمُ مهمّةٌ مُطابورة) وكلاهما بلا سياق. ⚠️ **والتعبيرُ الذي يرفعُ `ERROR 1690` هو هذا التعبيرُ** (انظرْ T001).
- [X] T057 [US2] أنشِئْ `backend/app/Modules/Payments/Actions/SettleCreditHold.php` — `UPDATE … WHERE id = ? AND settled_at IS NULL` ⛔ **والمطالبةُ والإنقاصُ في معاملةٍ واحدة**، وإلّا فعاملٌ قُتِلَ بينَهما يُحرِّفُ العدّادَ تحريفاً دائماً لا يراه شيءٌ في المنتَجِ كلِّه (ولذلك T018). ⚠️ **والإفراجُ ليسَ قيداً فلا فهرسَ يمتصُّه**، والختمُ يقعُ من مستمعٍ **ومن كنسٍ** على المقاعدِ نفسِها **بالتصميم**. و`lockForUpdate()` **ممنوع**.
- [ ] T058 [US2] استدعِ عقدَ الكتابةِ **داخلَ معاملةِ** `backend/app/Modules/LiveSessions/Actions/BookSeat.php` — ⛔ **وللفعلِ ثلاثةُ مداخلَ ومعاملتانِ**: `claim()` (يصلُها `handle()` و`claimGrantedSeat()`) **و`reviveReleasedSeat():107-155` بمعاملتِها الخاصّةِ ولا تمرُّ بـ`claim()`** — فحجزٌ في `claim()` وحدَها يعني **مقعداً مُحيا بلا حجزِ رصيد: حصّةٌ مجّانيّةٌ والثابتُ الليليُّ لا يرى شيئاً لأنّ صفّاً لم يُكتَبْ قطُّ**. ⛔ **والحجزُ يقعُ بعدَ كتابةِ صفِّ الحجزِ وقبلَ الالتزام**: قبلَه يحسبُ عاملانِ `hold_seq = 0` ويصطدمُ الثاني على `credit_holds`، وملتقِطُ التصادمِ في `claim()` لا يغطّي ذلكَ الفهرسَ.
- [ ] T059 [US2] ⛔ **مقعدُ الاشتراكِ لا يُحجَزُ له رصيدٌ إطلاقاً** — فرعٌ صريحٌ في `ClaimSubscriptionSeats.php` — ⚠️ يمرُّ بـ`BookSeat` في حلقة، **فبلا فرعٍ يُحجَزُ رصيدُ مشترِكٍ لا رصيدَ له** (٠٢٧ · FR-041).
- [ ] T060 [US2] ⛔ فُكَّ الحجزَ في المواضعِ التي تُطلِقُ المقعد — ⛔ **و`CancelBooking` شقّان لا واحد**: ذراعُ **الإلغاءِ في الوقتِ** وحدَها تُفرِج؛ **والإلغاءُ المتأخّرُ يُبقي المقعدَ ويكتبُ `is_billable = true`** (`CancelBooking.php:47-57`، و`seats_taken` لا يُنقَصُ إلّا في الوقت) وهو المقعدُ الذي يخصمُه T029 عندَ الإغلاق — **ففكُّه هنا يُحرِّرُ الرصيدَ ليُحجَزَ ثانيةً قبلَ نزولِ الخصم، والأرضيّةُ مطفأةٌ في مسارِ الخصم، فينزلُ الرصيدُ تحتَ الصفرِ ويُقرَأُ الطالبُ متعثّراً**. والمواضعُ الأخرى: `CancelClassSession` · `CreateFreezePeriod` · `Actions/ReleaseIneligibleBookings` (⚠️ **تحت `Actions/` لا `Jobs/`**) · `Listeners/ReleaseSeatsOnSubscriptionEnd` · `Listeners/ReleaseSeatsOnTransfer` (⚠️ **والأخيرتانِ تمرّانِ بـ`CancelBooking` نفسِها**، فالإفراجُ لا يُكرَّرُ إلّا امتصَّه شرطُ T057). ⛔ **وإعادةُ الجدولةِ ليسَت إفراجاً، واكتبْ ذلكَ بجانبَ القائمة**: `DecideSessionRescheduleRequest.php:109` يُحرِّكُ `starts_at` على **صفِّ الحصّةِ نفسِه** عبرَ `UpdateClassSession` — الحجزُ والمقعدُ والصفُّ كلُّها باقيةٌ بمُعرِّفاتِها، **فحجزُ الرصيدِ يركبُ مع الحصّةِ ولا يُفَكُّ ولا يُعادُ وضعُه**. وبلا هذا السطرِ يأتي قارئٌ بعدَ شهرٍ فيضيفُ التأجيلَ إلى قائمةِ الإفراجِ بحسنِ نيّةٍ **فيمنحُ حصّةً مجّانيّةً لكلِّ تأجيل**. ⛔ **والثلاثةُ الجُمليّةُ تُفكُّ جُمليّاً**: عبارتانِ مهما كانَ العدد — ختمُ كلِّ حجوزِ الحصّةِ في عبارة، ثمّ العدّادُ **مُسنَداً من استعلامٍ فرعيٍّ لا مزيداً بالزيادة** (فيصيرُ الختمُ المزدوجُ بلا أثر) — وإلّا صارَ إلغاءُ حصّةٍ بثلاثينَ مقعداً واحداً وستّينَ عبارةً بدلَ عبارتَين.
- [ ] T061 [US2] ⛔ **جديدةٌ — البابُ السابعُ**: أضِفْ ذراعَ إفراجٍ في **فرعِ «لم تُسلَّمْ»** من `CloseClassSession` (حيثُ `delivered_at === null` عندَ `:73`) — ⚠️ **المدرّسُ فتحَ الغرفةَ ومشيَ باكراً**: الحالةُ تصيرُ `Completed` **بلا `SessionDelivered`**، فلا خصمَ ولا ختمَ، ولا T060 ولا T062 يصلانِها ⇒ **الحجزُ معلَّقٌ إلى الأبد**، والثابتُ الليليُّ أخضرُ لأنّ الصفَّ غيرُ مختومٍ **فعلاً**. وهي الحالةُ الخامسةُ في FR-008د. **(لقيَها ثلاثةُ وكلاءَ مستقلّين.)**
- [ ] T062 [US2] ⛔ أضِفْ ذراعَ فكٍّ إلى `backend/app/Modules/LiveSessions/Actions/AbandonClassSession.php` — ⚠️ **الحالةُ التي لا إغلاقَ لها أصلاً**؛ **واجعلْ نقلةَ الحالةِ شرطيّةً** (`:56-65` قراءةٌ ثمّ كتابة) فالكاسبُ وحدَه يُرسِل.
- [ ] T063 [US2] ⛔ **جديدةٌ**: أنشِئْ `heldBillableSeat()` **بجانبَ** `holdsSeat()` في `ClassSession.php` لسؤالِ الحقِّ الماليِّ وحدَه — ⛔ **ولا تُضيِّقْ `holdsSeat()`، وكانَ ذلكَ خطأً في النسخةِ السابقة**: اتّساعُها **متعمَّدٌ وموثَّقٌ فوقَها** (`:160-173`): «the student whose enrolment lapsed and whose seat `ReleaseIneligibleBookings` then cancelled is precisely the person FR-038 exists for, and a booked-only read would refuse them the sentence explaining why». وقارئاها `BroadcastController:125` و`ClassSessionPolicy:64` يسألانِ «هل هذه الحصّةُ شأنُك» لا «هل دفعتَ». ⚠️ **وبجانبِ الجديدةِ تعليقٌ يسمّي السؤالَين** — والسابقةُ الفصلُ القائمُ في `EloquentSessionAttendanceDirectory:53-68`.
- [ ] T064 [P] [US2] وسِّعْ حمولةَ رصيدِ الطالبِ بـ`available_credits` و`held_credits` **عدَدَينِ لا مالاً** في `CreditBalanceResource` — ⚠️ **وهي حمولةُ الطالبِ عن نفسِه**؛ و`StudentBalanceAllowlist` شيءٌ آخرُ تماماً («the only fields a **TEACHER** may read about a student's credits»، `:8`) فإضافتُهما هناكَ **قرارٌ بأن يرى المدرّسُ ما جمَّدَه طالبُه** — احسمْه صراحةً ولا تُصرِّحْ به سهواً. ⛔ **ومعهما مفتاحُ الكورسِ دائماً**: جمعُ الأرصدةِ ممنوعٌ نصّاً.
- [ ] T065 [P] [US2] أضِفْ حالتَي `credit_holds` و`session_unlocks` إلى `backend/tests/Feature/Tenancy/WorkspaceIsolationTest.php` — ⛔ **بمساحتَي عملٍ وقارئٍ هو عضوٌ فعلاً**: تجهيزةٌ بطالبٍ سجَّلَ نفسَه لا تُثبِتُ شيئاً على هذَينِ الجدولَين، لأنّ سياقَه فارغٌ والنطاقُ عديمُ الأثرِ عليه.
- [ ] T066 [US2] ⛔ اكتبْ ترحيلَ حجوزاتِ الرصيدِ للحجوزاتِ القائمةِ بالشرطِ **الضيّق** — ⚠️ **مقيسٌ: ١٨ حجزاً «محجوز» ومنها صفرٌ على حصّةٍ قادمة**، فالشرطُ الساذجُ يكتبُ ١٨ حجزاً ميّتاً تُطرَحُ من المتاحِ إلى الأبد. ⛔ **و`uuid` والطوابعُ و`workspace_id` تُمرَّرُ صراحةً**: `DB::table()` لا يُقلِعُ النموذجَ فلا يعملُ `HasUuid`، وMySQL يُنزِلُ انتهاكَ NOT NULL إلى تحذيرٍ ويخزّنُ `''` — **فيصطدمُ كلُّ حجزٍ لاحقٍ على المنصّةِ بذلكَ الصفّ**. ⛔ **و`whereNotExists` على `credit_holds`** ليكونَ قابلاً لإعادةِ التشغيل، **والعدّادُ يُكتَبُ مطلقاً باستعلامٍ فرعيٍّ لا بالزيادة**. ويمشي بالتقطيعِ بالمُعرِّف. ⚠️ **ويكتبُ صفرَ صفوفٍ على بياناتِ اليومَ وهو الجوابُ الصحيح — فأضِفْ في اختبارِه حجزاً قادماً مصنوعاً ليُثبَتَ أنّه يكتبُ واحداً**، وإلّا فمخرجُه الصحيحُ غيرُ قابلٍ للقياس.
- [ ] T067 [P] [US2] ⛔ **جديدةٌ**: اكتبْ حارسَ FR-025 في `backend/tests/Feature/Payments/UnlockFloorTest.php`: `remaining = 1, held = 1` ⇒ رفضٌ ومعه طريقُ الشراء؛ `remaining = 1, held = 0` ⇒ قبول — **حالتانِ أو حدُّ `− held` غيرُ مقيسٍ إطلاقاً**، وهو الحدُّ الذي يقفلُ على الطالبِ كورساً دفعَ ثمنَه إن أخطأَ موضعُه.
- [ ] T068 [P] [US2] اعرضِ المملوكَ والمتاحَ بكلمتَينِ مختلفتَينِ في عروضِ الرصيدِ تحت `frontend/src/`، ورسالةَ الرفضِ ومعها **`first_release_at` من T042**.

**Checkpoint**: الحجزُ لم يعدْ مجّانيّاً، **وصفرُ حجزٍ يبقى معلَّقاً بعدَ الإغلاق** (SC-004) — **من الأبوابِ السبعةِ كلِّها**.

---

## Phase 5: US3 — المدرّسُ يُحاسَبُ على المقاعدِ المحمَّلة (P3)

⛔ **العنوانُ تغيَّرَ بقرارِ المالك (2026-09-13)**: `charged_seats` لا `attended_seats`. السببُ مكتوبٌ في FR-008د صفّ ٣ — المتخلّفُ الصامتُ **يُخصَمُ منه والمدرّسُ يأخذ**؛ ولولا ذلكَ لَذهبَت حصّةٌ فرديّةٌ غابَ طالبُها إلى فرعِ «الحصّةِ الفارغة» فأخذَ المدرّسُ نسبةً بينما دفعَ الطالبُ كاملاً **والفرقُ يذهبُ للمنصّة**.

- [ ] T069 [P] [US3] اكتبْ `backend/tests/Feature/Settlement/ChargedSeatsAccrualTest.php` — ⛔ **ثلاثُ حالاتٍ لا واحدة**: (أ) عشرونَ مقعداً واثنا عشرَ حاضراً **والثمانيةُ ألغَوا في الوقتِ صراحةً** ⇒ ١٢؛ (ب) نفسُ العشرينَ والثمانيةُ **تخلّفوا صامتينَ** ⇒ **٢٠**؛ (ج) **حصّةٌ فرديّةٌ بغيابٍ صامت** ⇒ وحدةٌ كاملةٌ لا نسبةُ حصّةٍ فارغة. ⚠️ **وبلا (ب) لا شيءَ يفرّقُ بينَ العمودَين**. والفتحُ المتأخّرُ **لا يُحرِّكُ مستحقَّ المدرّسِ بحرف**.
- [X] T070 [US3] اقرأِ العددَ المُجمَّدَ في `backend/app/Modules/Settlement/Actions/AccrueTeachingUnits.php` عبرَ الحدثِ الذي وسَّعَه T014 — **لا استيرادَ ولا استعلامَ عبرَ الحدّ**. ⛔ **وفي نفسِ المهمّة: احسمْ `attendedStudentIds()` (`:151-166`)** — تقرأُ `Present|Late` **ومصدرَها `Manual` ضمناً**، أي **علامةٌ يكتبُها المدرّسُ بيدِه تقرّرُ ما يأخذُه** على مقعدِ المشترِك، وهو ما تمنعُه FR-004 وT027 نصّاً. إمّا أن تُوفَّقَ مع الحكمِ المجمَّدِ أو تُحذَف؛ **وبغيرِ ذلكَ يرسبُ T079 لو كُتِبَ بأمانة**. ⛔ **حُسِمَ `attendedStudentIds()` (2026-09-13): `first_joined_at IS NOT NULL`** — لا العلامةُ ولا حدُّ القعود. العلامةُ كانت تسمحُ للمدرّسِ أن يقرّرَ أجرَه بيدِه (`Manual` في قائمةِ المصادر)، وحدُّ القعودِ سؤالٌ آخرُ تماماً: هو ما يُخصَمُ به الطالبُ الدافع، بينما قرارَ 2026-09-05 يسألُ **«مَن حضرَ الغرفة»**. و`first_joined_at` لا يكتبُه إلّا `RecordPresencePing` — نبضةٌ على طريقِنا من داخلِ الغرفة — فهو مقيسٌ لا مكتوب، ولا يحتاجُ فرعاً لما قبلَ ٠٣٥ لأنّ العمودَ قائمٌ منذُ ٠٠٥. **وغيرُ المشترِكِ يكسبُ بالمقعدِ المخصوم** (`credit_verdict_at`)، و`chargedSeats === null` يعني «لم يُحكَمْ» ⇒ قاعدةُ ما قبلَ ٠٣٥ حرفيّاً. ⚠️ **وT069 وT071 يبقيانِ في المرحلةِ الخامسة.**
- [ ] T071 [US3] ⛔ أضِفِ العمودَينِ إلى `backend/app/Modules/Settlement/Support/TeacherFieldAllowlist.php` في **`::UNIT` و`::EXPORT_COLUMNS`** بجانبَ `frozen_seats` (`:159` و`:244`) — ⛔ **لا `::STATEMENT`**، وكانَ ذلكَ خطأً في النسخةِ السابقة: `frozen_seats` ليسَ فيها أصلاً، والإضافةُ هناكَ إمّا تترُكُ مشيَ صفِّ الوحدةِ يرسبُ أو **تُصرِّحُ سلفاً بعددِ مقاعدَ على سطحٍ لم يراجعْه أحد**. ⚠️ **و«كم استُهلِك» رقمُ فوترةٍ يبقى ممنوعاً.**
- [ ] T072 [US3] ⛔ راجِعْ قاعدةَ «صفرٌ مريب» في `ChargeUnbilledDeliveriesJob.php`: حصّةٌ بصفرِ **حاضرين** صارَت حالةً عاديّةً — **وحصّةٌ بصفرِ محمَّلينَ لا تزالُ إشارةَ عطب**، فالتمييزُ بينَهما هو كلُّ فائدةِ العمودَين. وأبقِ معاملةَ الحصّةِ الفارغةِ القائمةَ كما هي.
- [ ] T073 [P] [US3] اعرضْ **ثلاثةَ أرقامٍ جنباً إلى جنب** في `frontend/src/app/(app)/(shell)/manage/settlement/page.tsx` و`frontend/src/lib/settlement.ts`: المحجوزُ · الحاضرُ · المحمَّلُ، وجملةً تقولُ إنّ المحاسبةَ على الثالث — ⚠️ **في عقدِه من أوّلِ يومٍ لا في كشفِ حساب**.

---

## Phase 6: الحرّاسُ والصقل

- [ ] T074 ⛔ اكتبْ حارسَ SC-008 في `backend/tests/Feature/Courses/ProgressDenominatorUnchangedTest.php` — ⛔ **وبتوكيدٍ صريحٍ لا بجملةٍ إنشائيّة**: «المقامُ لم يتغيّرْ» ليسَ توكيداً، ومكتوباً `progress_pct === 0` على تجهيزةٍ لم تكنْ عناصرُها في المقامِ أصلاً يبقى أخضرَ تحتَ أيِّ توسيع. الشكلُ: **مجموعةُ المُعرِّفاتِ** التي تُرجِعُها `countableForProgress()`/`progressEligible()` تُساوي قائمةً مكتوبةً باليد، **على تجهيزةٍ فيها تسجيلُ حصّةٍ وامتحانٌ مربوطٌ بحصّةٍ وواجبٌ مربوطٌ بحصّة** — الثلاثةُ التي يجرُّها توسيعٌ بحسنِ نيّة؛ **وتسجيلٌ مقفولٌ لا يقفُ شرطاً لما بعدَه** (الدرسُ التالي يُفتَح). ⚠️ **عائلةُ العطبِ المسجَّلةُ ستَّ مرّات.**
- [ ] T075 ⛔ امنعْ تعديلَ `duration_minutes` أثناءَ الجريانِ في `UpdateClassSession.php` و`UpdateClassSessionRequest.php` — ⛔ **والشرطُ `status === Live` لا `room_opened_at !== null`**: التجهيزاتُ تختمُ `live` بلا طابعٍ زمنيّ، فالصيغةُ الثانيةُ تقلبُ أربعينَ اختباراً قائماً إلى ادّعاءاتٍ عن درسٍ لم يقعْ. ⚠️ **والمنعُ القائمُ يشملُ النهائيَّ وحدَه** (`:26`)، فمدرّسٌ يُنزِلُ المدّةَ في الدقيقةِ الخمسينَ **يُهبِطُ حدَّ الحضورِ فيخصمُ من كلِّ من مرَّ خمسَ دقائقَ ويدفعُ لنفسِه عنهم**. ✅ **وقياسٌ يقولُ إنّ الملفَّينِ هما كلُّ الكتّاب** (ثلاثةُ مواضعِ إنشاءٍ + هذا التحديث؛ لا مورِدَ Filament للحصص، ولا بذرَ يكتبُ العمودَ بعدَ الإنشاء). ⛔ **وانتبهْ لمنادٍ ثالثٍ لا يمرُّ بطلبٍ ولا بنموذج**: `DecideSessionRescheduleRequest.php:109` ينادي `UpdateClassSession` مباشرةً بـ`starts_at` — **فالمنعُ الجديدُ يجبُ أن يخصَّ `duration_minutes` بالاسمِ لا أن يُقفِلَ الفعلَ كلَّه**، وإلّا سقطَ بتُّ التأجيلِ معه. ⚠️ **و`starts_at` على حصّةٍ جاريةٍ يُرفَضُ كذلك** — وهو رفضٌ مفقودٌ اليوم: الطلبُ محروسٌ عندَ `RequestSessionReschedule.php:59` والبتُّ ليسَ محروساً، فطلبٌ قُدِّمَ قبلَ البدايةِ ويُبَتُّ بعدَها ينقلُ موعدَ حصّةٍ **مفتوحةٍ غرفتُها**.
- [ ] T076 [P] ⛔ **جديدةٌ**: اكتبْ حارسَ FR-029 على حصّةٍ **جاريةٍ وغرفتُها مفتوحة** — الاختبارُ على حصّةٍ منتهيةٍ **أخضرُ اليومَ** ولا يقيسُ المنعَ الجديدَ في شيء.
- [ ] T077 [P] اكتبْ `backend/tests/Feature/Payments/VerdictImmutabilityTest.php` لـSC-012 — ⛔ **وثلاثةُ شروطٍ بلا أيٍّ منها يمرُّ على بناءٍ لم يجمّدْ شيئاً**: (أ) مدّةُ القعودِ في التجهيزةِ **بينَ النسبتَينِ** تماماً (٤٠٪ والحدُّ ينتقلُ من ٥٠٪ إلى ٣٠٪) وإلّا فالجوابُ واحدٌ قبلَ وبعد؛ (ب) «إعادةُ القراءة» **إعادةُ تشغيلِ المسارِ** (إرسالُ الحدثِ ثانيةً أو تشغيلُ الكنس) لا `SELECT` — فالقيودُ غيرُ قابلةٍ للتعديلِ أصلاً بحارسٍ قائم؛ (ج) **بطلانُ ذاكرةِ الإعدادِ بعدَ الكتابة**، وإلّا لم تُقرَأِ القيمةُ الجديدةُ أصلاً والاختبارُ يوكّدُ غياباً لتغييرٍ لم يقعْ.
- [ ] T078 [P] وسِّعْ `backend/tests/Feature/Learning/CurriculumQueryBudgetTest.php` (⚠️ **المسارُ مصحَّحٌ — لا مجلّدَ `Performance/`**) — ⛔ **وثلاثةُ تصحيحاتٍ في التجهيزة**: `CurriculumFixtures.php:260` **يختمُ `class_session_id` بالفعل** فالحُجّةُ السابقةُ باطلة؛ الحقيقيُّ أنّ **الحصّةَ واحدةٌ لكلِّ التسجيلات** فتنفيذٌ يُزيلُ التكرارَ بالمُعرِّفِ غيرُ مرئيٍّ في مقارنةِ الصغيرِ بالكبير — **فحصّةٌ لكلِّ تسجيل**؛ **وهي غيرُ مُسلَّمةٍ وبلا حكمٍ مجمَّد** فيمرُّ الفرعُ الجديدُ كلُّه بارتدادِ T032 — **سُلِّمَت وثُبِّتَ حكمُها والطالبُ غيرُ حاضر**؛ **وطلبُ تحميةٍ ثانٍ** قبلَ القياس.
- [ ] T079 [P] ⛔ **جديدةٌ**: اكتبْ ميزانيّةَ نقطةِ الفتحِ — شكلٌ ثابتٌ، طلبا تحميةٍ، وسقفٌ عندَ المستقرِّ المقيسِ + ١. ⚠️ **ستُّ شروطٍ متتاليةٍ ولا شيءَ يقيسُ كلفتَها.**
- [ ] T080 [P] اكتبْ `backend/tests/Feature/Payments/SubscriptionSeatUnaffectedTest.php` — ⛔ **وبشرطَينِ صريحَين**: الاشتراكُ **غطّى** المقعدَ فعلاً (القيدُ الصفريُّ يحملُ مُعرِّفَ الاشتراكِ في بياناتِه)، والطالبُ **لم يحضرْ** — وإلّا فـ«صفرُ خصمٍ» صحيحٌ لأنّه لا شيءَ ليُخصَم، و«المحتوى مفتوح» صحيحٌ لأنّه كانَ حاضراً.
- [ ] T081 [P] اكتبْ `backend/tests/Feature/Payments/UnlockWithoutSeatTest.php` — ⚠️ **وسمِّ الشروطَ الثلاثةَ التي تسبقُ الشرطَ المقصود**: الحصّةُ **مُسلَّمة**، والطالبُ **مموَّل** (وإلّا ٤٢٢ لا ٢٠٠)، والمجموعةُ **ترى الحصّة** — وبغيرِ تسميتِها يقيسُ الاختبارُ الشرطَ الثالثَ أو الرابعَ لا الحقَّ نفسَه. **وحالةُ المنتقلِ تُوكَّدُ على حصّةِ مجموعتِه القديمة.**
- [ ] T082 [P] ⛔ اكتبْ حارسَ `OverrideAttendance` في `backend/tests/Feature/LiveSessions/` — ⛔ **ولم تعدْ «اختباراً بلا تغييرِ كود»**: بعدَ T031 صارَ للفعلِ ذراعٌ ماليّةٌ، والقاعدةُ المقيسةُ هي **الاتّجاه**: علامةٌ تُعلَّمُ بعدَ `credit_verdict_at` **تُعفي وتُرجِعُ** ولا **تخصمُ** أبداً. ⚠️ **والتجهيزةُ لا بدَّ أن تكونَ حصّةً خُصِمَ منها فعلاً**؛ على حصّةٍ لم تُخصَمْ لا شيءَ يتحرّكُ أصلاً والاختبارُ أجوف.
- [ ] T083 [P] اكتبْ حالتَي «العذر» المتعمَّدتَينِ في `backend/tests/Feature/LiveSessions/ExcusedTwoMeaningsTest.php`: **في المال** (`excused_at` على الحجز) لا يُخصَمُ ومحتواه مقفول، **وفي التربية** (`AttendanceStatus::Excused`) يحجزُ التاليةَ عاديّاً — ⚠️ **كلمةٌ واحدةٌ بمعنيَينِ وعمودانِ مختلفان**، وأوّلُ مبرمجٍ يوحّدُهما بحسنِ نيّةٍ يكسرُ أحدَ البابَين.
- [ ] T084 [P] حدِّثْ `docs/README.md` و`docs/erd.md` بالجدولَينِ والأعمدةِ الستّةِ وجدولِ الحالاتِ الستّ.
- [ ] T085 شغِّلْ `php vendor/bin/pest` على **المجلّداتِ المتغيّرةِ وحدَها** — Payments · LiveSessions · Learning · Settlement · Courses · Community · Tenancy · **Assessments** · **Media** · **Notifications** (⚠️ **الثلاثةُ الأخيرةُ سقطَت من النسخةِ السابقةِ وT039 وT022 يلمسانِها؛ و`Performance` حُذِفَ لأنّه غيرُ موجود**) — **مرّةً واحدةً في النهاية**، ثمّ `./vendor/bin/pint` و`./vendor/bin/phpstan analyse`، و`npm test` و`npx tsc --noEmit` في `frontend/`.

---

## Dependencies

```
Phase 1 (المخطَّط)          ──┐
Phase 2 (المُعيق) ⛔          ─┴─→  كلُّ ما بعدَه. لا استثناء.
                                   T010 قبلَ T011 · T012 · T014 (الأسماء)
                                   T013 قبلَ T057 · T060 · T061 (الناقل)
                                   T015 → T016 قبلَ أيِّ توكيدِ فوترة
                                   T021 قبلَ T022  (كانَ معكوساً)

Phase 3 (US1) ──→ Phase 4 (US2)   T027–T029 تُنتِجُ الحكمَ الذي يختمُه T057
             └──→ Phase 5 (US3)   T028 يُنتِجُ العددَ الذي يقرأُه T070
Phase 6 يقرأُ الثلاثةَ معاً
```

⚠️ **وUS1 وUS2 وUS3 لا تُشحَنُ منفردة** (FR-021).

## Parallel Opportunities

- **Phase 1**: T001–T007 معاً، ثمّ T008 · T009.
- **Phase 2**: T014 · T019 · T020 · T021 معاً بعدَ T010؛ وT013 مستقلٌّ بعدَ T012.
- **Phase 3**: الاختباراتُ T023–T026 معاً؛ والواجهةُ T049–T052 معاً بعدَ T047.
- **Phase 4**: T054 · T055 معاً؛ T064 · T065 · T067 · T068 معاً.
- **Phase 6**: T076–T084 كلُّها معاً.

## MVP

⛔ **لا MVP في هذه الشحنة، وهذا قرارٌ لا سهو.** أصغرُ شيءٍ يُشحَنُ هو **Phase 1 + 2 + US1 + US2 + US3**. شحنُ US1 وحدَها يُنتِجُ منتَجاً يحجزُ فيه الطالبُ مجّاناً وتدفعُ فيه المنصّةُ عن كلِّ غائب — **أسوأُ من اليومِ لا أفضل**.

## عددُ المهامّ

| الطور | العدد |
|---|---|
| ١ — المخطَّط | ٩ |
| ٢ — المُعيق | ١٣ |
| ٣ — US1 | ٣١ |
| ٤ — US2 | ١٥ |
| ٥ — US3 | ٥ |
| ٦ — الحرّاسُ والصقل | ١٢ |
| **المجموع** | **٨٥** |
