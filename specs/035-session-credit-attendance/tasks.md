# Tasks: ٠٣٥ — الرصيدُ بالحصص

**Input**: `specs/035-session-credit-attendance/` — spec.md · plan.md · research.md (R1–R18) · data-model.md · contracts/README.md · quickstart.md

**Tests**: مطلوبةٌ صراحةً. المواصفةُ تحملُ ١٥ معياراً مقيساً، **وثلاثةٌ منها لا يمكنُ إثباتُها إلّا باختبارٍ يُكتَبُ قبلَ التنفيذ** (SC-003 · SC-008 · SC-012). ⚠️ **وخطرُ هذه الشحنةِ الأوّلُ ليسَ اختباراً يرسب، بل اختباراً يمرُّ أخضرَ على صفر** — ولذلك الطورُ الثاني كلُّه تجهيزاتٌ وحرّاس.

**Organization**: بالقصص، مع طورٍ مُعيقٍ قبلَها. ⚠️ **والقصصُ الثلاثُ تُشحَنُ معاً أو لا تُشحَن** (FR-021): US1 بلا US2 يجعلُ الحجزَ مجّانيّاً، وUS1 بلا US3 يجعلُ المنصّةَ تدفعُ من جيبِها عن كلِّ غائب. التقسيمُ هنا **ترتيبُ بناءٍ ومراجعةٍ لا خطّةُ إصدار**.

---

## نطاقٌ سقطَ قبلَ كتابةِ هذه المهامّ (2026-09-12)

⛔ **قسمُ المقامِ والشهادةِ كلُّه خارجُ الشحنة** — الشهادةُ غيرُ إلزاميّةٍ في كورساتِ الايف. **لا مهمّةَ واحدةً هنا تلمسُ** `CourseProgress` · `ResyncCourseProgress` · `EloquentProgressImpact` · `PreviewPublishImpact` · `Lesson::scopeProgressEligible()` · `Lesson::accessTo()` · `CourseDuration`. و**T064 يقيسُ ذلك**.

---

## Phase 1: المخطَّط — ثمانيةُ ترحيلاتٍ لا تسعة

**Purpose**: الجداولُ والأعمدةُ أوّلاً، لأنّ كلَّ ما بعدَها يقرأُها. لا منطقَ هنا.

- [ ] T001 [P] أضِفْ `held_credits` (`integer` · NOT NULL · default 0) إلى `credit_balances` في ترحيلٍ جديدٍ تحت `backend/app/Modules/Payments/Database/Migrations/` — ⚠️ **مُوقَّعٌ لا غيرُ مُوقَّع**، مع سببِه مكتوباً بجانبَه: `remaining − held` يقعُ في `WHERE` في **كلِّ حجزٍ على المنصّة**، وMySQL يرفعُ `ERROR 1690` على عمودٍ غيرِ موقَّعٍ بقيمةٍ سالبة، وSQLite لا تُظهِرُ ذلكَ أبداً.
- [ ] T002 [P] أنشِئْ جدولَ `credit_holds` في ترحيلٍ تحت `backend/app/Modules/Payments/Database/Migrations/` بالأعمدةِ التسعةِ في data-model §١ — ⚠️ **`hold_seq` (`unsignedInteger` NOT NULL default 0) ثالثاً في المفتاحِ الفريد** `unique(credit_balance_id, class_session_id, hold_seq)`: المقعدُ يُحيا شرعاً بعدَ إطلاقِه، ومفتاحٌ بعمودَينِ يُنتِجُ إمّا حصّةً ببلاش (بالتجاهُلِ عندَ التصادم) أو خطأَ خادمٍ على إعادةِ حجزٍ مشروعة.
- [ ] T003 [P] أضِفْ فهرسَي `credit_holds` في نفسِ ترحيلِ T002: `index(settled_at, credit_balance_id)` و`index(student_user_id, settled_at)` — ⚠️ **القراءتانِ الدوريّتانِ تسألانِ `settled_at IS NULL` بلا طالبٍ ولا رصيد**، فلا المفتاحُ الفريدُ يخدمُهما: بدونَهما مسحٌ كاملٌ كلَّ ربعِ ساعةٍ لجدولٍ ينمو صفّاً لكلِّ حجزٍ إلى الأبد.
- [ ] T004 [P] أنشِئْ جدولَ `session_unlocks` بأعمدةِ data-model §٢ و`unique(student_user_id, class_session_id)` في ترحيلٍ تحت `backend/app/Modules/Payments/Database/Migrations/` — ⚠️ **تحت Payments وليسَ LiveSessions**: جدارُ عزلِ السياقِ يشتقُّ قائمةَ جداولِ كلِّ سياقٍ من `Schema::create` في هجراتِ وحدتِه، فجدولٌ هنا تحت LiveSessions يصيرُ **مسموحاً لحسابِ المدرّسِ أن يستعلمَ عنه**.
- [ ] T005 [P] أضِفْ `credit_verdict_at` (`timestamp` · nullable) إلى `attendances` في ترحيلٍ تحت `backend/app/Modules/LiveSessions/Database/Migrations/`.
- [ ] T006 [P] أضِفْ `attended_seats` و`verdict_stay_seconds` (`unsignedInteger` · nullable) إلى `class_sessions` في ترحيلٍ تحت `backend/app/Modules/LiveSessions/Database/Migrations/` — **فارغٌ يعني «لم يُحسَبْ بعد»، لا صفراً**.
- [ ] T007 أنشِئْ نموذجَي `CreditHold` و`SessionUnlock` في `backend/app/Modules/Payments/Models/` بـ`HasUuid` و`BelongsToWorkspace`، و⚠️ **أخرِجْ `stay_seconds` من `$fillable` في `backend/app/Modules/LiveSessions/Models/Attendance.php`** في نفسِ المهمّة: لا كاتبَ اليومَ يمرُّ بالإسنادِ الجماعيّ، **لكنّ العمودَ يصيرُ مالاً بعدَ ٠٣٥**.
- [ ] T008 [P] أضِفِ الجدولَينِ الجديدَينِ إلى `backend/app/Modules/Payments/Support/PaymentsPersonalData.php` (مساراتُ التصدير · المحو · الانتهاء) وأنشِئْ ترحيلَ صفوفِ `data_categories` لهما بـ`seedMissing()` — ⚠️ **الحارسُ القائمُ لكلِّ وحدة، فجدولٌ جديدٌ داخلَ وحدةٍ مسجَّلةٍ أصلاً لا يراه أحد**: تُضافُ الثلاثُ في نفسِ التغييرِ أو لن يُنبِّهَ شيء.

---

## Phase 2: الطورُ المُعيق — لا شيءَ يُترجَمُ ولا **يقيسُ** شيئاً قبلَه

**Purpose**: العقودُ التي بدونِها لا يُترجَمُ الكود، **والحرّاسُ الذين بدونَهم يمرُّ كلُّ توكيدٍ في الشحنةِ أخضرَ على صفر**. ⛔ **T013 أخطرُها**: مساعدُ التجهيزِ القائمُ يُسجِّلُ حضورَ **المدرّسِ وحدَه** ولا يُدخِلُ طالباً قطُّ، وهو مُستدعىً في **خمسةِ ملفّاتِ فوترة** — فأوّلَ ما يصيرُ الخصمُ مشروطاً بالحضورِ تصيرُ الخمسةُ كلُّها «صفرٌ يساوي صفراً».

- [ ] T009 استخرجْ قوائمَ النصوصِ المحظورةِ فعليّاً من `backend/tests/Feature/Architecture/ContextIsolationTest.php` (فرعُ Settlement: أسماءُ أحداثِ Payments كأسماءِ ملفّاتٍ مجرَّدة + أسماءُ جداولِها)، ثمّ اكتبِ **الأسماءَ المختارةَ للعقدِ والحدثَينِ الجديدَين** في `specs/035-session-credit-attendance/contracts/README.md` بعدَ مطابقتِها عليها — ⚠️ **فخٌّ مزدوجٌ مقيس**: FR-032 يضيفُ مفرداتِ حضورٍ إلى ملفِّ حقولِ المدرّسِ في اللحظةِ نفسِها التي قد يصيرُ فيها اسمُ حدثٍ جديدٍ نصّاً محظوراً هناك. **الشطرانِ يُسقِطانِ بناءَ بعضِهما.**
- [ ] T010 أنشِئْ `backend/app/Shared/Contracts/SessionContentAccess.php` بالتوقيعاتِ الثلاثةِ في contracts/README §١ (`mayOpenSessionContent` · `openableSessionIds` · `unlockOfferFor`) — ⚠️ **الصورةُ الجُمليّةُ شرطُ صحّةٍ لا تحسين**: قراءةُ المنهجِ أسخنُ قراءةٍ في المنتَجِ وتكلفتُها اليومَ ثابتةٌ مهما كَبُرَت الشجرة.
- [ ] T011 أنشِئْ **عقدَ الكتابةِ** لحجزِ الرصيدِ في `backend/app/Shared/Contracts/` بالاسمِ الذي أقرَّه T009 — ⚠️ **هذا أوّلُ عقدِ كتابةٍ في المستودعِ (السبعةَ عشرَ القائمةُ كلُّها للقراءة)، وهو قرارٌ يُكتَبُ في رأسِ الملفِّ لا يُمرَّر**: الحجزُ يجبُ أن يقعَ داخلَ معاملةِ `BookSeat` ليُرَدَّ المقعدُ إن رُفِضَ الرصيد، وجدارُ العزلِ يسمحُ لـLiveSessions بسطرِ استيرادٍ واحدٍ حرفيٍّ وليسَ هذا هو، ومستمعٌ مُطابورٌ بعدَ الالتزامِ لا يستطيعُ ردَّ مقعدٍ التزم. **والبديلُ الوحيدُ مستمعٌ متزامنٌ — وهو نقطةُ الفشلِ المفردةُ التي سجّلَ هذا المستودعُ ثمنَها مرّة.**
- [ ] T012 [P] أضِفْ **عددَ الحاضرينَ عدداً صحيحاً** إلى `backend/app/Modules/LiveSessions/Events/SessionDelivered.php` — ⚠️ **عدداً لا نموذجاً ولا مُعرِّفاً**: الحدثُ يحملُ `Dispatchable` وحدَه بلا `SerializesModels`، والتحديثُ الشرطيُّ الذي يكتبُ العمودَ **لا يُحدِّثُ النموذجَ في الذاكرة**، وقراءةٌ فارغةٌ تُرسِلُ **كلَّ** حصّةٍ إلى فرعِ «الحصّةُ الفارغة» الذي يكتبُ مُعرِّفَ المدرّسِ مكانَ الطالبِ ويرفعُ رايةَ المراجعة.
- [ ] T013 ⛔ أصلِحْ مساعدَ تجهيزِ الحصّةِ المسلَّمةِ في `backend/tests/Pest.php` ليُسجِّلَ حضورَ **طالبٍ** بمدّةِ قعودٍ حقيقيّة، لا حضورَ المدرّسِ وحدَه — ⚠️ **قبلَ أيِّ توكيدِ فوترةٍ في الأطوارِ التالية**: المساعدُ مُستدعىً في خمسةِ ملفّاتِ فوترة، وتجهيزةُ الحضورِ الافتراضيّةُ «غائبٌ · صفرُ ثوانٍ»، فبدونِ هذا تصيرُ كلُّ توكيداتِ الفوترةِ القائمةِ «صفرٌ يساوي صفراً» **وتمرُّ خضراءَ بلا أن تقيسَ شيئاً**.
- [ ] T014 ⛔ **اقلِبْ** — لا تحذفْ — الاختبارَينِ القائمَينِ اللذَينِ يوكّدانِ عكسَ FR-002 (الغائبُ يُخصَمُ منه كالحاضر) تحت `backend/tests/Feature/Payments/`، وأضِفْ في كلٍّ منهما تعليقاً يقولُ متى ولماذا انقلبَ الحكم. ⚠️ **وأحدُهما يُعطِّلُ طابورَ المهامِّ تعطيلاً شاملاً فيبتلعُ مستمعَ الخصمِ أصلاً** — استبدلْه بتعطيلِ مهامِّ الخطِّ الزمنيِّ وحدَها (`fakeSessionTimeline()`)، وإلّا فالاختبارُ المقلوبُ يوكّدُ على جدولٍ فارغ.
- [ ] T015 ⛔ أعِدْ كتابةَ ثابتِ «قيدُ استهلاكٍ واحدٌ لكلِّ مقعدٍ في حصّةٍ محسوبة» في `backend/app/Modules/Payments/Jobs/ReconcileCreditBalancesJob.php` ليصيرَ **«قيدٌ لكلِّ مقعدٍ، بقيمةِ ١ لمن حُمِّلَ و٠ لمن لم يُحمَّلْ»** — ⚠️ **في الشحنةِ نفسِها**: بدونَها تُبلَّغُ كلُّ حصّةٍ فيها غائبٌ **كلَّ ليلةٍ إلى الأبد**، فيصيرُ التقريرُ الذي وُضِعَ ليكشفَ حصّةً لم تُحاسَبْ ضجيجاً كلَّه. **وثابتٌ يُولَدُ أحمرَ هو ثابتٌ يُحذَف.**
- [ ] T016 [P] أصلِحِ البذورَ الأربعَ التي تكتبُ حجوزاً وحضوراً وحركاتِ رصيدٍ تحت `backend/database/seeders/` لتكتبَ الأعمدةَ الجديدة — ⚠️ **جزءٌ من الشحنةِ لا تنظيفٌ بعدَها**: أوّلُ `migrate:fresh --seed` بدونَها يُعيدُ التقريرَ الليليَّ أحمرَ على بياناتٍ صنعَها المستودعُ بنفسِه.
- [ ] T017 [P] اربِطِ العقدَينِ في `backend/app/Modules/Payments/PaymentsServiceProvider.php` — ⚠️ **`scoped()` لا `bind()` ولا `singleton()`**: العقدُ يُسألُ عشراتِ المرّاتِ في قراءةِ منهجٍ واحدة (فـ`bind()` يعيدُ البناءَ في كلٍّ منها)، وحاويةُ العاملِ تعيشُ أطولَ من المهمّة (فـ`singleton()` يخدمُ حالةً قديمةً حتى يُعادَ تشغيلُه).
- [ ] T018 [P] أضِفْ ترحيلَ قوالبِ الرسائلِ الناقصةِ بـ`seedMissing()` (`firstOrCreate`) تحت `backend/app/Modules/Notifications/Database/Migrations/` للأنواعِ التي يسمّيها T044 — ⚠️ **`run()` ممنوعٌ في مسارِ النشر**: كلُّ صفٍّ قابلٌ للتعديلِ من اللوحة، و`updateOrCreate` يمسحُ كلَّ نصٍّ عرَّبَه مشغِّلٌ عندَ **كلِّ إصدار**. ورسالةٌ بلا قالبٍ **تُسقَطُ صامتةً**.

**Checkpoint**: بعدَ هذا الطورِ يُترجَمُ الكودُ، **وأيُّ توكيدٍ يُكتَبُ بعدَه يقيسُ ما يدّعي أنّه يقيسُه**.

---

## Phase 3: US1 — الخصمُ بالحضورِ أو بالتقصير، والإلغاءُ في الوقتِ يُقفِلُ ولا يخصم (P1)

**Goal**: طالبةٌ ألغَت في الوقتِ لا يُخصَمُ منها ومحتواها مقفولٌ حتى توافق؛ وطالبٌ تخلّفَ صامتاً يُخصَمُ منه **ويُفتَحُ له المحتوى بلا موافقة**.

**Independent Test**: حصّةٌ واحدةٌ وثلاثةُ طلّابٍ في **تجهيزةٍ واحدة** — حاضرٌ · مُلغٍ في الوقتِ · متخلّفٌ صامت ⇒ ثلاثةُ أجوبةٍ مختلفة. ⚠️ **الثلاثةُ معاً أو المعيارُ لا يقيسُ الفرقَ بينَهم.**

### اختباراتٌ تُكتَبُ أوّلاً

- [ ] T019 [P] [US1] اكتبْ `backend/tests/Feature/Payments/SessionVerdictTest.php`: الحالاتُ الخمسُ في FR-008د كلٌّ على حِدة (حضر · أخطرَ في الوقت · تخلّفَ صامتاً · عذرٌ من المدرّس · لم تُسلَّمِ الحصّة) — بثلاثةِ توكيداتٍ لكلِّ حالة: **رصيدُ الطالب · مستحقُّ المدرّس · حالةُ المحتوى**.
- [ ] T020 [P] [US1] اكتبْ `backend/tests/Feature/Payments/UnlockIdempotencyTest.php` لـSC-003 — ⚠️ **يُقاسُ بمساواةِ عددِ الخصوماتِ لعددِ الحصصِ المفتوحة، لا بعدِّ صفوف**: عدُّ صفَّينِ يمرُّ على تصميمٍ لا يُرجِعُ شيئاً.
- [ ] T021 [P] [US1] اكتبْ `backend/tests/Feature/Learning/SessionContentGateTest.php`: الأبوابُ الستّةُ **كلٌّ على حِدة** (بوّابةُ الدرسِ المفردةُ · الجُمليّةُ · منحُ التشغيل · ملفّاتُ الحصّة · الامتحانُ والواجب · نقاشُ الغرفة) — SC-002 يُقاسُ على أربعةِ أصنافٍ منفصلةٍ لا على واحد.
- [ ] T022 [P] [US1] اكتبْ حالةَ **القراءةِ لا الكتابة** في نقاشِ الغرفةِ في `backend/tests/Feature/Community/RoomThreadUnlockTest.php` — ⚠️ **بابُ الكتابةِ يسألُ بابَ القراءةِ أوّلاً**، فتوسيعُ الثاني يوسّعُ الأوّلَ **صامتاً** ما لم يُنَصَّ على الفصل (FR-013ب).

### التنفيذ

- [ ] T023 [US1] ⛔ ثبِّتِ الحكمَ في `backend/app/Modules/LiveSessions/Actions/CloseClassSession.php`: `credit_verdict_at` لمن بلغَ الحدَّ **داخلَ الحلقةِ القائمةِ التي تُكمِلُ السجلَّ** — لا حلقةَ جديدةَ ولا استعلامَ لكلِّ مقعد. ⚠️ **ولا يُشتقُّ الحكمُ من `status` ولا من `auto_status` أبداً**: الأولى يكتبُها المدرّسُ (FR-004)، و«متأخّر» هي حكمُ من دخلَ متأخّراً وقعدَ طويلاً **وحكمُ من دخلَ في وقتِه ومشيَ بعدَ دقيقتَين** — و٠٣٥ يخصمُ الأوّلَ ولا يخصمُ الثاني.
- [ ] T024 [US1] ⛔ اكتبْ `attended_seats` و`verdict_stay_seconds` على صفِّ الحصّةِ بتحديثٍ شرطيٍّ واحدٍ في `CloseClassSession` — ⚠️ **`verdict_stay_seconds` هو ما يجعلُ SC-012 صحيحاً**: النسبةُ المطلوبةُ صفٌّ يعدّلُه المشغِّل، فحفظُ **الرقمِ المُطبَّقِ فعلاً** يعني أنّ تحريكَ الإعدادِ لا يُعيدُ الحكمَ على ماضٍ. ولا قراءةَ ثمّ كتابة: عاملانِ يقرآنِ الحضورَ بفارقِ ثانيةٍ يجمّدانِ رقمَينِ مختلفَين وأحدُهما أجرُ المدرّس.
- [ ] T025 [US1] ⛔ طبِّقْ **شرطَ التقصير** في نفسِ الحلقة: من لم يبلغِ الحدَّ **وحجزُه ليسَ إلغاءً في الوقتِ ولا عذراً من المدرّس** ⇒ حكمُه حكمُ الحاضرِ في المالِ وفي فتحِ المحتوى (FR-008ج). ⚠️ **والجوابُ يُقرَأُ من العمودِ القائمِ `bookings.is_billable` وحالةِ الحجز**، لا من حسابٍ جديدٍ للموعد: `CancelBooking` يحسبُ الموعدَ مرّةً ويكتبُ الجوابَ في الصفّ، **وتهجئةٌ ثانيةٌ لسؤالٍ واحدٍ هي العطبُ المسجَّلُ في هذا المستودعِ عشراتِ المرّات**.
- [ ] T026 [US1] عدِّلْ `backend/app/Modules/Payments/Actions/ChargeSessionSeats.php` ليقرأَ الحكمَ المجمَّدَ بدلَ «كلِّ مقعد» — ⚠️ **والفرعُ يُقرَأُ من الحصّةِ لا من صفِّ الحضور**: `attended_seats` فارغٌ على الحصّةِ ⇒ **ارجعْ إلى قاعدةِ ما قبلَ ٠٣٥** (كلُّ مقعدٍ يُخصَم). النشرُ يرفعُ الكودَ **قبلَ** الهجرة، وEloquent يُرجِعُ فراغاً لعمودٍ غيرِ موجودٍ ولا يرمي خطأً، وكنسُ المحاسبةِ يعملُ كلَّ ربعِ ساعةٍ ثمّ **يختمُ الحصّةَ فتخرجُ من شرطِ الكنسِ إلى الأبد**. مقيسٌ: **حصّتانِ في هذه الحالةِ في قاعدةِ التطويرِ الآن**.
- [ ] T027 [US1] اكتبْ قيدَ الاستهلاكِ **بقيمةِ صفرٍ** لمن لم يُحمَّلْ في `ChargeSessionSeats`، بدلَ تركِه بلا قيد — ⚠️ **الصفرُ هو ما يحفظُ الثابتَ الليليَّ** (R4 · T015)، وهو نفسُ ما يفعلُه مقعدُ الاشتراكِ اليومَ بالضبط.
- [ ] T028 [US1] أنشِئْ `backend/app/Modules/Payments/Actions/UnlockSessionContent.php` — صفُّ `session_unlocks` + قيدُ الدفترِ في **معاملةٍ خارجيّةٍ واحدة**. ⚠️ **ومصدرُ القيدِ مستقلٌّ (`session_unlock`) لا `class_session`**: مفتاحُ عدمِ التكرارِ أربعةُ أعمدةٍ وخصمُ المقعدِ يشغلُ التركيبةَ بالفعل، فالدفترُ يقرأُ التصادمَ «خُصِمَ قبلَ ذلك» ويُرجِعُ فراغاً، والفعلُ يقرأُ الفراغَ نجاحاً ⇒ **يُفتَحُ المحتوى ولا يُخصَمُ شيءٌ ولا يُسجَّلُ شيء**، وSC-003 أخضرُ على صفر. **وجدَها أربعةٌ من خمسةِ مراجعين.**
- [ ] T029 [US1] ⛔ فعِّلْ **أرضيّةَ الرصيدِ** في مسارِ الفتحِ وحدَه (`enforceFloor: true`)، **وليطرحِ الشرطُ المحجوزَ أيضاً**: `remaining − held ≥ 0` — ⚠️ **الأرضيّةُ مُطفأةٌ افتراضيّاً بحُجّةٍ صحيحةٍ في موضعِها وباطلةٍ هنا**: دَينُ حصّةٍ سُلِّمَت يُسجَّلُ ولو عجزَ الطالب، أمّا الفتحُ فشراءٌ اختياريّ. وإن نُسِيَت: ينزلُ الرصيدُ تحتَ الصفرِ فيُعَدُّ الطالبُ متعثّراً **فيُقفَلُ عليه كلُّ حجزٍ قادمٍ وكلُّ درسٍ في كورسٍ دفعَ ثمنَه** — بضغطةِ الزرِّ الذي تمنعُ FR-013 تعطيلَه.
- [ ] T030 [US1] نفِّذْ `backend/app/Modules/Payments/Support/EloquentSessionContentAccess.php` — الدالّاتُ الثلاث، **والجُمليّةُ باستعلامٍ واحدٍ يُختَمُ على الصفوف**.
- [ ] T031 [US1] أضِفْ `POST /api/v1/class-sessions/{session}/unlock` في `backend/app/Modules/Payments/Http/` بترتيبِ الشروطِ الستّةِ في contracts/README §٢ **كما هو مكتوبٌ ولا يُعادُ ترتيبُه** — ⚠️ **الشرطُ الأوّلُ يُحَلُّ بالاستعلامِ صراحةً لا بالربطِ الضمنيّ**: الطالبُ عضوٌ في لا مساحةَ عمل، فالنطاقُ العامُّ **عديمُ الأثرِ عليه** والربطُ الضمنيُّ يَحُلُّ أيَّ حصّةٍ على المنصّةِ كلِّها. ⚠️ **والرابعُ شقّان**: سُلِّمَت **ومادّتُها لم تُؤرشَفْ** (FR-039أ) — عرضُ فتحٍ لمادّةٍ محذوفةٍ **بيعُ ما لا يُسلَّم**.
- [ ] T032 [US1] اجعلِ الأبوابَ الستّةَ تسألُ العقدَ — `backend/app/Modules/Learning/Support/LessonGate.php` (المفردُ والجُمليُّ) · `backend/app/Modules/Media/Actions/IssuePlaybackGrant.php` · `backend/app/Modules/Assessments/Policies/` · `backend/app/Modules/Community/Policies/ConversationPolicy.php` — ⚠️ **ولا سطرَ استيرادٍ واحدٍ من Payments في Learning ولا في Community**: الجدارُ يُسقِطُ البناءَ على **صفرِ ذكرٍ**، **والتعليقاتُ تُجرَّدُ قبلَ الفحصِ فلا تُنقِذُ سطراً**.
- [ ] T033 [US1] في `backend/app/Modules/Community/Policies/ConversationPolicy.php`: الفتحُ يمنحُ **القراءةَ وحدَها**، وحقُّ الكتابةِ يبقى معلَّقاً على المقعد (FR-013ب) — والتعليقُ بجانبَه يقولُ إنّ `post()` يسألُ `view()` أوّلاً.
- [ ] T034 [US1] ⛔ في `backend/app/Modules/LiveSessions/Support/BookingEligibility.php`: تخطَّ شرطَ واجبِ الحصّةِ السابقةِ **إذا كانَ الواجبُ نفسُه مقفولاً** (FR-013ج) — ⚠️ حُجّةُ الشرطِ المكتوبةُ أنّ الواجبَ **مبلوغٌ دائماً**، وFR-008 ينقضُها لأوّلِ مرّة: غيابٌ برصيدٍ صفرٍ ⇒ واجبٌ مقفولٌ ⇒ شرطٌ لا يُستوفى أبداً ⇒ **الطالبُ لا يحضرُ مرّةً أخرى إلّا بالمال**.
- [ ] T035 [US1] افتحْ محتوى الحصّةِ **تلقائيّاً بصفرِ حصصٍ** في `UnlockSessionContent` لثلاثِ حالات: مقعدُ الاشتراك (`reason: subscription`) · من أُخرِجَ تأديبيّاً (`removed_from_room`) · والمتخلّفُ المخصومُ منه (`charged_absence`) — ⚠️ **و`reason` ليسَ زينة**: صفٌّ بصفرِ حصصٍ بلا سببٍ مكتوبٍ **لا يُميَّزُ عن عطبٍ لمن يقرأُ الجدولَ بعدَ شهر**. والمشترِكُ لا يملكُ رصيداً أصلاً، فقفلٌ بثمنٍ لا يستطيعُ دفعَه **قفلٌ بلا مخرج**.
- [ ] T036 [P] [US1] وسِّعْ `backend/app/Modules/LiveSessions/Http/Resources/ClassSessionResource.php` وموردَ المنهجِ بـ`content_locked` · `unlock_offer|null` (وفيه `available_until`) · `locked_session_count` — **بلا مفتاحٍ يُحذَف، وبلا مبلغٍ نقديٍّ في أيٍّ منها**: ثمنُ الحصّةِ سعرُ تسويةِ المدرّسِ زائدَ ثابتَين، **فرقمٌ نقديٌّ على شاشةِ الطالبِ يُحَلُّ منه سعرُ المدرّس**.
- [ ] T037 [US1] أنشِئْ `frontend/src/app/(app)/(shell)/sessions/[uuid]/page.tsx` (FR-037) — حالةُ الحصّةِ · المقفولُ منها · زرُّ الفتحِ وعرضُه · طريقُ الشراء · **وزرُّ الغرفةِ ما دامتِ النافذةُ مفتوحة**. ⚠️ **والقفلُ يُقرَأُ من الحمولةِ ولا يُعادُ اشتقاقُه في TypeScript**: إعادةُ اشتقاقِ «هل هذه مقفولة؟» هي عينُ العطبِ الذي جعلَ تسجيلاً **مدفوعاً** غيرَ قابلٍ للفتحِ في ٠١٨.
- [ ] T038 [US1] ⛔ انقلِ الروابطَ الأربعةَ التي تشيرُ اليومَ إلى `/sessions/{uuid}/room` في `frontend/src/` لتشيرَ إلى `/sessions/{uuid}` **في نفسِ التغيير** — ⚠️ **سطحٌ بلا رابطٍ يصلُه ميزةٌ لا يملكُها أحد**، وهذا المستودعُ سجّلَ ثلاثَ حالاتٍ منها في يومٍ واحد. **ويبقى رابطُ الغرفةِ داخلَ الصفحة**، وإلّا كانَ النقلُ هو العطب.
- [ ] T039 [P] [US1] أنشِئْ `frontend/src/components/sessions/UnlockPanel.tsx` — الرقمُ والرصيدُ وقائمةُ ما سيُفتَحُ **عدداً وأصنافاً لا عناوين**، وطريقُ الشراءِ **دائماً** لا عندَ الرصيدِ الصفرِ وحدَه. ⚠️ **قائمةُ عناوينِ أوراقِ ساعةٍ هي خطّةُ درسٍ لم يحضرْه صاحبُها**؛ **وحمولةٌ يتغيّرُ شكلُها بما وجدَت جوابٌ مميَّزٌ يُخبِرُ السائلَ متى اقتربَ**.
- [ ] T040 [P] [US1] أضِفْ رمزَ المنعِ السابعَ «اشترِ وافتحْ» في `frontend/src/components/courses/LessonRow.tsx` و`frontend/src/lib/curriculum.ts` — ⚠️ **الرمزُ يُقرَأُ من الخادم**، والعددُ يمرُّ بـ`counted()` بخانةِ `other` صريحةً: «١ حصّة» و«٢ حصّتان» و«١٠٠ حصّة» ثلاثُ صيغٍ لا واحدة.
- [ ] T041 [P] [US1] صحِّحْ `frontend/src/app/(public)/pricing/page.tsx` — تقولُ اليومَ «الخصمُ بالتسليم» وستكذبُ يومَ الشحن.
- [ ] T042 [P] [US1] اكتبْ `frontend/src/components/sessions/UnlockPanel.test.tsx` (vitest) — ⚠️ **زرٌّ بلا رقمٍ ليسَ اختياراً**، والتوكيدُ على **النصِّ المعروضِ** لا على استدعاءِ الدالّة.
- [ ] T043 [US1] أضِفْ صنفَ الفتحِ إلى `backend/app/Modules/Payments/Support/BillingAuditSubjects.php` — بدونَه **FR-012 مُستوفىً على الورقِ وغيرُ مرئيٍّ في الشاشةِ الوحيدةِ التي يفتحُها المدقّق**.
- [ ] T044 [US1] ⛔ اعدُدْ صراحةً أنواعَ الإشعاراتِ التي يحتاجُها هذا العملُ واكتبِ العددَ في `specs/035-session-credit-attendance/spec.md`، ثمّ نفِّذْها عبرَ `DispatchNotification` وحدَه — ⚠️ **المواصفةُ لا تسمّي حدثَ إشعارٍ واحداً (مقيسٌ بالبحث)**، وإضافةُ نوعٍ تكلّفُ أربعَ مطابقاتٍ شاملةٍ بلا ذراعٍ افتراضيّة، **وأيُّ نوعٍ يُخاطِبُ وليَّ الأمرِ يُسقِطُ اختبارَ العددِ الدقيقِ للقناةِ الخارجيّة** (٢٥ اليوم). ولا ذكرَ لقناةٍ في أيِّ ملفٍّ تحت `Actions/`.

**Checkpoint**: US1 تعملُ من طرفٍ إلى طرف، **والأبوابُ الستّةُ تسألُ سؤالاً واحداً**.

---

## Phase 4: US2 — الحجزُ يحجزُ الرصيدَ ولا يخصمُه (P2)

**Goal**: طالبٌ برصيدِ ٣ يحجزُ ٣ ⇒ الرابعةُ تُرفَضُ بجملةٍ تقولُ **متى يُفرَجُ أوّلُ حجز**؛ ويُلغي في الوقتِ ⇒ يعودُ فوراً.

**Independent Test**: كما في المواصفة — **مع حالةِ إعادةِ الحجزِ بعدَ الإطلاق**، وهي الحالةُ التي وُجِدَ `hold_seq` لأجلِها.

- [ ] T045 [P] [US2] اكتبْ `backend/tests/Feature/Payments/CreditHoldLifecycleTest.php`: وضعٌ · ختمٌ إلى خصم · ختمٌ إلى إفراج · **وختمٌ مزدوجٌ من مستمعٍ وكنسٍ معاً** ⇒ العدّادُ يتحرّكُ **مرّةً واحدة**.
- [ ] T046 [P] [US2] اكتبْ حالةَ **المقعدِ المُحيا** في `CreditHoldLifecycleTest.php`: احجزْ ⇒ أُطلِقَ ⇒ خُتِمَ ⇒ **أُحيِيَ** ⇒ صفُّ حجزٍ ثانٍ بـ`hold_seq = 1` بلا تصادمٍ وبلا صمت.
- [ ] T047 [US2] أنشِئْ `backend/app/Modules/Payments/Actions/PlaceCreditHold.php` — صفٌّ + `held_credits + 1` بتحديثٍ شرطيٍّ واحد، **والكاسبُ وحدَه يُحرِّكُ العدّاد**. و`workspace_id` **يُسنَدُ صراحةً لا بالملءِ التلقائيّ**: الختمُ يقعُ في مستمعٍ مُطابورٍ بلا سياق.
- [ ] T048 [US2] أنشِئْ `backend/app/Modules/Payments/Actions/SettleCreditHold.php` — `UPDATE … WHERE id = ? AND settled_at IS NULL` — ⚠️ **بلا هذا يُفكُّ الحجزُ مرّتَين**: الختمُ يقعُ من مستمعٍ **ومن كنسٍ دوريٍّ يلتقطُ ما لم يُختَمْ**، وهما يشتغلانِ على المقاعدِ نفسِها **بالتصميم**. وهو آمنٌ في الخصمِ اليومَ لأنّ كلَّ كتابةٍ قيدٌ يمتصُّه فهرسُ الدفتر — **والإفراجُ ليسَ قيداً**، فالطالبُ يكسبُ حصّةً لم يشترِها **ولا ثابتَ ليليٌّ يراها**. و`lockForUpdate()` **ممنوع** (بلا أثرٍ على SQLite).
- [ ] T049 [US2] استدعِ عقدَ الكتابةِ **داخلَ معاملةِ** `backend/app/Modules/LiveSessions/Actions/BookSeat.php` — رفضُ الرصيدِ يردُّ المقعدَ في نفسِ المعاملة.
- [ ] T050 [US2] ⛔ **مقعدُ الاشتراكِ لا يُحجَزُ له رصيدٌ إطلاقاً** — فرعٌ صريحٌ في `backend/app/Modules/LiveSessions/Actions/ClaimSubscriptionSeats.php` — ⚠️ يمرُّ بـ`BookSeat` نفسِها في حلقة، **فبلا فرعٍ هنا يُحجَزُ رصيدُ مشترِكٍ لا رصيدَ له** (٠٢٧ · FR-041: «مقعدُ المشترِكِ يجبُ ألّا يخصمَ رصيداً»). **وهذا هو البابُ الثاني الذي أغفلَ فيه هذا العملُ ٠٢٧.**
- [ ] T051 [US2] ⛔ فُكَّ الحجزَ في **المواضعِ الستّةِ التي تُطلِقُ المقعد**: `CancelBooking` · `CancelClassSession` · `CreateFreezePeriod` · `Jobs/ReleaseIneligibleBookings` · `Listeners/ReleaseSeatsOnSubscriptionEnd` · `Listeners/ReleaseSeatsOnTransfer` — كلُّها تحت `backend/app/Modules/LiveSessions/` — ⚠️ **وثلاثةٌ منها كتابةٌ جُمليّةٌ لا تُحمِّلُ نماذجَ، فلا شيءَ يعملُ وراءَها تلقائيّاً**. الخطّةُ الأولى ذكرَت اثنَين.
- [ ] T052 [US2] ⛔ أضِفْ ذراعَ فكٍّ صريحةً إلى `backend/app/Modules/LiveSessions/Actions/AbandonClassSession.php` — ⚠️ **الحالةُ السابعةُ بلا إغلاقٍ أصلاً**: المدرّسُ لم يفتحِ الغرفةَ قطُّ، فلا سجلَّ ولا حدثَ ولا إطلاقَ مقاعد ⇒ **الحجزُ يبقى معلَّقاً إلى الأبد، والثابتُ الليليُّ أخضرُ لأنّ الصفَّ غيرُ مختومٍ فعلاً**.
- [ ] T053 [US2] عدِّلْ `backend/app/Modules/Payments/Support/CreditLedger.php` لتطرحَ الأرضيّةُ المحجوزَ — ⚠️ **في سؤالِ «هل يكفي لالتزامٍ جديد؟» وحدَه**، و`CAST(... AS SIGNED)` على الطرفَينِ مع السببِ بجانبَه. ⛔ **ويُمنَعُ طرحُه في «هل هذا الطالبُ محجوب؟» أو «هل يدخلُ الغرفة؟» أو «هل يُشغِّلُ الدرس؟»**: طالبٌ رصيدُه ١ حجزَ حصّةً يصيرُ متاحُه صفراً، فالطرحُ هناكَ **يُلغي له المقعدَ الذي سبَّبَ الحجزَ، ويمنعُه من غرفتِه، ويُطفئُ عليه كورساً دفعَ ثمنَه**.
- [ ] T054 [US2] ⛔ في `backend/app/Modules/LiveSessions/Models/ClassSession.php`: `holdsSeat()` يقرأُ **حالةَ الحجزِ لا وجودَه** (FR-030) — ⚠️ السؤالُ اليومَ **لا يقرأُ الحالةَ إطلاقاً**، فالحجزُ الملغيُّ لا يزالُ يُقرأُ مقعداً: **احجزْ ثمّ ألغِ ⇒ الحجزُ عادَ والمحتوى مفتوحٌ بلا ثمن**.
- [ ] T055 [P] [US2] وسِّعْ حمولةَ رصيدِ الطالبِ بـ`available_credits` و`held_credits` **عدَدَينِ لا مالاً**، وأضِفْهما صراحةً إلى `backend/app/Modules/Payments/Support/StudentBalanceAllowlist.php`. ⛔ **ومعهما مفتاحُ الكورسِ دائماً**: «المتاح» رقمٌ لكلِّ (طالب · كورس)، **وجمعُ الأرصدةِ ممنوعٌ نصّاً** — +١٠ في الرياضياتِ و−٦ في الفيزياءِ تُقرَأُ +٤ ومفتوحةً عندَ الجمع.
- [ ] T056 [P] [US2] أضِفْ حالتَي `credit_holds` و`session_unlocks` إلى `backend/tests/Feature/Tenancy/WorkspaceIsolationTest.php` — **شرطٌ دستوريٌّ لا اجتهاد**، وهو الاختبارُ الذي انقلبَ حينَ تبيَّنَ أنّ `credit_balances` **مقيَّدٌ بمساحةِ العمل** خلافاً لأوّلِ كتابةٍ للخطّة.
- [ ] T057 [US2] ⛔ اكتبْ ترحيلَ حجوزاتِ الرصيدِ للحجوزاتِ القائمةِ بالشرطِ **الضيّق** تحت `backend/app/Modules/Payments/Database/Migrations/`: محجوزٌ · قابلٌ للمحاسبة · وحصّتُه **مجدولةٌ وقادمةٌ** ولم تُحاسَبْ · ولها كورس · **ورصيدُه يُحَلُّ فعلاً** — ⚠️ **مقيسٌ: ١٨ حجزاً «محجوز» ومنها صفرٌ على حصّةٍ قادمة**، فالشرطُ الساذجُ يكتبُ ١٨ حجزاً ميّتاً (ثلاثةَ عشرَ منها على طالبٍ واحد) **لا شيءَ في الشجرةِ يستطيعُ ختمَها**، تُطرَحُ من متاحِه إلى الأبد. **يكتبُ صفرَ صفوفٍ على بياناتِ اليوم، وهو الجوابُ الصحيح.** ويمشي بالتقطيعِ **بالمُعرِّف** لا بالإزاحة، و`DB::table()` لا نموذج، والعدّادُ يُحسَبُ **من الصفوفِ المكتوبةِ فعلاً** في العبارةِ نفسِها.
- [ ] T058 [P] [US2] اعرضِ المملوكَ والمتاحَ بكلمتَينِ مختلفتَينِ في ستّةِ عروضِ الرصيدِ تحت `frontend/src/`، ورسالةَ الرفضِ ومعها **متى يُفرَجُ أوّلُ حجز** (US2 · سيناريو ٢).

**Checkpoint**: الحجزُ لم يعدْ مجّانيّاً، **وصفرُ حجزٍ يبقى معلَّقاً بعدَ الإغلاق** (SC-004).

---

## Phase 5: US3 — المدرّسُ يُحاسَبُ على مَن حضرَ فعلاً (P3)

**Goal**: عشرونَ مقعداً وحضورُ اثنَي عشر ⇒ المحاسبةُ على اثنَي عشر، والكشفُ يعرضُ الرقمَين.

**Independent Test**: كما في المواصفة، **مع حصّةٍ فارغةٍ في نفسِ التجهيزة**.

- [ ] T059 [P] [US3] اكتبْ `backend/tests/Feature/Settlement/AttendedSeatsAccrualTest.php` — والفتحُ المتأخّرُ **لا يُحرِّكُ مستحقَّ المدرّسِ بحرف** (FR-015).
- [ ] T060 [US3] اقرأِ العددَ المُجمَّدَ في `backend/app/Modules/Settlement/Actions/AccrueTeachingUnits.php` عبرَ الحدثِ الذي وسَّعَه T012 — **لا استيرادَ ولا استعلامَ عبرَ الحدّ**.
- [ ] T061 [US3] ⛔ أضِفْ `attended_seats` إلى `backend/app/Modules/Settlement/Support/TeacherFieldAllowlist.php` في **موضعَين** (الكشفُ **والتصدير**) — ⚠️ **وحدُّ ما يُسمَحُ دقيق**: «كم حضر» و«كم مقعداً محجوزاً» عددانِ مسموحان، **و«كم استُهلِك» رقمُ فوترة** — ووضعُه هناكَ يكسرُ الجدارَ بينَ حسابِ المدرّسِ وحسابِ الطالب، **وهو حارسٌ يُسقِطُ البناءَ لا عُرف**.
- [ ] T062 [US3] ⛔ راجِعْ قاعدةَ «صفرٌ مريب» في `backend/app/Modules/Payments/Jobs/ChargeUnbilledDeliveriesJob.php`: حصّةٌ بصفرِ حاضرينَ صارَت **حالةً عاديّةً** بعدَ ٠٣٥ لا إشارةَ عطب — مع إبقاءِ معاملةِ الحصّةِ الفارغةِ القائمةِ كما هي، **ولا تُخترَعُ لها قاعدةٌ ثانية**.
- [ ] T063 [P] [US3] اعرضِ المحجوزَ والحاضرَ **جنباً إلى جنب** في `frontend/src/app/(app)/(shell)/manage/settlement/page.tsx` و`frontend/src/lib/settlement.ts`، وجملةً تقولُ إنّ المحاسبةَ على الحاضر — ⚠️ **في عقدِه من أوّلِ يومٍ لا في كشفِ حساب**: سيرى استهلاكاً أكبرَ ممّا حوسِبَ عليه، **واكتشافُ ذلكَ من كشفٍ خلافٌ مضمون** (FR-015أ).

---

## Phase 6: الحرّاسُ والصقل

**Purpose**: ما يُسقِطُ البناءَ إن نُسِيَ، وما يحرسُ **غياباً** لا وجوداً.

- [ ] T064 ⛔ اكتبْ حارسَ SC-008 في `backend/tests/Feature/Courses/ProgressDenominatorUnchangedTest.php`: كورسٌ فيه حصصٌ حيّةٌ ومحتواها مقفول ⇒ **مقامُ نسبةِ الإتمامِ والتسلسلُ لم يتغيّرا** — ⚠️ **معيارٌ يحرسُ غياباً، وهو الشكلُ الوحيدُ الذي يكشفُ توسيعاً يدخلُ بحسنِ نيّةٍ بعدَ شهر**، وهو عائلةُ العطبِ المسجَّلةُ **ستَّ مرّاتٍ** في هذا المستودع.
- [ ] T065 ⛔ امنعْ تعديلَ `duration_minutes` بعدَ فتحِ الغرفةِ في `backend/app/Modules/LiveSessions/Actions/UpdateClassSession.php` و`backend/app/Modules/LiveSessions/Http/Requests/UpdateClassSessionRequest.php` — ⚠️ **عطبٌ أمنيٌّ مقيسٌ، وFR-004 لا يغطّيه**: المنعُ القائمُ يشملُ «المنتهيةَ والملغاةَ» **ولا يشملُ الجارية**، فمدرّسٌ يُنزِلُ المدّةَ في الدقيقةِ الخمسينَ **يُهبِطُ حدَّ الحضورِ فيخصمُ من كلِّ من مرَّ خمسَ دقائقَ ويدفعُ لنفسِه عنهم** — ونفسُ الرافعةِ تُهبِطُ حدَّ «الحصّةُ سُلِّمَت» عنه هو.
- [ ] T066 [P] اكتبْ `backend/tests/Feature/Payments/VerdictImmutabilityTest.php` لـSC-012: حرِّكْ نسبةَ الحضورِ المطلوبةَ في `platform_settings` ثمّ أعِدْ قراءةَ حصّةٍ ماضية ⇒ **لا يتغيّرُ شيء**.
- [ ] T067 [P] وسِّعْ `backend/tests/Feature/Performance/QueryBudgetTest.php` ليقيسَ قراءةَ المنهجِ **بمحتوى حصّةٍ مقفولٍ في التجهيزتَين** (SC-013) — ⚠️ **تجهيزةٌ بلا محتوى حصّةٍ تمرُّ بلا أن تلمسَ الفرعَ الجديدَ إطلاقاً**، وتُحمَّى حتى تستقرَّ قبلَ القياس.
- [ ] T068 [P] أضِفْ `backend/tests/Feature/Payments/SubscriptionSeatUnaffectedTest.php`: مقعدُ اشتراكٍ ⇒ **صفرُ حجزِ رصيدٍ · صفرُ خصمٍ · وفتحٌ تلقائيٌّ بصفرِ حصص** (٠٢٧ محفوظٌ بكاملِه).
- [ ] T069 [P] اكتبْ حالةَ «الطالبُ بلا مقعدٍ قطُّ يفتحُ» في `backend/tests/Feature/Payments/UnlockWithoutSeatTest.php`: سجَّلَ متأخّراً · انتقلَ مجموعةً · انتهى اشتراكُه — **الشرطُ التسجيلُ ورؤيةُ المجموعةِ للحصّة، لا حيازةُ مقعدٍ مضى**.
- [ ] T070 [P] تحقَّقْ أنّ `backend/app/Modules/LiveSessions/Actions/OverrideAttendance.php` **لا يمسُّ الحكمَ المالِيَّ** واكتبِ الاختبارَ الذي يُثبِتُه (FR-004) — **لا تغييرَ في الكود، والاختبارُ هو المهمّة**.
- [ ] T071 [P] اكتبْ حالتَي «العذر» المتعمَّدتَينِ في ملفٍّ واحد `backend/tests/Feature/LiveSessions/ExcusedTwoMeaningsTest.php` (FR-007أ): **في المال** لا يُخصَمُ ومحتواه مقفول، **وفي التربية** يحجزُ التاليةَ عاديّاً — ⚠️ **كلمةٌ واحدةٌ بمعنيَينِ في منتَجٍ واحد**، وأوّلُ مبرمجٍ يوحّدُهما بحسنِ نيّةٍ يكسرُ أحدَ البابَين.
- [ ] T072 [P] حدِّثْ `docs/README.md` و`docs/erd.md` بالجدولَينِ والأعمدةِ الأربعةِ وجدولِ الحالاتِ الخمس (FR-008د).
- [ ] T073 شغِّلْ `php vendor/bin/pest` على **المجلّداتِ المتغيّرةِ وحدَها** (Payments · LiveSessions · Learning · Settlement · Courses · Community · Tenancy · Performance) **مرّةً واحدةً في النهاية**، ثمّ `./vendor/bin/pint` و`./vendor/bin/phpstan analyse`، و`npm test` و`npx tsc --noEmit` في `frontend/`.

---

## Dependencies

```
Phase 1 (المخطَّط)          ──┐
Phase 2 (المُعيق) ⛔          ─┴─→  كلُّ ما بعدَه. لا استثناء.
                                   T013 قبلَ أيِّ توكيدِ فوترة
                                   T009 قبلَ T010 · T011 · T012 (الأسماء)

Phase 3 (US1) ──→ Phase 4 (US2)   T023–T025 تُنتِجُ الحكمَ الذي يختمُه T048
             └──→ Phase 5 (US3)   T024 يُنتِجُ العددَ الذي يقرأُه T060
Phase 6 يقرأُ الثلاثةَ معاً
```

⚠️ **وUS1 وUS2 وUS3 لا تُشحَنُ منفردة** (FR-021 · Summary): US1 بلا US2 يجعلُ الحجزَ مجّانيّاً، وUS1 بلا US3 يجعلُ المنصّةَ تدفعُ عن كلِّ غائبٍ بخسارةٍ بلا سقف. **التقسيمُ ترتيبُ بناءٍ لا خطّةُ إصدار.**

## Parallel Opportunities

- **Phase 1**: T001–T006 معاً (ترحيلاتٌ مستقلّة)، ثمّ T007 · T008.
- **Phase 2**: T012 · T016 · T017 · T018 معاً بعدَ T009.
- **Phase 3**: الاختباراتُ T019–T022 معاً؛ والواجهةُ T039–T042 معاً بعدَ T037.
- **Phase 4**: T045 · T046 معاً؛ T055 · T056 · T058 معاً.
- **Phase 6**: T066–T072 كلُّها معاً.

## MVP

⛔ **لا MVP في هذه الشحنة، وهذا قرارٌ لا سهو.** أصغرُ شيءٍ يُشحَنُ هو **Phase 1 + 2 + US1 + US2 + US3**. شحنُ US1 وحدَها يُنتِجُ منتَجاً يحجزُ فيه الطالبُ مجّاناً وتدفعُ فيه المنصّةُ عن كلِّ غائب — **أسوأُ من اليومِ لا أفضل**.

## عددُ المهامّ

| الطور | العدد |
|---|---|
| ١ — المخطَّط | ٨ |
| ٢ — المُعيق | ١٠ |
| ٣ — US1 | ٢٦ |
| ٤ — US2 | ١٤ |
| ٥ — US3 | ٥ |
| ٦ — الحرّاسُ والصقل | ١٠ |
| **المجموع** | **٧٣** |
