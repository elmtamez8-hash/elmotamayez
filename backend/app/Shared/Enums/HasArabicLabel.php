<?php

declare(strict_types=1);

namespace App\Shared\Enums;

/**
 * أيُّ مجموعةٍ مغلقةٍ تُعرَضُ على إنسان.
 *
 * ⚠️ العقدُ هو أنّ **مكاناً واحداً** يعرفُ العربيّةَ المقابلةَ لكلِّ قيمة. كانت
 * حالاتُ المقرّرِ مكتوبةً ثلاثَ مرّاتٍ في مورِدٍ واحد — في النموذجِ وفي المرشِّحِ
 * وفي الشارة — فظهرَتِ الشارةُ خاماً (`published`) بينما النموذجُ عربيّ، وهما
 * إجابتان مختلفتانِ عن سؤالٍ واحدٍ في شاشةٍ واحدة.
 *
 * والأسوأُ من اختلافِ الترجمةِ اختلافُ **المجموعةِ نفسِها**: قائمةُ ظهورِ المقرّرِ
 * كانت تعرضُ خيارَين، بينما `CourseVisibility` تحملُ ثلاثة — فحالةُ `hidden`
 * موجودةٌ في قاعدةِ البيانات، ولا سبيلَ إلى بلوغِها ولا إلى الخروجِ منها من الشاشة.
 * اشتقاقُ الخياراتِ من `cases()` يجعلُ ذلك مستحيلاً بالبناء.
 */
interface HasArabicLabel
{
    public function label(): string;
}
