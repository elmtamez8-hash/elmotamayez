<?php

declare(strict_types=1);

use App\Modules\Courses\Models\Course;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

/**
 * ⛔ **كورسٌ له مجموعةٌ هو كورسٌ جماعيّ، مهما قالَ عمودٌ لم يكتبْه أحد.**
 *
 * `courses.course_type` وُلِدَ في ٢٠٢٦-٠٨-٠١ بقيمةٍ افتراضيّةٍ `recorded` وبلا
 * كاتبٍ في الشجرةِ كلِّها — لا طلبٌ ولا فعلٌ ولا شاشة. فالصفوفُ القائمةُ تحملُ
 * تصنيفاً لم يختَرْه أحد، وله أثرانِ ظاهرانِ اليوم:
 *
 *  · صفحةُ الكورسِ عندَ الطالبِ تُسقِطُ تبويبَ «الحصص» وعدّادَ الحصّةِ القادمةِ
 *    على كلِّ `recorded` — فكورسٌ على الإنتاجِ بثماني حصصٍ جايّةٍ ومجموعةٍ
 *    مفتوحةٍ لا يعرضُ منها شيئاً لأربعةِ طلبةٍ مسجَّلين.
 *  · الصفحةُ العامّةُ تطبعُ شارةَ «مسجّل» على كورسٍ يُدرَّسُ حيّاً كلَّ أسبوع.
 *
 * ⚠️ **و`group` وحدَها تُشتَقّ، و`individual` لا.** وجودُ صفٍّ في `cohorts`
 * حقيقةٌ لا لبسَ فيها: لا يُنشِئُ المجموعةَ إلّا مدرّسٌ بقصد. أمّا «فردي» فلا
 * دليلَ عليه في البيانات — `private_session_minutes` **مدّةٌ لا علامة**،
 * و`RequestPrivateSession::durationOf()` يقرؤُها `?? 60`، أي أنّ فراغَها لا
 * ينفي الحصصَ الخاصّة. فاشتقاقُ «فردي» منها اختراعُ تصنيفٍ لا تسجيلُه، وهو
 * بالضبطِ العطلُ الذي تُصلِحُه هذه الهجرة. ما عداها يبقى كما هو، ويختارُه
 * مدرّسُه من شاشةِ التعديل.
 *
 * ⚠️ **بيانٌ واحدٌ لا مشية**: `chunkById` احتياطُ من يمشي الصفوفَ، ولا صفوفَ
 * تُمشى هنا. وهو صالحٌ للإعادةِ بطبعِه — الشرطُ نفسُه يُطابِقُ صفراً في المرّةِ
 * الثانية.
 *
 * قِيسَ على الإنتاجِ ٢٠٢٦-٠٩-١٥: ٧ كورسات، ٢ منها لهما مجموعات، وواحدٌ فقط
 * يتغيّرُ (الآخرُ `group` أصلاً من بذرةِ العرض).
 */
return new class extends Migration
{
    public function up(): void
    {
        if (! Schema::hasTable('cohorts') || ! Schema::hasTable('courses')) {
            return;
        }

        DB::table('courses')
            ->where('course_type', '!=', Course::TYPE_GROUP)
            ->whereIn('id', DB::table('cohorts')->select('course_id'))
            ->update(['course_type' => Course::TYPE_GROUP]);
    }

    /**
     * ⚠️ لا رجوعَ لها، وهذا مكتوبٌ بدلَ أن يُخمَّن. القيمةُ السابقةُ لم تكنْ
     * قراراً بل قيمةً افتراضيّة، وإعادةُ الصفوفِ إلى `recorded` تعني كتابةَ
     * تصنيفٍ خاطئٍ عمداً على كورساتٍ تُدرَّسُ في مجموعات.
     */
    public function down(): void {}
};
