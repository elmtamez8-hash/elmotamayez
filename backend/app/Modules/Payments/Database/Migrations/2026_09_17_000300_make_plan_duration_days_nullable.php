<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/*
| ٠٣٦ · T002 — المدّةُ تصيرُ اختياريّةً، لأنّ باقةَ الحصصِ ليست لها مدّةٌ تُكتَب.
|
| ⚠️ **ترحيلٌ منفصلٌ عن إضافةِ عمودِ الحصص، عمداً**: تعديلانِ في `Schema::table`
| واحدةٍ على SQLite إعادةُ بناءٍ للجدولِ كاملاً — وهي مقايضةٌ رفضَها هذا
| المستودَعُ مرّتَينِ مكتوبتَين.
|
| ⛔ **و«غيرُ موقَّع» مُعادٌ ذكرُه هنا صراحةً، وحذفُه عطبٌ لا يراه أيُّ اختبار.**
| MySQL يُعيدُ إعلانَ العمودِ بما في المخطَّطِ **وحدَه**: كلُّ ما لا يُذكَرُ في
| `->change()` يسقط. فلو كُتِبَ `integer('duration_days')->nullable()->change()`
| صارَ العمودُ موقَّعاً على الإنتاجِ ويقبلُ مدّةً سالبة — **ولا شيءَ هنا يراه**،
| لأنّ SQLite لا تعرفُ الفرقَ بينَ موقَّعٍ وغيرِه أصلاً. العائلةُ نفسُها التي
| كلّفَت `CAST(... AS SIGNED)` في `CreditLedger` فحصاً كاملاً.
*/
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('plans', function (Blueprint $table): void {
            $table->unsignedInteger('duration_days')->nullable()->change();
        });
    }

    /*
    | ⚠️ **فارغةٌ عمداً، والسببُ مكتوبٌ هنا لأنّ الصمتَ يُقرَأُ كسهو.**
    |
    | إعادةُ العمودِ إلزاميّاً تفشلُ على أوّلِ صفٍّ بشكلِ الحصص — وهي الصفوفُ التي
    | يوجَدُ هذا الترحيلُ من أجلِها. فالتراجعُ إمّا يُسقِطُ بياناتِ مدرّسٍ أو
    | يتوقّفُ في منتصفِه، وكلاهما أسوأُ من عمودٍ يقبلُ الفراغَ في مخطَّطٍ رجعَ
    | خطوةً. السابقةُ في هذا المستودَعِ ترحيلُ تخفيفِ عمودِ الامتحان، ويقولُ
    | الشيءَ نفسَه فوقَ نفسِه.
    */
    public function down(): void {}
};
